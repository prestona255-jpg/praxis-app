---
round: r-arc
title: R-ARC — knowledge-arc theorizing system
kind: round-doc
status: in round (opened 2026-07-15) — WAVE B FORMALLY CLOSED 2026-07-17 (felt verdict v3.220 ALL FOUR PASS); Wave C in progress: S6a spike · S6b v3.221 · S6c v3.222 · RAIL v3.223 · ROOM-1 v3.224 · S7 v3.225 · ROOM-2 v3.226 · ROOM-3 v3.227 (pushed+live, felt PASS) · SLICE 8 v3.228 (dismissal store, COMMITTED LOCAL — HALT for Preston's push word)
scope: The full journey — capture → marginalia → sub-theory → arc → theorize → publish — as one intelligent, structurally sound loop. R-ARC cut = S1 (the Door) + S2 (the Seedling) + S3 (the Room), deep; S4 (the arc's own argument) + S5 (the return current) = named follow-on rounds.
adjacent: arc interior (arc-detail) · sub-theory writing (subtheory-build) · R10 dependency · Yumi generative
---

## State

Not a surface — a **round doc** for the knowledge-arc theorizing system: the full journey
(capture → marginalia → sub-theory → arc → theorize → publish) as one intelligent,
structurally sound loop. Adjacent threads: the arc interior, sub-theory writing, the
R10 dependency, and Yumi generative.

**The round is OPEN** (promoted to `## Now` at the 2026-07-15 DW/S-A close). Its constitution is
`docs/studio/r-arc-brief.md` (v4) — no longer thin-by-design, and no longer awaiting a scoping chat:
SHAPE-A's six forks, the pedagogy/epistemology layer, the critique's amendments, and the
seed-lifecycle amendment are all decided. The brief governs; `docs/studio/r-arc-handoff.md` is its
brief-holder (superseded in turn by `r-arc-wave-b-handoff{,-2,-3}.md`).

**WHERE IT STANDS (2026-07-17):** every build slice of Wave A + Wave B has **shipped and is live** —
S1→S5 plus the F4 covenant removal and the FF-7 vocabulary rider (v3.211→**v3.220**). **S1–S4 are
felt-passed;** the CLOSE now awaits Preston's felt pass on **S5 (the dissolve, v3.219)** and **FF-7
(the vocab rider, v3.220)**. **Wave C OPENED 2026-07-17** (Fable 5 at
default effort per MODEL LAW; rulings issued under Preston's design-partner deferral). **S6a — the
caret-safety spike — ran first and its PASS is RATIFIED** (non-destructive decoration proven;
`docs/checkpoints/r-arc-s6a-spike.md` v2, gate-hardened: red-team BLOCK fixed by committing the
Phase-B drivers into the harness + full re-run; reviewer CLEARED). F-C stands permanently
(display-layer only; decorate-on-pause retired as unneeded, recorded as fallback of record).
**Approved Wave C slotting:** 6b (matcher+index) → 6c (read-only lighting) → RAIL SLICE (FF-12 +
UNFILED-REACH F1–F4 + FF-2 riding, destination-named door law) → 7 Room lighting → 8 dismissals
(F-D remembered) → 9 Yumi raised-hand seat + §C wording → 12 Room composition (+ FF-1 riding;
FF-11 rides CONDITIONALLY — only inside Slice 12's declared band, else back to the debt ledger).
**HARD GATE: no Wave C code pushes until Preston's combined 4-check felt verdict on v3.220 lands**
(gathering/draft vocab · honest doors · dot/word pairing · dissolve). Per-surface studio-census
re-measure across the touched surfaces (arcs, arc-detail, subtheory-build, subtheory-page,
notebook) remains the **deferred backlog carried to the round CLOSE**.

## Round history

- **2026-07-15 — GATE + Stage 0 recon.** Gate 4/4 on origin evidence (Now = R-ARC; DW/S-A close
  `fa278d1` an ancestor of origin/main; brief on origin, local md5 == origin blob; no other
  regen-owner; HEAD `42ef1a3` / live `praxis-v3.210`, deploy current). **The brief §9 F-DL
  first-check closed NO-INCIDENT** on Preston's ruling — no lost July-14 draft exists to find; the
  resume-flow friction stays in scope as design work. (The check was *unrunnable*, not negative: the
  real account's cloud is `permission-denied` to the test session and its local cache stops
  2026-07-13 — a day before the event. Recorded so it is never re-litigated.) Recon:
  `docs/checkpoints/r-arc-recon.md` — **seven forks (F1–F7) raised for Preston**, three of which
  correct the brief's own premises (the register default already exists; the pull rail is already
  corpus-wide; resume-flow is four independent causes, not one).
- **The build slice ledger (SHAPE-B → BUILD, each shipped+live; SHA · cache · one-line).** S1–S4
  felt-passed; S5 + FF-7 felt-pass PENDING. Per-slice checkpoints at `docs/checkpoints/r-arc-*.md`.
  - **S1** `98738b0` · v3.211 — the disabled-state law (a dead `.btn` looked alive app-wide) [the Door]
  - **S2** `ba4888c` · v3.212 — local-first notebook persistence, the hard gate [the Seedling]
  - **S3** `36dc570` · v3.213 — the ember: arc lifecycle + rename + graduation [the Room]
  - **S3R** `c681d35` · v3.214 — un-graduate: graduation's reverse gear
  - **S3B** `02b517e` · v3.215 — the basin: identity + naming (form follows naming)
  - **S3B-POLISH** `bb70889` · v3.216 — basin display fixes from the felt pass (also unified the maturity **word** ramp → `_stMaturityWord`)
  - **F4** `3fefc93` · v3.217 — remove the "From how you read" Yumi blocks (the covenant)
  - **S4** `62eeca8` · v3.218 — one door: three creation paths collapse, FF-8 row, arc-picker relit
  - **S5** `4a8f425` · v3.219 — reverse gear: dissolve a basin back to loose motes — **felt pass PENDING**
  - **FF-7** `8a20bf8` · v3.220 — vocab rider: GATHERING/DRAFT/FINISHED lifecycle, destination-named doors, unified maturity **dot** ramp (.34/.67, closes the Arc-Read dot/label residual) — **felt pass PENDING**
  - **S6a** (Wave C, docs-only) — caret-safety spike **PASS RATIFIED 2026-07-17**: per-text-node splitText+span wrap, abs-offset caret carry, serializer transparency; getValue value-identical in every case; undo stack unpolluted; spans expendable by design. Harness `.claude/rig/spike-caret.html` rides the first Wave C code commit. Record: `r-arc-s6a-spike.md` + `-recon.md`
  - **S6b** (Wave C) `5f2bce0` · v3.221 — deterministic recognition: `js/recognition.js` (own-library index + prose scanner, raw-offset map, longest-wins, term floor 4); supervised Sonnet build, session-caught Unicode offset-desync patch; red-team 2 HOLD documented · reviewer CLEARED; **PUSHED + LIVE** after the felt-verdict gate cleared. Record: `r-arc-s6b.md`
  - **WAVE B FORMALLY CLOSED 2026-07-17** — felt verdict v3.220 **ALL FOUR PASS** (gathering/draft vocab ✓ · honest doors ✓ · dot/word pairing ✓ · dissolve ✓). One intake finding: **WAVE-C-INT-1 "Na…"** (workshop narrow-column placeholder mid-word clip, FF-9 family) — proposed vehicle Slice 12.
  - **S6c** (Wave C) · v3.222 — read-only recognition lighting, **NOTEBOOK marginalia cards only** (one render site, views.js:2210 path; arc-detail's rail is plain-textContent and UNLIT — a recon claim to the contrary was caught FALSE at the gates and corrected). Spike-technique decorator + quiet non-interactive `.rec-lit` (DWF-1); rig-verified incl. cross-boundary in-`<strong>` segments + F6 timing (R5 numbers). Red-team BLOCK (doc integrity) fixed-with-evidence · reviewer HOLD (currency) fixed in-commit. Record: `r-arc-s6c.md`
  - **RAIL** (Wave C) · v3.223 — FF-12 gathered-evidence panel beside the canvas + UNFILED-REACH F1–F4 as ruled (global Unfiled rail group · "· N UNFILED" Page tail · weaving-never-files PROVEN live · no Inbox sibling) + FF-2 destination-named "Open the book →" doors. **HARD BAND BREACH halted honestly → re-banded by Preston's word at measured size** (views ≤7,400; the DOM-builder ~38 B/line pricing lesson is now standing). Reviewer CLEARED (4-feature hunk map + to-the-byte re-derivation, per conditions); red-team procedural BLOCK (smoke minimum) fixed with evidence. **PUSHED + LIVE.** Record: `r-arc-rail.md`
  - **ROOM BRIEF** `ee01554` (docs) — INT-2/3/4 charter brief; **ALL EIGHT RULINGS landed** (D3 create-lands-in-Room = knowing S4 reversal; D8 Finish-gate reaffirmed as shipped law, FINISH-CHOREO named; T11 covenant ratified into the plan roster). **PUSHED.**
  - **ROOM-1** (Wave C) `c33f22c` · v3.224 — **THE FIELD**: `js/room-field.js` (draggable evidence cards, 1200×900 logical canvas, native-pan M1-lite, press-hold touch, tap-lift) + `evidenceLayout` (additive, T3 both paths, T11 grep-clean at 3 traced sites) + **C1: create LANDS in the Room** (one-gesture return chip; newborn receipt kept). Two hard band breaches halted → re-banded by Preston's word. Red-team 2 HOLD + 3 NOTE → 4 FIXED + re-probed live, 1 → RM6. Reviewer CLEARED. **PUSHED + LIVE.** Record: `r-arc-room1.md`
  - **POST-S7 FELT CHECKPOINT (Preston, 2026-07-17): FUNCTIONAL PASS** — all four checklist items under real hands (field drag+reload · lighting/caret incl. both red-team spots · C1 landing/return/receipt · rail groups). Intake: INT-5 scrollbars · INT-6 sparse field · INT-7 workshop column · INT-8 card grammar (all → ROOM-3) · **INT-9 Bloom-hint collision → ON-8 (overnight)** · RM6 ruled wrong-feeling (ROOM-3 evidence) · Arc-Field findings → POST-WAVE LEDGER · FINISH-CHOREO vehicle → post-ROOM-3 slotting.
  - **ROOM-2** (Wave C) · v3.226 — **THE NOTE DOOR (data-write slice)**: `#note/<id>` — the note as a PLACE (view + provenance mirror-read with FF-7 doors) + **DURABLE register-aware editing closes MARG-EDIT** (canvas for marginalia; plain+autosave for journal/question — newlines byte-exact) + card-body open (selection-guarded) + field-card lifted door + **absorbs Slice 10** (spotlight/#search deep-links, dead en.title match retired). **The quote-snapshot law proven live** (woven quotes byte-frozen through edits + reload). Red-team deep (data-loss tier): B1 unbounded plain-editor loss window → FIXED by design resolution (**autosave model; Cancel removed — a discard promise can't coexist with autosave**); H2 sole-branch lens leak → fixed (my "conservative" framing was backwards — owned); N3 touch-writes → setter guard. Reviewer CLEARED (LOCAL ONLY). **COMMITTED LOCAL — the data-write exception holds: NO push without Preston's explicit word.** Record: `r-arc-room2.md`
  - **S7** (Wave C) · v3.225 — **ROOM LIGHTING, LIVE**: `_recogLightLive` (pause-cadence 600ms; abs-offset caret carry EVERY pass; strictly-inside restore; non-collapsed-selection + IME guards; decoration-aware memo). Red-team 2 BLOCK — **the wave's sharpest catches** (end-of-doc restore mathematically dead · undo-races-debounce dark-forever) — both fixed as ruled safety code (re-band ≤3,900) + LAW-PROVEN live (clamp signature exact; the race driven to re-light). FEEL measured: 0.5ms live pass · 5.1ms at 4,600 chars/120 spans; no motion by design. F-C held at the persistence layer (autosave byte-clean through live spans). NUL-byte edit-payload incident caught by diffstat + repaired (standing gotcha). Reviewer CLEARED. **Pushed under Run Mode v2** (autonomous green path). Record: `r-arc-s7.md`
  - **ROOM-3** (Wave C, Opus execution) · v3.227 — **WORKSHOP COMPOSITION** (the felt-brief's largely-subtractive recomposition — five pieces of furniture removed, none added). RD-2 the lengthening page (`room-field.js`: bounded width + growing height + NO inner scroll — the RM6 keyhole killed by construction; carved ground, not pane-in-panel) · RD-3 collision-avoiding row-major spawn + empty state ("Notes you gather will land here") + typographic clip-law · RD-4 one mono kicker + quiet court + RD-4a door-mirror corner (Finish + Open-the-page) + DE-CARDED sheet · RD-5 the "Gathered for this piece" panel DISSOLVES (INT-8) — the field is the sole evidence surface, its count folds into the header, one-click card door by kind (F2: entry→#note · book→#book · external→lift) · §2a P1 motion-free / P2 keyboard / P3 focus-coherent · RN1 dead `.note-edit-cancel` removed · RS4 `_recogLightLive` double-attach guard. **NO data-write** (positions re-interpreted at display only; `setEvidenceLayout` unchanged, state.js byte-identical). Gates: recon ×10 + recon-validation + red-team (2 BLOCK — `rfield` strict-mode crash & spawn x-clamp, both fixed; 3 residual dispositioned) → reviewer CLEARED on a frozen tree. Rig live-verify FULL PASS (console clean; F2/drag/empty-field/basin-court + Shelf/Arcs/Notebook smoke). **PUSHED + LIVE — mandatory POST-ROOM-3 FELT CHECKPOINT pending (Preston).** Record: `r-arc-room3.md` (+ `-recon.md`).
  - **POST-ROOM-3 FELT CHECKPOINT (Preston, 2026-07-18): FUNCTIONAL PASS** — every §3 canon item + both §9 questions pass at feature level; **drag works but is under-developed (a feel finding, not a ROOM-3 defect → R-POLISH L4).** Carried determinations ALL ACCEPTED: F2 kind-doors · F4/RM-COORD one-time display shift · D4 comment-only seat · FF-11 → debt ledger. **RM6 field/rail width DEFERRED** — resolved by R-POLISH, do NOT widen now. **INTAKE (evidence: Preston's three 1920px screenshots):** P-A big-screen occupancy collapse ≥~1600 (Home/Notebook/workshop float in voids at 1920 — the ledgered 2560-sag, arrived early) · P-B CONTROL DIALECT FRACTURE (native `<select>` in Pull-from-reading, default inputs, mixed chip/button styles — no ruled control canon app-wide) · P-C candy glyph palette leaks beyond the arc panel (Home arc thumbnails, workshop glyph) · P-D bare Yumi caption = a FAMILY of 3 strings (workshop/Home/Notebook) · P-E drag lacks physical choreography (lift/settle) · P-F sparse field reads uncomposed at wide widths. **RULINGS:** (1) **R-POLISH = new named pre-launch round, 5 lanes** — L1 XL-tier canon ≥1600 (designed at 1920 + per-page passes) · L2 control canon rule+sweep · L3 RD-1 glyph slice WIDENED (arc panel + Home thumbnails + workshop glyph) · L4 drag choreography · L5 caption-family removal (absorbs ON-8's three strings). Slots **TOP of Next at the close-out re-plan, AHEAD of SCAN/R-SHELF** (the control canon must precede control-heavy rounds); opens with a Fable design session, builds Opus. (2) **ON-8 RE-SCOPED** into R-POLISH L5 — no longer a standalone overnight item. (3) **Wave C continues to completion first.** **NEXT: Slice 8 (remembered dismissals, DATA-WRITE — LOCAL + HALT for push word) → Slice 9 → close-out re-plan.**
  - **SLICE 8** (Wave C, Opus) · v3.228 — **THE DISMISSAL STORE (REQ#1c, DATA-WRITE)**: Candidate 1 (minimal, Preston-ruled). The Portrait through-line "Set aside" becomes a durable+synced TOMBSTONE — `dismissReaderThread` flips `status='dismissed'` IN PLACE (FORWARD-ONLY, F-B) instead of a hard delete, completing the half-built readerModel path (the read-filter was already live-but-dead). **By-id** (not overlap-keyed: manual threads have empty memberNoteIds — proven at the rig). Rides the EXISTING readerModel plumbing (replaceReaderModel coerces 'dismissed' on load; threads sync wholesale) — **ZERO integrations.js/schema change**; consent gate unchanged; T2 gate untouched (0 yumi-brain.js lines). Gates: recon + recon-persist → fix-red-team DEEP (data-loss tier) **CLEAN** (every readerModel mutator traced; the sole reverse-writer `addReaderThreadFromName` is dead/0-callers) → praxis-reviewer **CLEARED (LOCAL ONLY)**, all 10 gates byte-for-byte. Rig data-loss battery **FULL PASS** (flip-not-delete + control · manual-by-id · forward-only/idempotent · persistence · load-coercion; console clean). **COMMITTED LOCAL — the data-write exception is ABSOLUTE: NO push without Preston's explicit word.** Follow-ons logged: **DISMISS-UNIFY** (C2 — app-wide durable dismissals across all four mechanisms; ground doc = the recon; slotting at a future re-plan) · **RM-SPLAT** (replaceReaderModel REPLACE-splat clobbers a sibling device's offline write, F-DL5-adjacent → beta-readiness basket, LAUNCH-STATUS.md). Slice 9 drift lesson recorded (D14 raised-hand vs shipped ROOM-3 — 3rd doc-drift instance; Slice 9's recon opens by reconciling it). Record: `r-arc-slice8.md` (+ `-recon.md`).

## Next

- **Preston's felt pass on S5 (dissolve, v3.219) + FF-7 (vocab, v3.220)** → then the round CLOSE
  (surface-census re-measure across the five touched surfaces + BOARD + Builder regen).
- **Wave C IN PROGRESS (Fable 5 rounds; ROOM-3 = Opus execution, 2026-07-18)** — S6a spike ·
  S6b v3.221 · S6c v3.222 · RAIL v3.223 · **ROOM-1 field v3.224 · S7 Room-lighting v3.225 ·
  ROOM-2 note-door v3.226 · ROOM-3 workshop-composition v3.227 — ALL PUSHED + LIVE.** The Room
  charter (INT-2/3/4) is ruled and built; the room-felt-brief (RD-1..RD-6) governed ROOM-3.
  **POST-ROOM-3 FELT CHECKPOINT = FUNCTIONAL PASS (Preston, 2026-07-18)** — carried determinations
  accepted; RM6 deferred; intake P-A..P-F → **R-POLISH** (new named pre-launch round, 5 lanes,
  top of Next at close-out, ahead of SCAN/R-SHELF); ON-8 absorbed into R-POLISH L5. **NEXT:
  Slice 8 BUILT + COMMITTED LOCAL (dismissal store v3.228) — HALT for Preston's push word; then
  Slice 9 (raised-hand seat, T2, always halts — its recon OPENS by reconciling the D14 raised-hand
  drift vs shipped ROOM-3) → close-out re-plan** (carrying R-POLISH, R-CAPTURE, RD-1-in-L3,
  DISMISS-UNIFY, FINISH-CHOREO). RD-1 arc-Field ground rides R-POLISH L3; C5/FF-10 D8 shipped.
- **Carried named items** (do not lose): FORMING-REACH residual (4 non-lifecycle maturity ramps incl.
  the frozen `arc-constellation.js:1433`, routed into 3B-MOTE's authorized single frozen edit) · §C Yumi
  corner tag wording (deferred to Wave C's raised-hand seat) · the Arc-Read dot/label residual **CLOSED**
  by FF-7 · CAPTURE-OWNER beta-gate hard review · YUMI NOTICE→FOLD watch before Wave C.
