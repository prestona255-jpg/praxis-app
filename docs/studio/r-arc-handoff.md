PRAXIS — R-ARC ROUND (dedicated chat) — FIRE ONLY AFTER THE DW/S-A CLOSE
[Preston: paste the DW/S-A close-out commit SHA here before sending: ______]

This is the round chat for R-ARC: converting Praxis from an exhibition
of finished theory into the instrument theory gets made with. Scope =
S1 (the Door: quick-capture) + S2 (the Seedling Arc) + S3 (the
Inhabited Room), DEEP. SHAPE-A is ALREADY DECIDED — all six forks,
July 14, do NOT relitigate. This chat runs the remaining beats:
GATE → RECON → SHAPE-B mockup → BUILD → CLOSE.

LOAD FIRST: memory_read /areas/praxis-r-arc.md (the decision record —
BINDING), /areas/praxis-studio-protocol.md, /areas/praxis-r6-subtheory.md
(R6 binds: one editor, Finish vocab, the pull rail), 
/areas/praxis-import-capture.md (book-association thread — reconcile
with S1, don't build twice), /areas/praxis-yumi-intelligence.md (frozen
gate + consent substrate), /areas/praxis.md, /areas/praxis-mobile-canon.md,
/areas/praxis-desktop-canon.md.

CANONICAL SPEC: docs/studio/r-arc-brief.md on origin. If the brief and
this prompt disagree, the brief wins. If the brief and the memory
decision record disagree, STOP and ask Preston.

GATE — all four must PASS before any design or build talk. Verify via
the GitHub API on origin/main (api.github.com/repos/prestona255-jpg/
praxis-app), never narrative:
  1. sequence.md Now = R-ARC and the DW/S-A close-out commit (SHA
     above) is on origin. FAIL → stop; report what sequence.md
     actually says.
  2. docs/studio/r-arc-brief.md exists on origin. FAIL → stop;
     Preston commits it first.
  3. No other open regen-owner — no round mid-flight per sequence.md /
     BOARD. FAIL → stop.
  4. Record fresh ground truth: origin HEAD SHA + live cache version.
     ALL anchors this round = function-name grep; zero line-number
     trust.
Report the four checks WITH EVIDENCE, then stop for Preston's GO
before recon.

BINDING DECISIONS (SHAPE-A, July 14 — build these, never reopen):
  F1 CUT = S1+S2+S3 deep; S4 (arc's own argument) + S5 (return
     current) = named follow-on rounds.
  F2 DOOR (AMENDED at critique) = one gesture from anywhere:
     landing stays Home; capture reachable in ONE gesture at zero
     decisions from any surface (persistent compose button +
     home-screen quick action + clipboard-detect offer). LAW:
     local-first capture — TTFK <~400ms, local write + later sync,
     survives offline/auth. iOS Shortcut endpoint = named follow-on.
  F3 = raw lands INSTANTLY, never auto-processed; "Ask Yumi to shape
     this" = explicit act → proto-arc PROPOSAL (question candidate,
     sources matched-to-shelf/flagged-unread, seeds) Preston accepts
     or edits. Mirrors gather→notice→mint.
  F4 = arc-with-SEEDLING-status: same arc object from birth, ADDITIVE
     fields only; graduation = status flip, no migration; field
     renders seedlings small.
  F5 = the deterministic rail grows CORPUS-WIDE (all marginalia +
     journal + sub-theories) AND Yumi rides it — the R6-held margin
     seat mounts: notice-while-writing / corpus retrieval / probe.
     Downstream of the frozen gate, behind existing consent flags.
  F6 = R-ARC owns connection-as-a-theorizing-act (pull/cite/link
     cross-arc from room + field); R10 owns app-wide/ambient viz.

PEDAGOGY + EPISTEMOLOGY LAYER (walked July 14 — EQUALLY BINDING;
full text = brief §4b): notice-and-ask shaping; NEW COVENANT LINE
"Yumi never speaks for the reader"; books-spine + threads (thinkers/
concepts graduate into books); routing by notice-and-ask (new /
fold-in / new-with-kinship-thread); lifecycle earned-then-declared,
seedlings never auto-delete; reading lens active-staged and ALWAYS
SHEDDABLE; first move = seam-first delivered AS the first raised hand
(pre-lit on entry, opens on tap); speaking model = the raised hand
ABSOLUTE (Yumi never speaks unbidden, EVER — no exceptions); critique =
structural grounding probes only (new move family, own eval battery);
retrieval = legible deterministic-first (rail builds candidates, Yumi
selects, every pull carries its why); privacy = two visibilities +
per-chip Finish gate (publish / paraphrase-out / hold); earning =
grounded + made + answering (reflective-threshold question at Finish,
answer captured as the ANSWERING LINE); same grammar at every depth
(floors + fallbacks, never fabricates). CRITIQUE LAWS (July 14,
equally binding — brief §4b/§7b): raw captures join the rail's
candidate corpus + get quiet presence on Home; seedlings keep their
birth eruption (lineage); Finish choreography = privacy sweep first,
then the threshold question as ONE quiet moment (never stacked
modals); SHAPE-B mandates incl. the 60-second demo standard with the
AI-liberation eruption as verbatim mockup fixture. SEED LIFECYCLE (Preston's amendment, binding):
seeds carry only their origin phrase — no title field exists early;
they gather as BASINS (fragments accrete; split/merge); naming is
INVITED at mass and THE NAME IS THE MINT (mark taken, workshop opens
with the basin pre-loaded in the rail); naming never asked, never
forbidden. Visual law: FORM FOLLOWS NAMING — named sub-theories are
shaped marks, unnamed seeds are formless motes, brightness = mass.

OPEN IN-ROUND: the 8 questions in brief §7 (entry-trigger nuance,
inbox surface, seedling rendering, notice cadence/budget, retrieval
mechanics, consent-flag reuse vs new, signed-out posture under CRAFT
§3.1, injection battery for the shaping call). These ARE this round's
design work, alongside SHAPE-B.

NON-GOALS (must not touch): the eval gate / frozen gate region
(function-diff proof required); schema migrations (additive only);
S4/S5 features; R10 visualization; relitigating R5/R6 decisions;
arc-constellation.js, tradition-forms-arc.js, yumi-brain.js, state.js
except via an explicitly authorized single-edit pattern with byte-
frozen siblings.

SIDECAR RIDES THIS ROUND: workshop resume-flow fix + notebook capture
affordances. ALSO INHERITED (DW routing, July 14): the sub-theory
surfaces' DESKTOP COMPOSITION — DW-3's widened workshop + read page
got no DW-POLISH pass; their desktop layout is this round's S3
design work. FELT INPUT: COLLECTED July 14 (seven
desktop screenshots) — findings + validations live in brief §9b;
note the ruling there that the existing Yumi "From how you read"
blocks are SUPERSEDED (removal/migration = in-round work) and that
the Notebook's existing Inbox tab is the S1 reconciliation target. FIRST CHECK AT RECON: Preston's July-14 bounced draft
persists in bodyPublic — if ABSENT, STOP everything and re-open F-DL
before any design work.

RAILS: mobile + desktop canon bind (canon-native, canon is not a
topic); strict ES3; prestonpraxistest for behavioral smoke /
prestona255 read-only; FIX-PROTOCOL discipline; local commits with ONE
stop before any push — nothing pushes without Preston's word; felt
pass on DEPLOYED, fresh Incognito. Mockup standard: judged by Preston
WRITING in it, not screenshots.

Start with the gate.
