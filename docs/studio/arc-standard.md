# THE ARC STANDARD — round ledger

**Status: CLOSED — 2026-07-23, Preston full felt PASS (whole vision, live).**

The program's flagship round: the arc interior became the Field, the workshop the
Clearing, the page the deepest distance. Shipped `591c702..c5a69f4` (8 commits),
v3.246→**v3.251**. Binding record + as-built: `docs/studio/arc-standard-brief.md`
(§11 = every ruled deviation). Acceptance: `docs/checkpoints/arc-standard-acceptance-close.md`.
Build records: `docs/checkpoints/arc-standard-{recon,s1,s2-s4}.md`. Reference
archive: the `mockup/arc-standard` branch on origin (do-not-merge).

## What shipped

- **S-FELT** `53c406d` — composed fit (the S1-void fix-forward), presence pass,
  killed the hover ghost.
- **S2** `a7f8e73` — tap-to-light → the zoom (law 7) → the clearing (workshop
  reframed: breadcrumb + faint horizon + return, court preserved).
- **S3** `3876924` — the unrooted seat (adopt orphans into an arc), the soil-reach
  (native `<select>` retired), the weave (drag-across-edge at the caret + a11y twin).
- **S4** `7ec7ff3` — the mark composer (form-first 9×3×10, in-arc uniqueness,
  offered-not-applied; replaces every Change-mark path).
- **Gate fixes** `5ca540e` (red-team) + `b4b1097` (reviewer residuals).

## The carried-in felt observations — all resolved

The 2026-07-22 FINISH-CHOREO S2 notes, now closed by this round: scattered controls
→ the Gate Row (F5); empty values panel → dissolved to sky embers / nothing (F4);
cream-panel-on-dark-room seam → one ground, no panel (F1, seam gone by
construction); mark-picker hues repetitive → PALETTE-X ten named pigments +
form-first composer (F6).

## Debt carried out of the round (the debt list)

1. **Ask-Yumi arc-context gap** — the Bloom's chat never learns which arc you are in
   (`renderRoute` sets `state.currentArcId=null` on `#arc/<id>`; `assembleContextData`
   carries `{title}` at best). Ask-Yumi is parked in the ⋯ overflow rather than
   retired. **Yumi-adjacent → belongs to a Yumi round, not this one.** (Stage-0 ruling 2.)
2. **Derived-mark collision residual** — uniqueness is guaranteed at the composer;
   two never-composed (derived) marks in one arc can collide (birthday-paradox at
   ~19+ marks). Offered/never-applied — forcing a derived mark's identity would break
   the pure-function-of-id / covenant law. Surfaced + resolvable in the composer.
   (Red-team finding, brief §11.)
3. **Question authoring → R-CAPTURE** — the horizon carries `arc.title`; true
   arc-level question/answer AUTHORING is R-CAPTURE's chartered seam (already noted in
   sequence.md `## Now` under R-CAPTURE — the D4 carrying-question seam is the same
   family). (Stage-0 ruling 2.)
4. **B-M mobile items — unchanged.** The mobile-wave items outside this round's scope
   are untouched; the field's 390 behavior is felt-passed, but no B-M debt was closed
   or opened here.

## Round history

- **2026-07-22** — vision session (F1–F8, G1–G6 + sweep) → brief committed `9f254a0`.
  Mockup round on `mockup/arc-standard` → felt PASS, approach ruled ZOOM (`1ee4bb5`).
- **2026-07-22/23** — Stage-0 recon (6 rulings: rig home · horizon string · unseated
  controls · F8 lifecycle · orphan seat · derive-only migration) → S1 field build →
  **felt FAIL on composition, foundation ratified** → S-FELT fix-forward → S2/S3/S4
  continuous run → both gates → real-account composition check → **full felt PASS.**

## Next

Round closed. The build lane frees for **R-CAPTURE** (sequence.md `## Now`), which
inherits the question-authoring seam (debt #3).
