# THE ARC STANDARD — round ledger (seed)

Status: NOT STARTED. The round's mockup spec comes from Preston's chat; the `arc-standard-mockup`
branch (off `36b7d45`, do-not-merge) holds empty for it — do NOT self-design. In the build serial
order the ARC STANDARD build **queues behind the R-SHELF build** (sequence.md `## Now`).

This ledger seeds the round: carried-in felt observations live here until the round's scan → shape.
This file is not in `tools/studio-build`'s surface list, so it does not affect the Builder's counts.

## Carried-in felt observations (no action now — for this round's scan/shape)

- **2026-07-22 (Preston, felt, at the FINISH-CHOREO S2 live-smoke).** The arc **read / finished
  pages** have composition issues to resolve in this round:
  - **Scattered controls** — the page's controls are spread out rather than composed into a clear
    grammar.
  - **Empty values panel reads as furniture** — the values panel sits as an empty frame (occupancy
    without content); it should earn its space or collapse when empty (sparse-honest).
  - **Cream panel clashes with the dark finished room** — a light/cream panel against the full-amber
    finished room breaks the ground spectrum (finished = full-amber; a cream panel fights it).
  - **Mark-picker hues visually repetitive** — the mark-picker's colors read as repetitive/samey;
    the hue set needs more differentiation.
  These are Preston's felt notes, logged for the round; not scoped, not ruled, no action taken now.
