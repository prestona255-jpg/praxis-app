# R-ARC — ROOM-3 SUCCESSION HANDOFF (Wave C; ROOM-2 pushed+live; ROOM-3 next)

**Written at the ROOM-2 close (2026-07-17), Fable 5 session ending. Per Preston's
word: ROOM-3 runs in a FRESH OPUS 4.8 SESSION — the composition scope is
ruled work under the ratified charter, not fresh design judgment. The successor
does NOT start ROOM-3 without Preston's word. ON-8 stays HELD.**

---

## 0. SUCCESSOR'S FIRST ACT

Verify `HEAD == origin/main == ls-remote` (expect the close-out docs commits atop
`15e9ef7`; live `sw.js` = **praxis-v3.226**, proven ×2 cache-busted at this
close). Run `sh tools/ground-truth`. Read, in order: CLAUDE.md ·
docs/FIX-PROTOCOL.md · **docs/studio/room-felt-brief.md (hand-placed, verified
byte-exact: 17,565 B · 270 lines · md5 303928465cda46af469a75a1face7cb6 — it
carries Preston's Room felt direction and governs ROOM-3's design choices)** ·
docs/checkpoints/r-arc-room-brief.md (the charter + 8 rulings appendix) ·
docs/checkpoints/r-arc-room1.md · r-arc-s7.md · r-arc-room2.md ·
docs/studio/r-arc.md (the ledger is current through ROOM-2).

## 1. WHERE THE TREE STANDS

Wave C ledger, all PUSHED + LIVE: S6a spike (ratified) · S6b v3.221 recognition ·
S6c v3.222 notebook lighting · RAIL v3.223 (FF-12 + UNFILED-REACH + FF-2) ·
ROOM BRIEF ruled (D1–D8) · ROOM-1 v3.224 the field + C1 · S7 v3.225 Room
lighting (post-S7 FELT CHECKPOINT = FUNCTIONAL PASS) · **ROOM-2 v3.226 the note
door (data-write; pushed on Preston's explicit word at this close)**.

## 2. ROOM-3 SCOPE (absorbs old Slice 12 + the ratified riders)

The Room's COMPOSITION under the EMPTY-STATE MANDATE (every state designed —
empty · single-card · loading · failed) + the raised-hand VISIBILITY proven by
measurement (the D14 chrome only — the Yumi half is Slice 9's). **Intake it
must answer (all Preston-ratified):** INT-5 native scrollbars in the paper
world · INT-6 sparse-field undesigned (huddle + void + clip) · INT-7 workshop
left-column composition (ghost oversize · crowded control row · Yumi-line
orphan) · INT-8 field-vs-gathered card grammar unification · **RM6 keyhole
(Preston: the pane-onto-canvas feel is WRONG — composition evidence)** ·
INT-1/"Na…" placeholder clip (confirmed rider) · FF-11 Notebook void
(CONDITIONAL: only inside the declared band, else back to the debt ledger) ·
FF-1 beat orientation (the loop-position strip; FF-7 vocabulary) · RN1 dead
`.note-edit-cancel` selector cleanup · RS4 one-line double-attach guard
candidate. **The room-felt-brief.md governs the design answers.**

## 3. BINDING RULES (unchanged; read before building)

- **RUN MODE v2:** standing push on fully-green (gates clear · bands held ·
  tripwires zero · docs rode) + self-run deployed smoke + push ledger. HARD
  HALTS: band breach · fork/unruled judgment · unresolvable BLOCK · T2 work.
  **FELT CHECKPOINT after ROOM-3 is MANDATORY — no Slice 8 past it.**
  Slice 8 (dismissal store) is a DATA-WRITE slice: local + explicit word.
  Slice 9 (T2, frozen gate) ALWAYS halts.
- **Pricing laws (all ratified):** ~38 B/line default unless proven tighter
  from shipped code · CSS ~125 B/line from shipped evidence · +20% line-count
  contingency into hard bands · line counts derived from branch structure ·
  never add estimates to totals — re-measure.
- **Gates sequenced:** self-verify → fix-red-team → dispositions → reviewer
  on a FROZEN tree → rig live-verify → checkpoint. Two-figure bands. FEEL
  STANDARD measured per the rider.
- **Tripwires:** T11 (evidenceLayout never in Yumi context — grep every Room
  slice) + T1–T10 per docs/r-arc-plan.md §1.
- **Rig:** LOAD `.claude/rig/` (serve.ps1, fresh port per JS change — this
  session used 8931–8939) · seed + d0tester stub (re-set `praxis_user`
  post-boot) · prestona255 NEVER · pane gotchas: screenshots dead ·
  left_click_drag needs a screenshot → dead (synthetic Mouse/TouchEvents at
  the real listeners are sanctioned) · modifier keys don't reach handlers
  (synthetic KeyboardEvent) · clipboard denied · hashchange routes ASYNC
  after a click (re-assert in a fresh call) · different-hash location.hash
  DOES route · NodeLists invalidate on re-render · **an invisible NUL in an
  edit payload flips the file to `w/-text` + whole-file diffstat — the tell;
  perl -pe 's/\x00/|/g' repairs** · sw.js edits: sed + unix2dos.

## 4. NAMED ITEMS (do not lose)

| Item | Home |
|---|---|
| **ON-8** Bloom-hint collision | overnight queue — **HELD** by Preston's word |
| **FINISH-CHOREO** (§4b sweep + threshold q) | vehicle at the POST-ROOM-3 slotting |
| **CORPUS EXPORT** | round-close prep, beside the beta gate; built never in Wave C |
| Arc-Field page findings | POST-WAVE LEDGER — never Wave C |
| YUMI NOTICE→FOLD watch + R6 panel-name-clear ruling | at that live moment |
| Slice 8 (F-D remembered) → Slice 9 (raised-hand seat, OPUS per plan, T2) | after ROOM-3's felt checkpoint |
| FORMING-REACH · 3B-MOTE · 3B-SM · CAPTURE-OWNER beta gate | carried (pre-Wave-C homes) |
| Studio-census re-measure ×5 surfaces | round CLOSE |

## 5. LESSONS THIS SESSION PAID FOR (inherit, don't relearn)

Verify LIVE code, never comments (S6c's stale-comment BLOCK; the D8 "still
unruled" claim) · the claiming-absence law bites twice per wave · gross-vs-net
byte deltas (net the deletions) · `grep -c '^+'` counts the `+++` header (use
numstat) · a discard promise can't coexist with autosave (ROOM-2's Cancel
resolution) · the lens-pointer clear is 17-of-17 now — keep it that way ·
safety code is never trim material (Preston, standing).
