# R-CAPTURE — THE RULED BRIEF
v1 · July 18, 2026 · Shaped in-chat on Fable 5 (CD-1–3 tappable at rec; CD-4–6 + sweep rulings Preston-deferred to Claude). Builds = Opus 4.8, firing at R-CAPTURE's sequence slot (after R-POLISH + FINISH-CHOREO + R-SHELF), so the sheet is born canon-native — kit controls, motion tokens, type ramp all inherited.

## 1 · GROUND (shipped + locked — not up for redesign)
Upload+paste LIVE (v3.137) · dictation v2 LIVE (v3.140, 400/hang fixed) · book-association chain (leading clip + generous candidates/conservative matcher + review picker) staged → v3.141 · commit-everything model (confident→book, low→Inbox filed:false, undo=deleteEntry) · own-notes default MARGINALIA, journal = opt-in flip · N0 schema LOCKED · the approved Apple-lens shape: ONE capture entry, multiple inputs, review-by-exception, quiet receipt, progressive disclosure, instant undo, Yumi adds nothing · R-ARC laws: capture is one gesture from anywhere, never the landing; local-first; raw captures join the corpus with Home presence.

## 2 · THE RULINGS
**CD-1 · THE DOOR — two ruled corners (formally amends R-POLISH AMB-1).** Bottom-left = the "+" create door, app-wide, every page. Bottom-right = the Yumi flower. Nothing else floats, ever. The nav keeps a matching entry; ⌘N summons on desktop.
**CD-2 · THE LANDING — the capture sheet.** "+" summons a light sheet over the current page: bottom sheet mobile, centered card desktop. Text field focused within one frame. Mode row one glance below. Pre-rendered at boot; zero network before the first keystroke. The sheet has TWO ruled sizes: QUICK (text) and EXPANDED (voice / bulk paste-import / photo review — the existing "capture window" evolves into this state; no second surface).
**CD-3 · CONTEXT-SMART, NEVER SILENT.** From Book Detail the sheet pre-associates that book; from an arc's Room it honors D4's in-Room ruling verbatim; from neutral pages, Inbox default with the matcher assisting. The association is always a visible chip, one tap to change.
**CD-4 · TALK-IT-THROUGH SEAT.** The sheet carries the "Talk it through" entry now; the conversation itself ships with the Yumi Generative round. Seam stated — no double-own.
**CD-5 · COMMIT-AND-STAY.** Committing clears the field, fires the SAVE PULSE with a one-line receipt ("filed to <book> · Undo"), and the sheet stays open for the next thought. Explicit gesture closes. Rapid capture is the native rhythm.
**CD-6 · ONE CREATE DOOR, ONE COMPONENT.** The sheet's full mode set: note (default) · voice · paste/import · photo · Scan/add-book — unifying SC2's Add-FAB reading; everything creatable lives behind one "+". One shared component everywhere: Book Detail's "Add marginalia" opens it pre-associated; the Notebook's "Catch a note" is its resident twin. No parallel capture implementations survive the round.

## 3 · STATED LAWS
**THE <400ms LAW (engineered, then measured):** gesture → focused input in <400ms — sheet pre-rendered at boot, focus within one frame, zero network pre-keystroke, local-first write on commit. Verified in the PERF GATE, not assumed. **⌘Enter commits; Enter is a newline** — marginalia are paragraphs. Commit debounces (UX-3). Offline: local commit succeeds visibly + the pending-write chip shows (UX-3). Sheet a11y rides the kit: focus trap, Esc closes, announced on open. The sheet is the ME-1 activation instrument (first capture = the activation counter's second half). Voice inside the sheet = the shipped dictation flow with the leading-clip warm-up state.

## 4 · BUILD LANES (all Opus)
**Lane 1 — the door + the sheet** (shared component, both sizes, corners amended, nav entry, ⌘N, context chip, commit-and-stay).
**Lane 2 — mode integrations** (voice embed · paste/import · Scan/add-book chip → SCAN surface · photo/OCR fast-follow via the vision endpoint).
**Lane 3 — OS SHARE TARGET (stretch):** Android manifest share_target routes shared text/images into the sheet; iOS PWA share limits noted honestly, not promised.
**Lane 4 — hardening close-out:** segmentDoc auto-retry on 5xx · tag_audio_events stripping · live-caption cosmetic (guarded, OFF on iOS, Scribe always the filed transcript) · worktree cleanup (../praxis-dictation-400, ../praxis-dictation-v2).
Intake at build open: pull CAPTURE-OWNER 1b's requirements from sequence.md's beta gate.

## 5 · NON-GOALS
No URL unfurling on paste (text only). No new-book creation inside the association picker (existing rule stands). The Yumi conversation itself (YG round). First-run orchestration (onboarding round — it opens THIS instrument). Schema/migrate/normalizer untouched. SCAN's camera surface internals (SC1–SC12 govern).

## 6 · FELT CANON
- **Speed:** From anywhere in Praxis, a thought is caught before it can fade — one gesture, under half a second, on any device.
- **Honesty:** The sheet reads the room but never guesses silently; where a note will land is always visible and always one tap to change.
- **Safety:** Nothing entered is ever lost — offline, ambiguous, or abandoned, every word lands somewhere you can see.
