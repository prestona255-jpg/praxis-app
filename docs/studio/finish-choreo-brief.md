# FINISH-CHOREO — the ceremony of finishing
Charter: r-arc-brief.md:76-77. The shipped gate (basin-guard, doubly enforced) is NOT rebuilt. Recon: docs/checkpoints/finish-choreo-recon.md.

## Ruled decisions
D1 SCOPE — A1 (sub-theory Finish) carries the full ceremony. A3 (publish to commons) gets one unified publish ceremony across both entries. A2 (graduate/return) gets dignity only: copy + settle motion, no new questions, no modal. A4 (books) is OUT — recon confirmed no privacy dimension.

D2 THE FINISHING MOMENT (A1) — Click Finish → (i) privacy sweep, deterministic, ONLY if private marks/chips exist on this sub-theory: "What stays yours" / "What travels," a plain list, no toggles invented here. → (ii) THE THRESHOLD: one quiet full-screen overlay — never stacked modals. The arc's central question IS the headline, verbatim. Sub-copy: "Before this enters the finished room — how does it answer?" One text input = the ANSWERING LINE (new field: subTheory.answeringLine, via ensureSubTheoryFields). The question is always shown; the answer is optional — we ask, never demand. Primary "Finish" · secondary "Not yet." On Finish: status flips published + answeringLine saved + one quiet settle back to the room. THE ANSWERING LINE IS PUBLIC-FACING BY DESIGN: it is authored at the crossing and travels with the finished sub-theory into the commons projection — state this in its input's sub-copy plainly ("this line travels with it"). REOPENING is instant, no ceremony — ceremony marks the crossing IN only. The always-on st-room-threshold label is REPLACED by the answering line when one exists; falls back to current copy when blank.

D3 PUBLISH UNIFICATION (A3) — ONE shared publish ceremony used by BOTH entries (arc-head + own-profile): identity step (single canonical pen/display resolution — the publish path stops hand-rolling it; the other two identity copies elsewhere become named debt if consolidation isn't cheap) → freshness → sweep summary: "What travels: N finished sub-theories · What stays: M drafts" → confirm. buildPublishedArcDoc GAINS THE STATUS FILTER (status==='published' only) — beta-gate #5 flips as a verified side effect once red-teamed. PUBLISH REQUIRES ≥1 finished sub-theory: at zero, the ceremony explains plainly ("Nothing is finished yet — finish a sub-theory first") and does not write. MIGRATION EDGE, DO NOT DECIDE SILENTLY: arcs ALREADY published whose sub-theories are all drafts will project empty (or stale) once the filter ships. S1 Stage-0 must report the existing publishedArcs population and the filter's exact effect on each, and bring the disposition to me as a FORK-VERBATIM item before building the filter. UNPUBLISH gains a confirm on both entries (pattern: openArcDeleteConfirm, lighter copy) — removing a live arc may never be quieter than deleting a draft.

D4 MOTION — One vocabulary: the KIT tokens (--dur-*, --ease-emphasis); theme.css's --ease/--motion-base are legacy for ceremony purposes. The ceremony uses exactly two moves everywhere (P9): opacity fade + sheet/overlay slide. NO scale() in the ceremony on any viewport. Reduced-motion = instant, all states. FEEL STANDARD applies: latency/fps/settle measured at final pass.

D5 COPY — Apple-plain, Praxis-hearted, sentence case. No exclamation points. The central question is always verbatim, never paraphrased.

## Build program — three slices, staged, felt deltas declared (owner viewports: 1360 desktop / 390 mobile)
S1 PUBLISH SPINE — shared ceremony component + status filter + ≥1-finished gate + migration disposition (post-fork) + unpublish confirms. Files: integrations.js, views.js, components.css. FELT DELTA: publishing from either entry asks who you are and shows what travels; publishing an empty arc explains instead of silently projecting nothing; unpublishing asks first. Gate #5 flips on red-teamed proof of the filter.
S2 THE THRESHOLD — privacy sweep + full-screen question + answeringLine (schema via ensureSubTheoryFields) + threshold-label replacement. FELT DELTA: finishing a sub-theory opens one quiet full-screen question, and your answer thereafter stands at its threshold.
S3 DIGNITY + MEASURE — A2 graduate settle/copy, ceremony motion unified on kit tokens, FEEL measurements tabled. FELT DELTA: the graduate flip settles instead of snapping; every ceremony motion is fade+slide, measured.

Each slice: Stage-0 anchor verification → build → red-team + reviewer (aimed at claims-outliving-code) → self-commit (pre-granted) → HOLD at the push word. S1 and S2 both touch user-data paths: their pushes hold for the explicit word regardless of green, per the data-write exception. Frozen files untouched; ES3; FORK-VERBATIM; felt-delta clause; owner-viewport primacy. Ceremony verification includes driving the real flows headless on prestonpraxistest per the test-automation directive.
