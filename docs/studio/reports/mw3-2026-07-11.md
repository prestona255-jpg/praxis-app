# MW-3 — Sub-theory + Book Detail mobile pass (canon P1–P9)

**Wave:** MW-3 (final mobile wave). **Date:** 2026-07-11. **Ground:** main.
**Precedents:** MW-1 (`a405730`/`e5ab754`), MW-2 (`bd5c4c5`/`900aa4f`).
**Target cache bump:** v3.193 → v3.194.

Status: **COMPLETE (LOCAL, not pushed).** Both surfaces PASS at live 390; 3 chips → `mobile: native`;
the mobile wave (MW-1/2/3) is complete. Commits `99f7fb0` (sub-theory) · `5dd7cee` (book detail) ·
`19ca15b` (cache v3.194) · docs close. One desktop residual carried (MW3-BKBOX).

---

## GATE 0 — RECON

**Preconditions (all PASS):** HEAD `9182b33` (= MW-2 close ✓); hook ARMED; FIX-PROTOCOL v1.2 live;
scope files (views.js / components.css / theme.css / sw.js) clean; `sequence.md` Now = MW-3; canon +
3 ledgers present. Report cats back ✓.

**True render anchors (all shifted since R6/R7 — MW-2 touched views.js):**
- `renderBookDetail` → **views.js:8929** (ledger said 8781)
- `renderSubTheoryReadOnly` → **views.js:9849**
- `renderSubTheoryPage` → **views.js:10045** (ledger said ~9838)
- `renderSubTheoryBuild` → **views.js:10449** (ledger said ~11128 at R6 ship)
- `renderBookView` → **views.js:8568** (thin redirect → `#book/<id>`, marks route retired R7)

**Grounds (verified from live CSS, per the GROUND-CHECK-FIRST lesson):**
- Sub-theory **build** = DARK ground, warm-dim contained panel (`.st-build.lum-amber-deep` +
  `.stb-warm-dim`); Sub-theory **page** = DARK ground (warm-dim draft / full-amber finished room).
  New chrome uses `--lum-*` (already the surface namespace) — but MW-3 adds SIZING only (min-height /
  font-size), no color tokens, so the "no --lum-* in NEW css" rail is trivially met.
- Book **detail** = LIGHT ground (scoped `.bk-surface.lum-amber-deep` re-point). MW-3 adds sizing only.

**MW-2 precedent (both CSS-only, inside `@media (max-width:759px)`):** P7 = `font-size:16px`;
P3 = `min-height:44px; display:inline-flex; align-items:center`. MW-2's classes
(`.notebook-editor-*`, `.arcfield*`) do NOT overlap sub-theory/book-detail — no shared-class coverage.

**Existing mobile coverage (what NOT to re-add):**
- Workshop `@media` 11251–11267: P8 cols stack, P7 title 26px + `.wc-input` 18px, P3 pubpill/openpage/
  weave/conn-add/crow; focus-toggle 44px at 11109–11111.
- Page `@media` 10958–10968 + 11072–11081: P8 grid→1-col, P3 pill-publish/conn-add/conn-row/edit-door/
  walknav-side.
- Book `@media` 10648–10655 (base) + 10869–10877 (scoped BD2 4-I): P8 cols→column, P2 actions column +
  primary `order:-1` padding 13/17 (~44px), aside width 100%.

**GAP CANDIDATES (static census — to confirm at live 390):**
- **Workshop P7:** `.stb-pull-book-sel` + `.stb-pull-search` = 11.5px (views.js:449/465) — under 16px.
  **Workshop P3:** `.stb-delete` (11px/6-10pad ~23px), `.stb-yx` (14px ×), pull inputs (~30px).
- **Page P3:** `.st-tb-back` (12px, no min-h — the finished-room EXIT affordance).
- **Book P7:** `.bk-edit-input`/`.bk-edit-select` = 13px (components.css:10630). **Book P3:** secondary
  `.bk-actionbtn` (9/16 ~37px), `.bk-arcchip` (5/12 ~28px), `.bk-moved` (8/13 ~35px), `.bk-edit-toggle`
  (7/14 ~30px), `.bk-showall` (8/14 ~32px), `.bk-becamelink` (5/12 ~28px), `.bk-gopen` (7/14 ~30px),
  `.bk-backlink`/`.bk-find` (inline links), edit-panel buttons.
- **P7 inputmode:** no numeric/email fields on either surface (search is prose-ish, category is a
  `<select>`) → n/a. **P7 autofocus:** none on page load (the `.focus()` calls 9520–9752 are
  user-triggered picker opens). **P4 safe-area / P5 large-title / P1 sheet / P9 motion:** n/a (no new
  fixed chrome, no page-title collapse, no new overlays/animations in scope).

**Verdict:** CSS-only pass expected (P7 font-size + P3 min-height at ≤759). No JS → fix-red-team
skippable per task; praxis-reviewer gates each commit. Two-layer verify: static (above) + live 390.

---

## STAGE A — SUB-THEORY PASS — **PASS** (commit `99f7fb0`)

Two faces. Anchors: `renderSubTheoryBuild` views.js:10449 (workshop, dark warm-dim),
`renderSubTheoryPage` views.js:10045 / `renderSubTheoryReadOnly` views.js:9849 (Page:
warm-dim draft + full-amber finished room). ONE `@media (max-width:759px)` block added to
components.css after the SUB-THEORY BUILD section (36 insertions / 0 deletions, CSS-only).

Live rig: local :8760, SW cleared + stylesheet `<link>` cache-busted (browser HTTP-cached the
stale CSS through the SW clear — had to hot-swap the `<link>?v=` to load the real file), auth
stubbed (`getCurrentUser`→mw3tester, in-memory), seeded arc `arc_mw3` + a draft
(`subtheory_mw3_draft`) + a published (`subtheory_mw3_fin`) sub-theory. Console 0 errors.

**Per-pattern (two-layer: static relational + live 390 rect):**
- **P8 — APPLIED (fixed a real overflow).** The **finished room** had a **~130px h-scroll**
  (scrollWidth 492): the author topbar-right stacked `saved`(148) + Finished pill(110) +
  `Edit in the workshop →`(208, `white-space:nowrap`) = ~496px in a `flex-wrap:nowrap` group
  whose base `flex:0 0 auto` (components.css:10902) sizes it to max-content regardless of the
  wrapped topbar line. Fix: `.st-page.lum-amber-deep .st-tb-right{ flex-wrap:wrap; width:100% }`
  → constrained to 342, children wrap, **scrollWidth 390, 0 offenders**. The DRAFT topbar was
  already clean (R#3/R#2 stripped saved+pill from it). Workshop + draft: scrollWidth 390, 0
  offenders. (Relational: `.stb-build{flex-direction:column}` 11254, `.st-grid{1fr}` 10963.)
- **P7 — APPLIED.** `.stb-pull-book-sel` + `.stb-pull-search` were **11.5px** → **16px**
  (kills Safari focus-zoom). Already ≥16 (no change): title 26px (11260), canvas `.wc-input`
  18px (11261). inputmode n/a (no numeric/email fields; search is prose, no `<select>` needs
  it). No page-load autofocus (the `.focus()` at views.js:9520–9752 are user-triggered picker
  opens). Live: pull inputs fs 16 ✓.
- **P3 — APPLIED.** Sub-44 → 44px effective hit area: `.stb-delete` (26→44),
  `.stb-hero-mark-edit` (32→44×44), `.stb-yx` Yumi dismiss (8×19→44×44), `.st-tb-back`
  (16→44, the read's EXIT), `.st-hero-mark-edit` (30→44×44), pull inputs (30→44/57). Covered
  already (verified ≥44, no change): pubpill/openpage/weave/conn-add/crow/focus-toggle (Build,
  11262–11266+11110), pill-publish/conn-add/conn-row/edit-door/walknav-side (Page, 10965/11076/
  11079). `.stb-brow` 78 (cover row).
- **P1/P2/P4/P5/P6/P9 — n/a.** No new bottom-sheet/overflow (P1); the primary verb is the
  composer canvas itself, already low (P2); no new fixed chrome for safe-area (P4); interior
  faces, no page-title collapse (P5); the faces are route-swapped, not a same-region segmented
  toggle (P6); no new transitions introduced (P9).

**Gate:** praxis-reviewer **PASS** (6/6 gates: scope 36/0 CSS-only, desktop-unchanged, rails
clean, no selector collisions, canon fidelity, no unproven claims). Its one non-blocking note —
pull inputs P7-only, ~33px — was **closed** (added to the P3 min-height rule → 57px). No JS →
fix-red-team skipped per task. FAIL RULE not triggered.

---

## STAGE B — BOOK DETAIL PASS — **PASS** (commit pending reviewer re-confirm)

LIGHT ground. Anchor: `renderBookDetail` views.js:8929 (`bookAmberSurface` views.js:8144 →
`.bk-surface.lum-amber-deep`, scoped light re-point). ONE `@media (max-width:759px)` block added
to components.css after the R7·S5 section (40 insertions / 0 deletions, CSS-only). Light
primitives only — MW-3 adds sizing/reflow, no color tokens.

**Per-pattern (two-layer: static + live 390 rect, real served CSS):**
- **P8 — APPLIED (fixed a real 20px overflow, scrollWidth 410→390).** TWO compounding causes:
  (a) `.bk-surface` (base 10505) is the ONE surface that omits `box-sizing` (siblings
  `.st-page`/`.st-build`/`.notebook`/`.shelf` all set `border-box`; there is NO global reset —
  grep-confirmed), so `width:100%` + 28px mobile side-padding = a **content-box overflow**.
  → `box-sizing:border-box` at ≤759. (b) the read-status control's 3 `white-space:nowrap`
  segments ("Currently reading"/"Have read"/"Will read" = ~372px) overflow the row →
  `.rstatus{display:flex;width:100%}` + `.rs-opt{flex:1 1 0;min-width:0;text-align:center;
  white-space:normal}` (the app's own `.rrow-right` idiom, 9211-9212). Live: scrollWidth 390,
  0 offenders; rstatus = 3 equal 100px segments, "Currently reading" wraps to 2 lines (47px).
  **DESKTOP RESIDUAL (out of scope, flagged):** the same content-box overflow exists at desktop
  (scrollWidth 1297 vs 1265). The base rule is UNTOUCHED (desktop byte-identical), so the desktop
  fix (adding border-box to the base rule = a desktop layout change) is deferred to its own
  felt-gated pass. Named **MW3-BKBOX** below.
- **P7 — APPLIED.** `.bk-edit-input` + `.bk-edit-select` (category picker + ISBN/tradition
  fields, views.js:8782/8819/8850) were **13px** → **16px**. Live: fs 16 ✓. inputmode n/a (no
  numeric/email); no page-load autofocus.
- **P3 — APPLIED.** 44px effective hit area on the sub-44 controls: backlink/find (19→44, +
  inline-flex since base is inline-block), moved (35→44), arcchip (28→55, wraps), edit-toggle
  (29→44), edit-btn/remove (34→44), secondary action (~37→44), edit input/select (→ font-16 +
  44), remove-cancel/confirm + show-all/became/grew-open (structural floor, grep-confirmed no
  colliding min-height). The **5 rating stars** `.bk-star` (real `book.rating` writes) get a
  **genuine 44×44 hit box** (glyph stays 15px, centered) — the row fits the full-width aside
  (228px, no overflow). Covered already (BD2/R7, no change): action row → column with the gilded
  primary at `order:-1` (P2, 10870-10872).
- **P1/P4/P5/P6/P9 — n/a.** No new sheet/overflow menu (P1); primary is already thumb-first via
  BD2 `order:-1` (P2 satisfied); no new fixed chrome (P4); interior page, no title collapse (P5);
  no same-region sibling-pane toggle (P6); no new motion (P9).

**Gate:** praxis-reviewer FIRST pass = **HOLD** — one blocking finding: `.bk-star` (real rating
buttons) shipped ~15px and the comment mislabeled the canon's density clause as an "exemption"
(the canon *requires* padding/pseudo hit-area expansion, not a skip). **Fixed once** per the FAIL
RULE: added `.bk-surface .bk-star{ min-width:44px; min-height:44px; display:inline-flex; ... }`
(44×44 hit box, glyph unchanged) + rewrote the comment honestly. Re-verified live (44×44, row
228px, P8 clean) and returned to the reviewer to confirm the HOLD is cleared. No JS →
fix-red-team skipped per task.

**Named residual — MW3-BKBOX (desktop, out of MW-3 scope):** `.bk-surface` base rule (10505)
lacks `box-sizing:border-box`, so book detail also h-scrolls ~40px at desktop (1297 vs 1265).
MW-3 fixes mobile only (≤759); the desktop fix edits the base rule (a desktop change) and wants
its own felt-gated pass. Pre-existing since R7 (not introduced here).

---

## STAGE C — CLOSE THE WAVE

1. **Ledgers — chip rulings written.** All three surface chips ruled **`mobile: native`** (every
   applicable canon pattern verified BOTH layers — static relational + live 390):
   - `subtheory-build.md` → `mobile: native`; anchor refreshed (10242→10449); MW-3 round entry.
   - `subtheory-page.md` → `mobile: native`; anchor refreshed (9838→10045); MW-3 round entry.
   - `book-detail.md` → `mobile: native`; anchor refreshed (8781→8929, view redirect 8420→8568);
     MW-3 round entry + the **MW3-BKBOX** desktop residual filed.
2. **sequence.md** — MW-3 → Shipped (with the MOBILE-WAVE-COMPLETE one-liner: MW-1/2/3, 8 surface
   chips native); **Now = S-A** (About · Search · what-Yumi-sees); the held **ARC-FIELD MOBILE TOUCH
   MODEL** promoted Then→Next (truest remaining mobile line now the pages are native); dated Re-plan
   log entry (autonomous advance, no `PROPOSED:` — nothing retired, no Preston decision touched).
3. **Cache bump** `praxis-v3.193 → v3.194` (`sw.js`, 1/1 equal-length string, no EOL flip), committed
   through the **armed hook** (no `--no-verify`): `19ca15b`.
4. **Builder regenerated** (`tools/studio-build`, harness-tracked). Integrity: see Final Report.
5. **Docs commit** `studio: MW-3 close — …` bundles the 3 ledgers + sequence.md + this report +
   builder.html.

## FINAL REPORT

### Per-surface PASS/FAIL (live 390, real served CSS)

| Surface | Face | P8 h-scroll | Chip |
|---|---|---|---|
| Sub-theory build | workshop (warm-dim) | scrollWidth 390, 0 offenders | **PASS** |
| Sub-theory page | draft (warm-dim) | scrollWidth 390, 0 offenders | **PASS** |
| Sub-theory page | finished room (full-amber) | **492 → 390**, 0 offenders | **PASS** |
| Book detail | light | **410 → 390**, 0 offenders | **PASS** |

All three surface chips → **`mobile: native`** (native only because every applicable pattern verified
both layers).

### Per-pattern

**Sub-theory (commit `99f7fb0`):**
- **P8** — the finished room's author topbar-right (`saved`+`Finished`+`Edit in the workshop →`, nowrap,
  base `flex:0 0 auto`, ~496px) → `.st-tb-right{flex-wrap:wrap;width:100%}` → 342, children wrap. Draft
  + workshop already clean.
- **P7** — pull-filter `select`+`search` 11.5→16px. Already ≥16: title 26, canvas 18. No autofocus.
- **P3** — `.stb-delete` 26→44, `.stb-hero-mark-edit`/`.st-hero-mark-edit` 30/32→44×44, `.stb-yx` 8×19→
  44×44, `.st-tb-back` 16→44 (EXIT), pull inputs →44/57. Covered already: pubpill/openpage/weave/
  conn-add/crow/focus-toggle + pill-publish/conn-add/conn-row/edit-door/walknav-side.
- **P1/P2/P4/P5/P6/P9** — n/a (one line each in Stage A above).

**Book detail (commit `5dd7cee`):**
- **P8** — (a) `box-sizing:border-box` on `.bk-surface` at ≤759 (the only surface omitting it; no global
  reset) fixes the content-box overflow; (b) read-status 3 nowrap segments → 3 equal shrinkable wrapping
  segments (the `.rrow-right` idiom).
- **P7** — edit-panel category select + text inputs 13→16px.
- **P3** — backlink/find 19→44, moved 35→44, arcchip 28→55, edit-toggle 29→44, edit/remove btns 34→44,
  secondary action ~37→44, edit fields →44; the 5 rating stars `.bk-star` → **genuine 44×44 hit box**
  (glyph stays 15px). BD2 primary `order:-1` (P2) pre-existing, untouched.
- **P1/P4/P5/P6/P9** — n/a.

### Chip rulings
- subtheory-build · subtheory-page · book-detail → **native** (all applicable patterns, both layers).

### Commits (LOCAL — NOT pushed)
- `99f7fb0` — mw3: sub-theory mobile pass — canon P3·P7·P8 (components.css, 36/0)
- `5dd7cee` — mw3: book detail mobile pass — canon P3·P7·P8 (components.css, 40/0)
- `19ca15b` — mw3: cache bump v3.193 → v3.194 (sw.js, 1/1, armed hook)
- `<docs>` — studio: MW-3 close (ledgers + sequence.md + report + builder.html)

### Gates
- praxis-reviewer: Stage A **PASS** (6/6, one non-blocking pull-input note closed); Stage B **HOLD →
  fixed (.bk-star 44×44) → re-confirmed PASS**. fix-red-team: N/A (CSS-only, no JS).

### Named residual (carried, out of MW-3 scope)
- **MW3-BKBOX** — `.bk-surface` base rule (content-box) also h-scrolls ~40px at **desktop** (1297 vs
  1265). Pre-existing since R7. MW-3 fixes mobile only (≤759); the desktop fix edits the base rule (a
  desktop layout change) and wants its own felt-gated pass. Filed in `book-detail.md` Residuals.

### Preston's phone checklist (live-verify after you push)
- **Sub-theory — workshop:** write in the build face with the keyboard up — the caret stays visible,
  no zoom-jump when you tap the title / body / the "Search your marginalia" field; the ✎/delete/mark
  controls are comfortably tappable.
- **Sub-theory — published read:** open a *finished* sub-theory — **nothing scrolls sideways** (this
  was the 130px overflow), the prev/next walk-nav + the "‹ back" exit are easy to hit.
- **Book Detail:** the hero + arc chips + read-status control + lineage folds are all comfortably
  tappable, the rating stars are easy to tap, and **nothing scrolls sideways** (this was the 20px
  overflow). *(Note: at DESKTOP width book detail still h-scrolls ~40px — the MW3-BKBOX residual, a
  separate pass.)*

**NO PUSH** — all four commits are LOCAL. Round CLOSE is your felt pass.

---

## FINAL REPORT

_(pending)_
