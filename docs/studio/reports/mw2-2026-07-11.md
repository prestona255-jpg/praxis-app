# MW-2 — Notebook + Arcs mobile pass (canon P1–P9)

**Date:** 2026-07-11
**Task:** MW-2 SWEEP — sweep Notebook + Arcs to the mobile canon (`docs/studio/praxis-mobile-canon.md`).
**Mode:** SELF-RUNNING. APP-CODE work. Rails: edit only views.js, components.css, theme.css + sw.js (one CACHE_VERSION line v3.192→v3.193) + studio docs at close. Second mobile-wave session; reuse MW-1 machinery (`a405730`/`e5ab754`).

---

## Gate 0 — Recon

### 0.1 Preconditions
- HEAD includes the MW-1 docs-close (`0f661f3`): **PASS** — HEAD *is* `0f661f3`.
- sequence.md `## Now` reads MW-2: **PASS** — "MW-2 — Notebook + Arcs mobile pass (SWEEP) … touches: [notebook, arcs, arc-detail]".
- Tree clean in scope files (views.js, components.css, theme.css, sw.js): **PASS**.

### 0.2 Anchors (live; ledgers stale)
- `renderNotebook` = **views.js:1773** (ledger 1755). `renderArcsPage` = **3669** (ledger 3581). `renderArcDetail` = **12157** (ledger 12691).
- **GROUND (MW-1 lesson applied):** none of `.notebook`/`.arcs`/`.arcfield` re-point `--ink`/`--paper` — all TRUE dark-ground (body `data-ground="dark"`). So new chrome uses dark-resolved semantic tokens (`--surface`/`--border`/`--ink`=cream/`--scrim`), not light primitives. (Notebook composer is a light *paper card* within the dark page, but that's existing.)
- Existing mobile reflows already present: arcs grid `.arcs.lum-amber-deep .arcs-grid{1fr}` (1749); notebook `.notebook-leaves{flex-direction:column}` (11312/11387) + `.notebook-spread{1fr}` (9645).
- Notebook inputs (P7): `.nb-ce` writeline (already 16/17px), `.notebook-editor-title-input` (14), `.notebook-editor-body` (14), `.notebook-shot-cap-input` (14.5). All prose (no inputmode). Focus calls (chip-click 2699, openEditor 3315) are user-triggered — NO page-load autofocus.
- **P6 reality:** the canon cites a "Capture|Working toggle" as precedent, but it does NOT exist in live code — the notebook STACKS both leaves at ≤759 (flex-column). Stack ⊃ P6's guarantee (both panes persist, no re-render). Verified + cited; NOT rebuilt.
- Arc-detail: 3 faces (Field/Read/Page) via `.arcfield-faces .seg-opt`; `.arcfield-stage` = constellation host + rail.

### 0.3 Gate-0 verdict: **PASS** — proceed.

---

## Stage A — Notebook pass  → **PASS** (committed `bd5c4c5`)

**File:** `assets/components.css` only (+11/−0). One `@media (max-width:759px)` block bumping the three sub-16px inputs to 16px.

### Per-pattern (walk P1–P9)
- **P7 — APPLIED (the substantive change).** `.notebook-editor-title-input` (14→16), `.notebook-editor-body` (14→16), `.notebook-shot-cap-input` (14.5→16) at ≤759 only. Live: probes compute **16px @390**, **14/14/14.5px @1265** (desktop byte-unchanged). Writeline `.nb-ce` already 16-17px. All prose fields → inputmode n/a; no page-load autofocus; the composer is in-flow so the keyboard is handled natively (not a fixed bottom composer).
- **P8 — PASS already (verified, no change).** Live @390: `scrollWidth 390 = clientWidth`, hScroll false, 0 offenders across **inbox, book-band, editor-open**. Relational: `.notebook-leaves{flex-direction:column}` (11387), `.notebook-spread{1fr}` (9645).
- **P3 — PASS already (verified).** Live @390: tabs **49px**, register chips **44px**, capture button **44px**.
- **P6 — SATISFIED (existing, cited, not rebuilt).** The two leaves STACK at ≤759 (flex-column); both panes persist, no toggle/re-render → superset of P6's "panes hide, not destroyed." The canon's "Capture|Working toggle" precedent is aspirational/unbuilt.
- **P1 — n/a** (no overflow-management cluster; composer affordances wrap, h-scroll 0).
- **P2 — n/a (reasoned)** (the primary action "Capture" is composer-adjacent, right under the writeline where the thumb lands after typing; Enter also commits — a floating bottom FAB would separate it from its text).
- **P4 — n/a** (no bottom-anchored chrome introduced).
- **P5 — n/a (reasoned)** (the notebook's orientation anchor is the tab row, not the one-word "Notebook" title, which sits above an intended-to-scroll intro; a sticky tab row is a possible future follow-up).
- **P9 — n/a** (no new motion).

### Verify
- **Static:** brace balance 3454/3454; new lines 0 `--lum-*`, 0 new hex; +11/−0 additive; only `components.css` (no JS). Every rule `@media (max-width:759px)` → desktop byte-unchanged.
- **Live 390 + 1265** (auth stubbed for layout, non-destructive): values above are live probes/getBoundingClientRect; console **0 errors**.
- **Gate:** praxis-reviewer **CLEARED TO COMMIT** (all 9 gates PASS/N-A; +537 B reconciled; confirmed no autofocus, prose-only inputmode-n/a, source-order safe — noted `.subtheory-register-body` harmlessly overrides to 18px). fix-red-team N/A (CSS-only). **Stage A committed `bd5c4c5`.**

---

## Stage B — Arcs pass

**File:** `assets/components.css` only (+14/−0). One `@media (max-width:759px)` block giving 44px hit areas to the arc-detail controls (after the `[data-st-sub-id]:focus-visible` rule, ~12104).

### Per-pattern (walk P1–P9)
- **P8 — PASS already (verified, no change).** Arcs LIST @390: `scrollWidth 390`, hScroll false, grid 1-col (`.arcs.lum-amber-deep .arcs-grid{1fr}` @1760), cards 293–300px. Arc-detail INTERIOR @390: hScroll false + 0 offenders across **all 3 faces (Field/Read/Page)** with a real **26-node constellation** rendered (stage w:374, svg w:342, stage overflow 0 — a genuine pass, not empty-stage).
- **P3 — APPLIED (the substantive change).** Arc-detail controls were sub-44px: `.arcfield-pub-btn` 27, `.arcfield-addsub-canon` 29, `.arc-voice-ask` 29, `.arcfield .btn` primaries 39/40, `.read-list .read-title a` (sub-theory links) 23, `.read-list .read-change-mark` 13. All → **44-45px** at ≤759 (live-measured; 0 sub-44 interactive controls remain across all faces). List cards are already huge (whole card = tap target). Desktop @1265: pub-btn 27, addsub 29, read-title min-height 0 → **byte-unchanged**.
- **P6 — SATISFIED (existing).** The Field/Read/Page faces seg (`.arcfield-faces .seg-opt`, 44px) switches faces at all widths — exercised all 3 live (each renders, no loss). The canon's segmented-control pattern is realized.
- **P5 — n/a (reasoned).** The arcs list is short; the arc-detail's orientation anchor is the always-visible faces seg + the arc question, not a sticky page title (a sticky faces-seg is a possible future follow-up). Consistent with the Home/Notebook P5 calls.
- **P7 — n/a for arc-specific / covered by Stage A.** No arc-detail text inputs surfaced; the arcs-create flow reuses the `.notebook-editor-*` inputs already bumped to 16px in Stage A.
- **P1 — n/a** (no overflow-management cluster; the arc-detail control chips fit — h-scroll 0). **P2 — n/a** (list primary = the big "start an arc" card; arc-detail primary = the now-44px in-flow add-sub `.btn`). **P4 — n/a** (no bottom-anchored chrome). **P9 — n/a** (no new motion).

### Verify
- **Static:** brace balance 3458/3458; new lines 0 `--lum-*`, 0 new hex, 0 setProperty/transform; +14/−0 additive; only `components.css` (no JS — constellation renderer untouched). Every rule `@media (max-width:759px)` → desktop byte-unchanged.
- **Live 390 + 1265** (auth matched to the demo-seed arc `arc_1783781344391_409468`, owner `__praxis_seed__`, 5 books — non-destructive): values above are live getBoundingClientRect readouts; console **0 errors**.
- **Gate:** praxis-reviewer **CLEARED TO COMMIT** (no blockers). One non-blocking cosmetic residual it named — the Page CTA `.arcfield-page-open` kept a pre-existing `display:inline-block` (0,3,0) over my `.arcfield .btn{display:inline-flex}` (0,2,0), so it reached 44px (inline-block honors min-height) but wasn't flex-recentered — **RESOLVED** with a specificity-matched `.arcfield.arcfield-warm .arcfield-page-open{display:inline-flex…}` at ≤759 (live re-verify: page-open now `inline-flex`, 64px, hScroll false, 0 sub-44 controls). fix-red-team N/A (CSS-only, constellation renderer untouched). **Stage B committed `900aa4f`.**

### Stage B verdict: **PASS** — arcs list + arc-detail (3 faces) both layers verified. → committed.

---

## Stage C — Close

Both surfaces passed.

1. **Ledgers:** `notebook.md`, `arcs.md`, `arc-detail.md` frontmatter → `mobile: native`; MW-2 round-history entries with per-pattern evidence + anchors + live-390 numbers. **CHIP RULINGS — all three `native`** (every applicable canon pattern verified on both layers): Notebook (P7 applied; P3/P6/P8 verified; rest n/a) · Arcs list (P8/P3 verified; rest n/a) · Arc-detail (P3 applied; P6/P8 verified; rest n/a).
2. **sequence.md:** MW-2 → `## Shipped` (SHIPPED-LOCAL, not pushed); **`## Now` advanced to MW-3** (Sub-theory + Book Detail — the last mobile-wave pass), promoted from Next; dated Re-plan-log entry (also corrected the MW-1 entry to "PUSHED + live v3.192"). Both surfaces passed → Now = MW-3.
3. **BOARD.md — deliberately NOT touched.** Its "Mobile @759" column already marks notebook/arcs ✅ (reflow coverage, predating MW-2); MW-1 set the precedent that a mobile-canon pass doesn't change that cell — the canon-native status lives in the Builder chip. No cell changed.
4. **Builder regen** (`sh tools/studio-build`, harness-tracked): **INTEGRITY PASS** — islands **24**, sections **29**, CRLF **0** in the committed blob (the normally-staged blob has 0 `\r`; `git show HEAD:` shows 2645 only because autocrlf *smudges* LF→CRLF on output — a red herring), notebook + arcs + arc-detail chips all render **`mob-native` "native"** (8 native total), NOW card = **MW-3**. `builder.html` delta +96/−95 explained: bstamp HEAD/date, the 3 chip flips, the 3 ledgers' embedded round-history markdown, the NOW/overview shift MW-2→MW-3.
5. **Cache bump** — `sw.js` `praxis-v3.192` → `praxis-v3.193`, its own commit `8dbbd83` under the armed pre-commit hook (non-interactive; passed). Then this docs close-out commit.

## Commits (all LOCAL — NOT pushed; origin/main stays `0f661f3`)
- `bd5c4c5` — mw2: notebook mobile pass — canon P7 (P3·P6·P8 verified) [components.css +11/−0]
- `900aa4f` — mw2: arcs mobile pass — canon P3 (P6·P8 verified) [components.css +18/−0]
- `8dbbd83` — mw2: cache bump v3.192 → v3.193 [sw.js +1/−1]
- `<this commit>` — studio: MW-2 close [docs; self-referential hash left as descriptor]

## Aggregate diff --stat (code+bump, 0f661f3..cache-bump)
```
 assets/components.css | 29 (+29/-0)   (notebook P7 +11, arcs P3 +18)
 sw.js                 |  2 (+1/-1)
```

---

## Final report

### Per-surface verdict
- **Notebook — PASS** (praxis-reviewer CLEARED; live-DOM proof at 390 + 1265).
- **Arcs (list + arc-detail) — PASS** (praxis-reviewer CLEARED, one cosmetic residual resolved; live-DOM proof).
- Both passed → MW-2 SHIPPED-LOCAL; Now = MW-3.

### Per-pattern one-liners
**Notebook:** P1 n/a · P2 n/a (Capture is composer-adjacent) · P3 verified (tabs 49, chips 44, capture 44) · P4 n/a · P5 n/a (tab row is the anchor, not the title) · P6 SATISFIED (leaves STACK — no toggle in live code; stack ⊃ P6) · **P7 APPLIED** (3 inputs 14/14/14.5→16px @≤759) · P8 verified (390, 0 offenders across inbox/book-band/editor-open) · P9 n/a.
**Arcs:** P1 n/a · P2 n/a · **P3 APPLIED** (arc-detail: pub/addsub/voice/primaries/read-links/change-mark → 44px; list cards already huge) · P4 n/a · P5 n/a (faces seg is the anchor) · P6 SATISFIED (Field/Read/Page faces seg) · P7 n/a (arc-create inputs fixed in Notebook stage) · P8 verified (list + all 3 faces, real 26-node constellation, 0 offenders) · P9 n/a.

### Chip rulings + why
All three → `mobile: native` — every applicable pattern verified on both layers (static relational + live 390 CDP), anchors in the ledgers. Both surfaces were already largely mobile-clean; MW-2 was a light pass (Notebook = P7 input floors only; Arcs = arc-detail P3 hit areas only) — no structural work, unlike MW-1's Manage sheet.

### Residuals (honest)
- Arc-detail `.read-change-mark` and read-title links are now 44px inline-flex in the dense read list — verified no h-scroll/overflow, but the taller read rows are a visual change worth Preston's eye at the felt pass.
- P5 was ruled n/a on both surfaces (Notebook tab row / Arcs faces seg are the real orientation anchors) — a **sticky tab-row / sticky faces-seg** is a possible future follow-up if the felt pass wants persistent orientation on scroll.
- `.arcfield .btn` broad selector: the one collision (Page CTA) was resolved; the rule also min-heights any other arcfield `.btn` (verified no layout break, h-scroll 0).

### Gate-0 chip-count answer
builder.html: **24 islands** (INDEX + 23 surfaces/layers), **29 page-sections** (23 surface + 6 page); native chips now **8** (MW-1's 5 + MW-2's 3).

### FELT-PASS CHECKLIST for Preston (on your phone, after you push)
- **Notebook** (`#notebook`, signed in): tap into the composer/writeline and an edit-a-note editor — **type with the keyboard up: caret stays visible, no zoom-jump** (16px inputs). Switch tabs (Inbox/Journal/a book) with a draft in the composer — **nothing lost**. The two leaves stack cleanly; **no sideways scroll**.
- **Arcs** (`#arcs` → open an arc): swipe around the **field** — **no sideways scroll**; toggle **Field / Read / Page** — every chip/button/link is **tappable without zooming** (44px); the constellation renders.
- Check at **390** and **~1280**. Desktop is byte-unchanged (all rules are ≤759).

**NO PUSH** — round CLOSE (your felt pass + `git push`, then confirm `praxis-v3.193` live) is yours. Deploy = push `main`; hard-refresh past the sticky SW.
