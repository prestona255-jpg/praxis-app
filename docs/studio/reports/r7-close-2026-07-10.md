# R7 Book Detail — round close-out report (2026-07-10)

Docs/studio-only close of the R7 Book Detail round. No app code. Self-running; gates PASS/FAIL.
R7 shipped + verified live through `bff5d82` (sw.js praxis-v3.191, schema 1.28.0, Preston's deployed felt pass = PASS).

## Gate log

- **GATE 0 — RECON: PASS.** HEAD `a246e4a` "studio: mobile canon v1 — doc + Builder section + surface chips"; `a246e4a` + `1ac1536` both ancestors of HEAD (R7 `bff5d82` shipped, then the other session's two studio commits landed). Only tracked M-dirty = the expected `stage1a-2026-07-10.md`; `tools/studio-build` now clean (committed by the other session). Untracked `??` files (home.html, notebook-recon, launch-tracker, 1a-m) pre-existing, excluded by path-explicit staging.
- **GATE 1 — LEDGER (book-detail.md): PASS.** `state: closed` · `rounds: 1` · `ground: light` (F1, supersedes the stale "dark"); render_fn refreshed `views.js:8781` (greps true; stale ledger said 8124). Decisions F1–F6 + LENS drop ACCEPTED (inert `buildBookLensPanel`, not a debt row). 4 gaps stamped closed with fresh `[fix:]` anchors — BD1 (views.js:8904 ranked action row), BD2 (components.css:10863 `order` reorder), BD3 (components.css:10580 two-column base), Hygiene (views.js:8454 + components.css:10653 dead-code sweep). Residuals named: R7-BYTES, R7-F6AUTO, R7-INK4, R7-LENS. **MOBILE CHIP RULING:** R7's mobile evidence is the BD2 4-I `order` reorder (BD2-level, 390 CDP verified) — NOT the full two-layer mobile-canon gate; per the ruling the `mobile:` frontmatter field is left **ABSENT** and the ledger reads "mobile pass = MW-3." Chip not upgraded on vibes.
- **GATE 2 — SEQUENCE (sequence.md): PASS.** R7 → Shipped (2026-07-10). Re-plan log entry added. Now = **MW-1** (Shelf + Home mobile pass). Next = **MW-2** (Notebook + Arcs) · **MW-3** (Sub-theory + Book Detail) · then **S-A** (verbatim). Everything downstream shifted intact — Then now leads with ARC-FIELD → FX-1 → R8 (order + text unchanged). R7 absent from Now/Next (verified 0).
- **GATE 3 — BOARD.md: done.** Book-detail row (6), **Amber cell only** per R3 precedent: `✅ Universal-light v8781 · css10708 (R7, v3.191)`. No other cell/row touched.
- **GATE 4 — MW-precondition probe (report-only, no abort):**
  - `praxis-mobile-canon.md`: EXISTS, `wc -c = 9347` → **GREEN**
  - `builder.html`: Mobile canon section renders (3 hits), but `desktop-only` count = **27**, expected 23 → **RED** — flagged: the section + chips DO render (MW-1 not hard-blocked), but the count differs (canon chip set likely grew since the task was authored, or a grep-granularity artifact). **Preston should confirm the MW chip count.**
  - `overnight.md`: EXISTS with ON-1/ON-2/ON-4 queued → **GREEN**
- **GATE 5 — REGEN + integrity: PASS.** PARSER DRY-RUN passed (book-detail.md frontmatter parses, exit 0; the tool has no dry-run flag — usage `sh tools/studio-build`). Regen ran DETACHED, exit 0 ("closed 8/22" — book-detail now counts closed). Integrity: **islands 24 == 24 unchanged** · **page-sections 29 == 29 unchanged** (no section added) · **CRLF 0** · book-detail card **`u-orb--unlit` → `u-orb--lit`** (untouched → closed) with a `desktop-only` mobile badge (no mobile chip — Gate 1's ruling). Byte delta explained: stage1a report **+3068** = the known residual (reconciled by the sweep); book-detail.md +3991 (closed ledger), sequence.md +2207 (R7 shipped + MW re-plan), BOARD +41 (Amber cell), builder.html +290 (re-render).
- **GATE 6 — COMMIT (no push): done.** Path-explicit staging (never `-A`) of exactly 6 files: `book-detail.md`, `sequence.md`, `BOARD.md`, `builder.html`, this report, and the swept M-dirty `stage1a-2026-07-10.md`. Subject: "studio: R7 close — book detail closed, mobile wave lands as Now, Builder regen". NOT pushed (commit is the end). Hash in the session report.

## What Preston checks

Open `docs/studio/builder.html`: **book detail reads `closed`** (the lit amber orb on its card, `desktop-only` mobile badge), **the plan shows MW-1 as Now** (MW-2/MW-3/S-A in Next), and the **GATE 4 probe is green** except the flagged `desktop-only` chip count (27 vs the task's expected 23 — confirm the MW chip set).
