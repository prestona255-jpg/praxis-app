# MW-1 — Shelf + Home mobile pass (canon P1–P9)

**Date:** 2026-07-10
**Task:** MW-1 SWEEP — sweep Shelf + Home to the mobile canon (`docs/studio/praxis-mobile-canon.md`).
**Mode:** SELF-RUNNING. APP-CODE work. Rails: edit only views.js, components.css, theme.css + sw.js (one CACHE_VERSION line) + studio docs at close.

---

## Gate 0 — Recon

### 0.1 Preconditions
- HEAD includes c35c263 (R7 close): **PASS** — HEAD *is* `c35c263`; `git merge-base --is-ancestor c35c263 HEAD` → true.
- Tree clean in scope files (views.js, components.css, theme.css, sw.js): **PASS** — `git status --porcelain` shows zero tracked-modified files; only untracked `??` docs present.
- sequence.md `## Now` reads MW-1: **PASS** — "MW-1 — Shelf + Home mobile pass (SWEEP) … touches: [shelf, home]".

### 0.2 Ground truth
- `sh tools/ground-truth`: HEAD `c35c263`, hook ARMED (`core.hooksPath = hooks`), FIX-PROTOCOL v1.2, 7 agents present.
- Canon `docs/studio/praxis-mobile-canon.md` read in full (v1, 9 patterns, breakpoint 759, two-layer gate).

### 0.3 Chip-count PROBE (Gate-0 settle)
- `badge-mobile` occurrences in builder.html = **28**. CSS rule definitions (lines 184–188) = **5**. Chip ELEMENTS = 28 − 5 = **23**. **Matches expected 23.** The 5 "extras" are the `.badge-mobile*` CSS rules, not stray chips. Every one of the 23 surface cards (builder.html lines 465–487) carries exactly one `badge badge-mobile mob-desktop-only` chip.

### 0.4 Fresh anchors (live line numbers — ledgers cite stale ones)
- `renderShelf` = **js/views.js:3931** (ledger says 3730 — stale). Toolbar block 4037–4264. Head 3964–4020 (`.shelf-head` → `.shelf-title` "Your shelf" + `.shelf-count`). Signed-out hard-gate 3957–3962.
- Toolbar order today: Add-a-book (`btn btn-primary`, 4049) · Covers|List `.seg` (4060) · Sort `.shelf-sort` (4089) · Select (4140) · Filters `.shelf-filters-btn` (4155, mobile-only; `display:none` ≥760 at 9711/11507) · `.spacer` (4172) · Scan shelf (4180) · Scan barcode (4200) · Bulk add (4209) · Resolve covers (4220) · Tidy library (4236) · search `.shelf-search-well`/`.nav-search` (4247). All secondary chips signed-in-only (`if (user)`, 4178).
- `renderHome` = **js/views.js:1447** (ledger says 1440 — stale). Structure: `.home-welcome` (`.home-welcome-title` h2 + `.home-alt` field/left `.seg`) → `.home-variant[field]` (yumiline + `.home-sectlabel` + `.home-wholefield` + `.home-fieldstat`) → `.home-variant[left]` (left-off cards) → `.home-glimpses` (`.home-glimpse` → `.home-gl`+`.home-spines`+`.home-reading-status`). No text inputs. No standalone primary-action button (seg toggle + arc lights + `#books` link are the interactives). Signed-out hard-gate 1461–1467.
- Existing mobile filter-drawer (reference for sheet mechanics): `openShelfFilterPanel`/`dismissShelfFilterPanel` (4898/4912), module var `shelfSidebarEscapeHandler` (views.js:5708). Backdrop-click + close-× + Escape dismiss; **no scroll-lock, no focus mgmt** (so it is NOT a full P1 impl — my Manage sheet adds both).

### 0.5 CSS anchors (assets/components.css, 13071 lines)
- Shelf R2 Amber skin `.shelf.lum-amber-deep` block **11420–11531**: `.shelf-head` 11424, `.shelf-title` 11425 (clamp 30–40px), `.shelf-toolbar` 11433, `.seg`/`.seg-opt` 11441-44, `.nav-search` 11445 (input `font-size:13px` @11447), `.shelf-layout` grid `204px 1fr` @11452, `.shelf-side` sticky @11453.
- Shelf mobile block `@media (max-width:759px)` **11516–11531**: `> * {padding 16px}` 11517; **`.shelf-layout{grid-template-columns:1fr}` 11518 (P8 relational reflow already present)**; `.shelf-grid` 130px 11519; `.shelf-side` drawer 11520; **`.btn,.chip,.seg-opt{min-height:44px}` 11528–11530 (P3 already present)**; `.shelf-filter{min-height:44px}` 11531. Second shelf mobile block (generic `.shelf`) 9719–9730.
- Fixed-bottom `.shelf-selectbar` (select-mode) 11689 (position:fixed;bottom:0;z60). `.shelf-sort`/`.shelf-sort-menu` 11700–11707.
- Home `.home-page.lum-amber-deep` block **12057–12114**; home mobile `@media (max-width:759px)` **12115–12118** (only `.home-arc` col + `.home-arcfield` w100% + `.home-glimpse` basis100% — minimal). Home has NO title-collapse, NO safe-area.
- Global mobile block 5223+: top nav 56px + hamburger; `.yumi-bloom` FAB fixed **bottom-right** (`bottom/right:var(--sp-5)`, z9999); `#app{padding-bottom:calc(96px+env(safe-area-inset-bottom))}` @5255 (bottom scroll-gutter already exists). ⇒ P2 "Add a book" bottom control goes **bottom-LEFT** to clear the Yumi orb.

### 0.6 Token resolution (rails: Universal v1.2 only, no --lum-*/concat)
- Both `books` + `home` ∈ `umberGroundDark` (views.js:373) ⇒ body `data-ground="dark"` on both.
- Under `[data-ground="dark"]` (theme.css:336+): `--surface`=#3e2814, `--color-surface`=--dark-2 (#2a1a0c), `--border`=rgba(210,162,62,.18), `--ink`=cream, `--ink-2/3`=--muted, `--gold`=#d2a23e. `--br-deep`=#1c1209 (:root, un-repointed) ⇒ backdrop via `color-mix(in srgb,var(--br-deep) N%,transparent)` (token-derived, not hard black; 116 color-mix precedents). Radii `--radius-lg 16`/`--radius-xl 22`/`--radius-pill 999`. ⇒ Manage-sheet chrome uses `--surface`/`--border`/`--ink*`/`--gold`/`--radius-*` — all Universal, ground-aware, zero --lum-* introduced.
- P4: `index.html:5` viewport meta ALREADY has `viewport-fit=cover` → satisfied, no index.html follow-up.
- CACHE_VERSION today = `praxis-v3.191` → bump target `praxis-v3.192`.

### 0.7 Gate-0 verdict: **PASS** — proceed to Stage A.

---

## Stage A — Shelf pass

**Files:** `js/views.js` (+135/−9), `assets/components.css` (+~120/−0). New CSS block "MW-1 · SHELF MOBILE PASS" (components.css after the R2 block); new views.js: Manage scaffolding in `renderShelf`, 2 module vars (`shelfManageEscapeHandler`, `shelfHeadScrollHandler`), top-of-render cleanup, P5 scroll listener.

### Per-pattern (walk P1–P9)
- **P1 (STRUCTURAL, ON-2 reference impl) — APPLIED.** One "Manage" control at every viewport. Toolbar visible set (live 390): `[Add-a-book, Sort, Filters, Manage, spacer, search]`; desktop (1265): `[Add, Sort, Manage, spacer, search]` (Filters `display:none`). Sheet body holds exactly the 7 moved controls: Covers|List, Select, Scan shelf (+file input), Scan barcode, Bulk add, Resolve covers, Tidy library — appended as created so handlers ride the elements (no re-wire). Mobile ≤759 = fixed bottom sheet (settled: `position:fixed`, top 388/bottom 840, left 0/right 390, 54vh ≤ 90%, `overflow-y:auto`), scrim backdrop (`opacity:1`, `rgba(15,9,4,.6)`=`--scrim`, fixed), **body scroll-locked** (`overflow:hidden`), **focus moves in** (activeEl=`shelf-manage-close`), **backdrop-tap closes** + **focus returns** to Manage btn, Escape wired. Desktop ≥760 = anchored popover (`position:absolute`, 202–490, within viewport), no scroll-lock. Anchors: views.js `renderShelf` scaffolding + `openManageSheet`/`closeManageSheet`; components.css `.shelf-manage*`.
- **P2 — APPLIED.** `.shelf-add-primary` fixed bottom-LEFT FAB at ≤759 (live: `position:fixed`, left 24, bottomGap 24), hidden on `.is-selecting`; desktop `position:static`. Bottom-left chosen because Yumi bloom is bottom-right (theme.css `.yumi-bloom`).
- **P3 — APPLIED (extends existing 44px rule).** Sampled getBoundingClientRect @390: Add FAB h44, Manage btn h44, Filters h44, sheet close 44×44, seg-opt h44, chip h44. (Base 11528–30 already gave .btn/.chip/.seg-opt 44px; sheet close sized explicitly.)
- **P4 — APPLIED.** `env(safe-area-inset-bottom)` on the sheet padding + the FAB (2 uses). Viewport-meta `viewport-fit=cover` already present (index.html:5) — no follow-up.
- **P5 — APPLIED.** `.shelf-head` `position:sticky top:0` @≤759 + ES3 window scroll listener toggling `.is-stuck` (title font-size 30→22 step). Live @1265: head `position:static` (mobile-only). CSS-first backbone + font-size-only transition (P9-safe).
- **P6 — n/a.** No sibling-pane collapse on the Shelf (the Covers/List seg lives in the sheet as a control, not a region toggle; the shelf grid is one region).
- **P7 — APPLIED.** Filter input `font-size:16px` @≤759 (live: `16px`) + `inputmode=search` (views.js).
- **P8 — APPLIED.** Live @390: `scrollWidth==clientWidth==390`, hScroll=false, **closed AND sheet-open**. Relational: desktop grid `.shelf-layout{204px 1fr}` (11452) reappears 1-col `{1fr}` @759 (11518). (Fixed an 8px right overflow found in verify — sticky-head negative margins removed.)
- **P9 — APPLIED.** Motion vocabulary = exactly {backdrop opacity fade, sheet `translateY` slide, title font-size step}; `prefers-reduced-motion` block zeros all. 0 transform-rig tokens.

### Verify — static (hard evidence)
- `cscript tools/parse-check js/views.js` → **PARSE OK**.
- New CSS block scan: **0** `var(--lum-`, **0** `backdrop-filter`/`blur(`, **0** hardcoded hex, **0** setProperty/transform-rig. `transform:` only `translateY(100%)`/`none` on the sheet class. `env(safe-area-inset-bottom)`×2, reduced-motion block ×1.
- Brace balance 3447/3447. Diff purely additive (components +/−0 deletions), no EOL flip (numstat localized).

### Verify — live 390 CDP (real app, auth gate stubbed locally for layout — non-destructive, no Firebase writes)
- All values above are live getBoundingClientRect / getComputedStyle readouts at 390×840 and desktop 1265×840. Console: **0 errors**.
- NOTE: CSS transitions do not advance frames in this headless pane (a known artifact) — the sheet slide/backdrop-fade were verified by reading the SETTLED cascade result with the transition bypassed (`transition:none`), which is correct (sheet `transform:none`→top 388; backdrop `opacity:1`). Screenshots timed out (renderer wedged); live-DOM structural proof is the hard evidence per CLAUDE.md, and it is complete.

### Gates
- **fix-red-team → BLOCK (1 blocker), fixed → re-verified.**
  - **Finding 1 (BLOCK): scroll-lock leaks on back-nav.** Body `overflow:hidden` was released only in `renderShelf`/`closeManageSheet`; opening the mobile sheet then hitting OS **back** (`hashchange`→`renderRoute`, which does NOT call `renderShelf`) stranded the next surface permanently unscrollable. **FIX:** global cleanup in `renderRoute` (views.js:359, beside the existing nav-close) — release `body.style.overflow` + purge both handlers on every route change. This also closes **Finding 2** (listener leaks on nav-away). **Live re-verify @390:** open sheet → `body.overflow='hidden'`; simulate back-nav (`#home`+`renderRoute`) → `body.overflow=''`, `shelfManageEscapeHandler`/`shelfHeadScrollHandler` both `null`. PASS.
  - **Nits fixed:** `aria-modal` now tracks the mobile sheet (`true` at ≤759, verified live) + reset on close; desktop popover got `max-height:calc(100vh-120px); overflow-y:auto` (7 controls on a short viewport).
  - **Nits recorded as residuals (not fixed, low sev):** (a) no focus-trap on the mobile sheet — it has focus-in + Escape + scrim + backdrop-tap, which is reasonable; a full Tab-trap is a nice-to-have follow-up. (b) modal-over-sheet: tapping Scan/Bulk/Resolve/Tidy opens their editor without closing the sheet (sheet stays open behind, body relocks-clears on sheet close — not a permalock; deliberately NOT auto-closed to avoid an overflow-unlock race with those editors' own scroll handling).
  - fix-red-team PASS on all other seams (handler preservation, focus, z-index/FAB-in-select, desktop, ES3/tokens/scope).
- **praxis-reviewer → CLEARED TO COMMIT.** All 9 protocol gates PASS/N-A; ES3 sweep clean; token cascade traced (Shelf redeclares `--ink #241710`/`--gold-deep #855410` locally at 0,2,0; `--page-2`/`--line-page*`/`--scrim`/`--font-mono`/`--radius`/`--sp-5` untouched → light everywhere); WCAG hand-computed: `--gold-deep` on `--page-2` = **5.95:1**, `--ink` on `--page-2` = **16.17:1** (both AA); P1–P9 fidelity verified (not just read). Two non-blocking findings: (1) `.shelf-manage-btn`'s `btn-quiet` is specificity-shadowed by `.shelf.lum-amber-deep .btn` so it renders as a plain `.btn` — **accepted** (identical treatment to the existing `.shelf-filters-btn`, visually consistent, cosmetic); (2) desktop popover missing `max-height`/`overflow` — **already fixed** in the red-team cycle. Reviewer also flags: the checkpoint must carry the byte deltas (now recorded below) and the VISUAL GATE (Preston's settled-screenshot felt pass) remains a human gate for round close, not satisfiable by the executor.

### Byte deltas (PROTOCOL §3, measured after both fix cycles)
- `assets/components.css`: **+122 / −0** lines (numstat; purely additive, LF worktree, content +≈6.6 KB). New `shelf-manage*` selectors: 23. `env(safe-area-inset-bottom)`: 2.
- `js/views.js`: **+157 / −9** lines (CRLF worktree; content +≈6.3 KB). `manageBody.appendChild`: 8 (the 7 controls + scan file-input). The −9 = the 8 `toolbar.appendChild`→`manageBody.appendChild` retargets + the newBtn className line.
- EOL: both files keep their pre-existing convention (`components.css` LF, `views.js` CRLF — a repo-wide per-file split, confirmed against untouched siblings). No flip.

### Stage A verdict: **PASS** — both gates green, live-DOM proof complete. → commit.

---

## Stage B — Home pass

**Files:** `assets/components.css` only (+14/−0; no JS). Two additive edits since a405730: (a) P3 44px rules in the Home mobile block; (b) a `≤759` `.home-welcome::before{ left:0; right:0 }` override placed after the base rule (P8 lamplight fix).

### Per-pattern (walk P1–P9)
- **P1 — n/a.** Home has no overflow/management action cluster to collapse into a sheet (it is a navigational landing: seg toggle + arc lights + `#books` link). One line: nothing to sheet.
- **P2 — n/a (reasoned).** Home has no single primary-verb button — its interactives are the field/left seg, the arc lights (enter an arc), the left-off arc buttons, and the "Your shelf →" link. There is no "the one thing the user came to do" button to thumb-anchor; adding a bottom bar would invent a control the surface doesn't have ("apply what FITS").
- **P3 — APPLIED.** Home tappables were sub-44px: `.seg-opt` ~31px, `.home-arcbtn` ~34px, `.home-gl-link` tiny. Added `min-height:44px` (+ inline-flex centering) at ≤759. Live @390: seg-opt **h44**, gl-link **h45**, spine h79 (already ≥44). Desktop @1265: seg-opt computed `min-height:auto` (mobile-only, desktop untouched).
- **P4 — n/a.** No new bottom-anchored chrome on Home (the global `#app` bottom gutter + safe-area already clears the Yumi orb; unchanged).
- **P5 — n/a (reasoned).** Home's `.home-welcome-title` is a greeting ("Welcome back."/"Welcome to Praxis."), not a persistent orientation title like the Shelf's "Your shelf"; Home is not in canon P5's named set (Shelf/Arcs/Notebook/Account). Making a greeting a sticky-compact bar would be strange; it scrolls away naturally.
- **P6 — SATISFIED (existing, no new work).** The field/left variant toggle is a **display switch on persistent panes** — `homeShowVariant` (views.js:1354) sets className to `home-variant` / `home-variant home-variant-hidden` (`.home-variant-hidden{display:none}`), so both panes stay in the DOM and neither re-renders/drops handlers. Meets P6's "panes hide, not destroyed."
- **P7 — n/a.** Home has no text inputs.
- **P8 — APPLIED (fixed a PRE-EXISTING overflow).** At 390 Home carried a standing **23px horizontal scroll** (scrollWidth 413) — traced to the `.home-welcome::before` lamplight (`inset:-16% -8%`) spilling ~8% (~30px) past the viewport edge (getBoundingClientRect showed 0 element offenders; the container `.home-welcome` had scrollW 404 vs clientW 374 = the pseudo). Fixed with `left:0; right:0` at ≤759 (vertical -16% wash kept — felt glow preserved). Live @390: **scrollWidth 390 = clientWidth, hScroll false, welcome overflow 0, 0 doc offenders**. Relational: desktop `.home-arc` row → `flex-direction:column` @759 (12238), `.home-glimpse` → `flex-basis:100%` (12240). NOTE: this overflow was PRE-EXISTING (independent of MW-1) — confirmed my P3 `inline-flex` was not the cause (reverting it left scrollWidth 413); the Home mobile pass owns the P8 gate, so it is fixed here.
- **P9 — n/a for new work.** Stage B introduces no new motion (sizing + one inset override only); existing Home transitions + reduced-motion coverage stand.

### Verify
- **Static:** brace balance 3452/3452; new lines 0 `--lum-*`, 0 new hex; +14/−0 (purely additive); only `components.css` touched (no JS → no parse needed). Every new rule is inside a `@media (max-width:759px)` block → desktop byte-unchanged.
- **Live 390 + 1265 CDP** (auth stubbed locally for layout, non-destructive): values above are live getBoundingClientRect/getComputedStyle readouts; **console 0 errors** both widths.

### Gate
- **praxis-reviewer → CLEARED TO COMMIT.** All 9 gates PASS/N-A; +946 B / 14 lines / 0 deletions (independently summed), desktop byte-unchanged (both new rules inside `@media (max-width:759px)`), EOL unchanged, scope = `components.css` only. Cascade placement independently retraced (override at 12332–34 sits after the base `::before` at 12326 — correct; only `left`/`right` added, vertical `-16%` wash preserved). Inline-display trap checked (`.home-gl-link` `<a>` correctly paired with `inline-flex`, else `min-height` would no-op). Matches the Shelf `.seg-opt` precedent.
  - **Advisory (non-blocking, recorded as residual):** live P3 sampling covered 2 of 3 selectors (seg-opt h44, gl-link h45); `.home-arcbtn` was not live-measured (the layout test seed didn't render a left-off card). The rule is a **provable hard floor** (reviewer grep-confirmed no colliding `min-height` override; `.home-arcbtn` base is already `inline-flex`, so 44px takes effect) — verified structurally, not live-sampled. A full 3-of-3 live sample is a follow-up at Preston's felt pass.
- fix-red-team: N/A (CSS-only, no JS/state/data).

### Stage B verdict: **PASS** — gate green, live P8+P3 proof (seg-opt/gl-link) + structural arcbtn floor. → commit.

---

## Stage C — Close

Runs even though both surfaces passed (recording reality).

1. **overnight.md ON-2 → `status: closed`** — note "ABSORBED BY MW-1 … shipped as the P1 REFERENCE IMPLEMENTATION (a405730)"; `anchors:` filled with the shipped sheet code (openManageSheet/closeManageSheet, the 7 relocations, renderRoute cleanup, the `.shelf-manage*` CSS block); evidence + report set. (Stage A passed, so ON-2 closes.)
2. **Shelf (books.md) + Home (home.md) ledgers** — frontmatter `mobile: native` on both; MW-1 round-history entries with per-pattern evidence, anchors, live-390 numbers, honest residuals. **CHIP RULINGS:** both `native` — every applicable canon pattern verified on both layers (static relational + live 390 CDP), evidence anchored in this report. (Shelf: P1,P2,P3,P4,P5,P7,P8,P9; P6 n/a. Home: P3,P8; P6 pre-satisfied; rest n/a.)
3. **sequence.md** — MW-1 → `## Shipped` (SHIPPED-LOCAL, not pushed); **`## Now` advanced to MW-2** (Notebook + Arcs), promoted from Next; dated Re-plan-log entry (pure program advance on completed work — no `PROPOSED:` flag). Both surfaces passed → Now = MW-2.
4. **Builder regen** (`sh tools/studio-build`, run harness-tracked so it wasn't killed mid-write): **INTEGRITY PASS** — islands **24**, sections (`class="page"`) **29**, CRLF **0** (committed blob is LF; `git ls-files --eol` = `w/lf`, HEAD + renormalized-staged both 0 CR — working-tree CRLF is autocrlf-cosmetic), Shelf + Home chips render **`mob-native` "native"**, NOW card = **MW-2**. Byte delta `builder.html` +186/−25 explained: bstamp HEAD `a246e4a`→`e5ab754` + generated-date, the 2 chip flips (desktop-only→native), and my ledger/sequence/overnight markdown is embedded verbatim in the `data-md` islands.
5. **Cache bump** — `sw.js` CACHE_VERSION `praxis-v3.191` → `praxis-v3.192`, its own commit `1e4b90c` under the **armed pre-commit hook** (non-interactive: structural filename BLOCKs + non-blocking ES3 warn; passed, no `--no-verify`). Then this docs close-out commit.

## Commits (all LOCAL — NOT pushed; origin/main stays `bff5d82`)
- `a405730` — mw1: shelf mobile pass — canon P1-P9 (ON-2 absorbed) [views.js +157/−9, components.css +122/−0]
- `e5ab754` — mw1: home mobile pass — canon P2-P9 [components.css +14/−0]
- `1e4b90c` — mw1: cache bump v3.191 → v3.192 — shelf + home mobile pass ships [sw.js +1/−1]
- `<this commit>` — studio: MW-1 close — shelf+home mobile pass, chips ruled, Builder regen [docs; self-referential hash, so left as a descriptor]

## Aggregate diff --stat (c35c263..HEAD)
```
 assets/components.css                 | 136 +
 docs/studio/books.md                  |  31 +
 docs/studio/builder.html              | 211 (+186/-25)
 docs/studio/home.md                   |  27 +
 docs/studio/overnight.md              |  10 (+5/-5)
 docs/studio/reports/mw1-2026-07-10.md | 123 +  (grows with this Stage C/Final section)
 docs/studio/sequence.md               |  26 (+19/-7)
 js/views.js                           | 166 (+157/-9)
 sw.js                                 |   2 (+1/-1)
 9 files, 685 insertions(+), 47 deletions(-)
```

---

## Final report

### Per-surface verdict
- **Shelf — PASS** (both gates green; live-DOM proof complete on 390 + 1265).
- **Home — PASS** (gate green; live P8+P3 proof, arcbtn structural hard-floor).
- Both surfaces passed → MW-1 SHIPPED-LOCAL; Now = MW-2.

### Per-pattern one-liners
**Shelf:** P1 Manage sheet(≤759)/popover(≥760), one JS path, 7 controls relocated with handlers, scroll-lock+focus+Escape+backdrop, back-nav release in renderRoute — APPLIED · P2 bottom-left Add FAB — APPLIED · P3 44px (close 44×44 + inherited floor) — APPLIED · P4 safe-area ×2 — APPLIED · P5 sticky-title compaction — APPLIED · P6 n/a · P7 16px + inputmode=search — APPLIED · P8 zero h-scroll @390 (fixed an 8px sticky-head overflow) — APPLIED · P9 {opacity, sheet-slide, title font-size} + reduced-motion — APPLIED.
**Home:** P1 n/a · P2 n/a (no single primary verb) · P3 44px on seg/arcbtn/gl-link — APPLIED · P4 n/a · P5 n/a (greeting ≠ orientation title) · P6 SATISFIED (existing display-switch panes) · P7 n/a · P8 fixed a PRE-EXISTING 23px lamplight overflow (413→390) — APPLIED · P9 n/a (no new motion).

### Chip rulings + why
- **Shelf → `mobile: native`** — all 8 applicable patterns verified both layers, anchors in the ledger.
- **Home → `mobile: native`** — applicable patterns (P3, P8) verified both layers, P6 pre-satisfied; the sole gap (arcbtn not live-sampled) is a provable hard floor (reviewer-confirmed), a follow-up at the felt pass.

### Gate-0 chip-count answer
builder.html `badge-mobile` = **28** occurrences − **5** CSS rule definitions (lines 184–188) = **23** chip ELEMENTS (one per surface card). Matches expected 23; the 5 "extras" are the `.badge-mobile*` CSS rules, not stray chips. (Post-MW-1 the builder now shows 5 `mob-native` among those 23.)

### Residuals (honest)
- Shelf: no Tab focus-trap on the mobile sheet (has focus-in + Escape + scrim + backdrop-tap); modal-over-sheet not auto-closed (avoids an overflow-unlock race); the slide/fade animation couldn't be observed *settling* in the headless rig (verified via settled-cascade + reduced-motion static) and screenshots timed out — live-DOM structural proof is the hard evidence.
- Home: `.home-arcbtn` 44px verified structurally, not live-sampled (seed didn't render a left-off card).
- The `.shelf-manage-btn`'s `btn-quiet` is specificity-shadowed to a plain `.btn` (accepted — matches the existing Filters button).

### Named follow-up (P4 viewport check)
Canon P4 names an index.html `viewport-fit=cover` check — it is **already present** (index.html:5). No index.html edit needed; reported per rails (index.html is FORBIDDEN to edit this pass anyway).

### FELT-PASS CHECKLIST for Preston (on your phone, after you push)
- **Shelf** (`#books`, signed in): tap **Manage** → the sheet **slides up** from the bottom · the dim **backdrop dismisses** it on tap-outside (and the × closes it) · **Add a book** sits in **thumb reach** at bottom-left · **no sideways scroll** in Covers, List, or with the sheet open · Filters/Sort/search still work.
- **Home** (`#home`, signed in): the greeting/segmented **everything is tappable without zooming** · **no sideways scroll** · the whole-field wash still reads (glow preserved).
- Check at **390** and **~1280**; the Manage control is one button at both widths (bottom sheet on phone, anchored popover on desktop).

**NO PUSH** — round CLOSE (your felt pass + `git push`) is yours. Deploy = push `main`; verify `praxis-v3.192` in DevTools after the Netlify build, hard-refresh past the sticky SW.
