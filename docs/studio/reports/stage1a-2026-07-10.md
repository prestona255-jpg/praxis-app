# Stage 1a — Overnight System Structure — Run Report

- **Date:** 2026-07-10
- **Task:** Builder infra only (docs/studio + tools/studio-build + generated builder.html). NO app code.
- **Contract:** self-running, gate-driven (PASS → proceed; FAIL → per-gate abort rule). COMMIT only, never push.
- **Scope allowed:** docs/studio/overnight.md (new), tools/studio-build, docs/studio/builder.html (generated), this report, optional CLAUDE.md pointer line.

---

## Status: COMPLETE — committed `1ac1536`, NOT pushed

_(GATE 0 · GATE 1 · GATE 2 all PASS; Stage 3 committed. No aborts. Addendum: none present.)_

---

## GATE 0 — Recon → **PASS**

**HEAD:** `05e653a studio: R7 S5 — book-detail light re-ground (scoped) + BD2 mobile 4-I`
Ground-truth: hook gate ARMED, FIX-PROTOCOL v1.2, 7 agents present.

**Parallel-aware clean check → PASS (proceed).**
- `git status --porcelain --untracked-files=no` → **empty**. Zero modified tracked files anywhere in the repo. No canon studio file (sequence.md, builder.html, tools/studio-build, universal-depth.css, any ledger, CLAUDE.md) is modified → **no close-out / studio task is mid-flight**.
- No R7 app-file modifications present at this instant either (the parallel round has nothing staged right now); if it stages later it cannot ride my path-explicit commit.
- **Untracked artifacts under `docs/studio/`** (accepted findings, NOT abort triggers — stale, not mid-flight, not touched by this task, excluded from the path-explicit commit):
  - `docs/studio/mockups/home.html` — mtime 2026-07-09 10:44 (~37h old; read-only per non-goals)
  - `docs/studio/notebook-recon.md` — mtime 2026-07-09 14:41 (~33h old; old scan output)
  - `docs/studio/praxis-launch-tracker-light.html` — mtime 2026-07-07 17:16 (3 days old)
  - `docs/studio/reports/` — created by THIS run (the report dir).
  - Rationale: the abort rule guards against a mid-flight close-out/studio task, which would appear as MODIFIED tracked canon. None is modified; these three are 33h–3 days stale. The generator does not read them; Stage 3 uses explicit `git add <paths>`, never `-A`.

**Generator understanding (tools/studio-build, POSIX sh, extensionless):**
- Sources parsed: `sequence.md` (parse_items for Now/Next/Then/Shipped/Discovered; parse_bullets for Re-plan log/Standing rules/Open calls), per-surface `$STUDIO/<slug>.md` ledgers (frontmatter via `fm`, gapcount, has_brief, state, rounds), `INDEX.md`, and `design/*.html` (mockups list).
- Whole doc is built inside a `{ … } > "$OUT"` block (L258–732). Pages = `<section class="page" id="page-…">` (Overview, Plan, Mockups, About) + surface pages `id="s-<slug>"`. Sidebar `.brail` carries `.brail-dest` nav links to #page-overview / #page-plan / #page-mockups.
- No built-in `--dry-run`. Plan for Stage 2: validate the new overnight.md parser standalone (awk over the file, eyeball emitted records) BEFORE the full regen that overwrites the tracked builder.html; then regen DETACHED (the ~5-min foreground tool cap can truncate mid-write).
- Ingesting a NEW source doc = add an awk parse fn → precompute into `$TMP/` → render one new `<section class="page" id="page-overnight">` + one `.brail-dest` nav link. Additive; existing sections untouched.

**Baseline integrity metrics (builder.html @ clean HEAD):**
- top-level page sections (`<section class="page[ "]`): **27**
- markdown islands (`type="text/markdown"`): **24**
- CRLF lines in working tree: **0**
- size: **2352 lines / 298802 bytes**
- nav dest-link occurrences: **3** (Overview, Plan, Mockups)

**Rubric pointer placement:** CLAUDE.md `## Studio Protocol (added July 2026)` (L414) is the unambiguous studio section → the one-line pointer WILL be placed there (not skipped).

`overnight.md` does not yet exist (confirmed) — Stage 1 creates it fresh.

## Stage 1 — overnight.md → **GATE 1 PASS**

Created `docs/studio/overnight.md` (frontmatter incl. `current-round: R7` + four parts):
- **(A)** intake rubric — 3 buckets (OVERNIGHT / ROUND GAP / PROGRAM) with verbatim rules (single-surface; CSS + views.js ONLY, state/integrations/rules/sw categorically excluded; objectively verifiable; revert-safe; max 3/night; each = own commit; FAIL→revert+continue; COMMIT ONLY no push; visual→felt-pass-required not code-closable; STALE at 2+ rounds) + sorting (append proposed; buckets confirmed in chat; no self-promote).
- **(B)** item schema — all 14 fields (id · title · plain · surface · bucket · files · anchors · verify · revert · felt-pass-required · status{proposed|queued|ran-PASS|ran-FAIL|awaiting-felt-pass|closed} · evidence · report · proposed-in).
- **(C)** `## Runs` — appendable, seeded empty with a FORMAT comment.
- **(D)** seed items: **queued(3)** ON-1, ON-2 (Mobile-Canon P1 ref-impl note), ON-4; **proposed(2)** ON-5, ON-6; **## Confirmed non-overnight** → DEL-1 (dedicated supervised build).
- Self-check greps: buckets 1/1/1 · overnight-rules 6 · sorting 5 · schema fields 14 · Runs+FORMAT 1 · ids ON-1/2/4/5/6+DEL-1 · queued 3 · proposed 2 · CRLF 0. **All present.**

**Refined baseline for GATE 2 (precise `grep -oE` occurrence patterns, immunized against inlined-report escaping):**
- islands `<script type="text/markdown"` → **24**
- pages `<section class="page[ "]` → **27**
- CRLF → **0** · size **298802 bytes**

## Stage 2 — Generator Overnight panel → **GATE 2 PASS**

Six additive edits to `tools/studio-build` (no existing section restructured):
1. `parse_overnight()` — awk over the `## Queue` section → 8 US fields per item (id·title·plain·surface·files·felt·status·proposed-in).
2. Precompute — `$TMP/overnight`, `$TMP/ovruns` (last-5 Runs), `OV_CURRNUM` (from `current-round`), `OVQ/OVP/OVTOT` counts. Inert if `overnight.md` absent.
3. Render helpers — `ov_render_item` (card + STALE-if-2+-rounds + felt flag), `ov_render_group` (status group + count + empty line), `ov_render_report` (newest report from `docs/studio/reports/`, HTML-escaped verbatim in a collapsible `<pre>`).
4. Sidebar nav — one `.brail-dest` "Overnight" link after Mockups.
5. Overnight `<section id="page-overnight">` — after Plan: stats · TONIGHT'S SLATE (queued ≤3) · groups Proposed/Queued/Awaiting-felt-pass/Closed · Confirmed non-overnight (DEL-1) · Runs last-5 · latest-report slot.
6. CSS block before `</style>` (slate, ov-item, ov-stale, ov-felt, ov-report/pre…), reusing existing Builder tokens.

**Parser DRY-RUN (before any regen):** parse_overnight → 5 clean records (ON-1/2/4/5/6, all fields, Unicode preserved); Confirmed non-overnight → DEL-1; Runs → empty; current-round → R7. `sh -n tools/studio-build` → **SYNTAX OK**.

**Regen (detached, exit 0):** `studio-build: wrote …/builder.html (HEAD 05e653a, praxis-v3.190, …; closed 7/22, gaps 185, ship 15, map 6)`.

**GATE 2 integrity (measured, before → after):**
- islands `<script type="text/markdown"` : **24 → 24** (pre-existing metric-identical ✓)
- pages `<section class="page[ "]` : **27 → 28** (exactly one added: `id="page-overnight"` ✓)
- CRLF : **0** ✓ · file ends `</html>` (not truncated) ✓
- size : 298802 → **312102 bytes** (Δ **+13300**, +104 lines — CSS + section + inlined report)
- Report-escaping immunization verified: report's raw `<section class=` → `&lt;section` in the `<pre>`, so pages stayed 28 (uninflated).

**Panel content (rendered occurrence-greps):** nav link ✓ · stats 3q/2p/5t ✓ · slate = ON-1,ON-2,ON-4 ✓ · Proposed(2)=ON-5,ON-6 · Queued(3)=ON-1,ON-2,ON-4 · Awaiting-felt-pass(0) · Closed(0) ✓ · rendered stale badges **0** (ON-5/6 at R7 = 0 rounds old; machinery present as CSS `.ov-stale`) ✓ · felt badges **5** ✓ · DEL-1 listed ✓ · Runs "no runs yet" ✓ · report inlined `stage1a-2026-07-10.md` ✓.

_(A second detached regen follows to inline this finalized report — CURRENCY — then GATE 2 is re-verified on the committed file.)_

## Stage 3 — Commit → **DONE (committed, NOT pushed)**

- **Commit:** `1ac1536` — `studio: overnight system — queue doc, intake rubric, Builder overnight panel` (em-dash intact).
- **`git diff HEAD~1 --stat` — exactly the 5 intended files, no app code:**
  - `CLAUDE.md` (+5) · `docs/studio/builder.html` (+128/−2) · `docs/studio/overnight.md` (+186, new) · `docs/studio/reports/stage1a-2026-07-10.md` (+88, new) · `tools/studio-build` (+139). Total **546 insertions, 2 deletions**.
- Pre-commit hook **passed** (no top-level `*.js` in the diff → no sw.js-bump gate; `tools/studio-build` is extensionless by design).
- **Not pushed:** `HEAD 1ac1536` is **1 ahead / 0 behind** `origin/main` (`bff5d82`). Task ends at commit.

### Parallel-round note (expected, handled)
During this run the R7 Book-Detail session landed two commits, advancing HEAD `05e653a → d5041ee → bff5d82`:
- `d5041ee` — R7 fixes → `assets/components.css`, `js/views.js` only.
- `bff5d82` — cache bump v3.190→v3.191 → `sw.js` only.
Neither touched my scope (docs/studio, tools/studio-build, CLAUDE.md). My working-tree changes stayed intact and unbundled; my commit's parent is `bff5d82`. The regenerated `builder.html` stamp refreshed to `bff5d82 / praxis-v3.191` — the correct generator behavior (it stamps current repo state), **not** an R7-content entanglement (R7 changed no studio markdown, so the builder diff is purely stamp + nav + the Overnight panel + inlined report). I did **not** touch `sw.js` or bump any cache.

### WHAT PRESTON CHECKS
Open `docs/studio/builder.html` → sidebar **Overnight** → the **Overnight** panel shows **ON-1 · ON-2 · ON-4** in Tonight's slate (also under Queued); **ON-5 · ON-6** under Proposed; **DEL-1** under Confirmed non-overnight; run-history "no runs yet"; the latest report inlined.

### Honest residuals
- The commit-evidence above is appended to this report **on disk after the commit**, so the committed blob of this report necessarily predates its own hash `1ac1536` (unavoidable chicken-and-egg). This trailing report edit is an intentional, unpushed working-tree change.
- The report inlined in `builder.html` is the regen-#2 snapshot (through Stage 2 PASS; it does not contain this commit hash). It self-heals on the next `sh tools/studio-build` per the CURRENCY rule.
- Three stale untracked `docs/studio/` artifacts (`mockups/home.html`, `notebook-recon.md`, `praxis-launch-tracker-light.html`) were left untouched and excluded from the commit (path-explicit staging), as at GATE 0.

---

## ADDENDUM (Stage 1a-M)
**None present.** The prompt's `═══ PASTE THE STAGE 1a-M ADDENDUM BELOW THIS LINE ═══` marker had nothing after it → no addendum to chain. Recorded as **not-run (absent)**.

## FINAL OUTCOME
**Stage 1a COMPLETE — all gates PASS, committed as `1ac1536`, not pushed.**
GATE 0 PASS · GATE 1 PASS · GATE 2 PASS · Stage 3 committed. No aborts.
Delivered: the overnight queue source doc (intake rubric + item schema + Runs log + seeded items), the generator's Overnight panel, a CLAUDE.md pointer, and this report.
