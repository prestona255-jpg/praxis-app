# VL-1 + LENS-1 — generator cross-awareness + lens index-grounding (paired fix)

**STARTED** 2026-07-11 · HEAD (confirm below) · sw.js target v3.196 → v3.197
Follows docs/FIX-PROTOCOL.md. Self-running to the commit gate (Stage 4 HALT,
Path B); NEVER push.

## The two fixes
- **LENS-1** — `generateLenses`/`evalLensResponse` still ground by TITLE STRINGS
  (the exact output-truncation bug RF1 diagnosed; latent at 129 books). Port the
  proven RF1 index-grounding fix verbatim in idiom.
- **VL-1** — the two sibling generators (lens gen v3.116, value retrofit
  v3.195/v3.196) don't know each other exists, so on the same corpus they
  converge on kin names (Preston's live finding: suggested values duplicated his
  lens list). Values = normative commitments (marks/lineage/load); lenses =
  descriptive angles on the shelf. Enforce the difference in BOTH the prompt (it
  persuades) and the eval (it enforces), both directions.

## Rails
- Editable: `yumi-brain.js`, `views.js`, `components.css`, ONE-line `sw.js` bump
  (v3.196 → v3.197) LAST, + report docs (incl. the r8-rf1 status ride-along).
- READ-ONLY: `integrations.js`, `state.js`, `firestore.rules` (one permitted
  mid-run stop if a fix truly needs them).
- Strict ES3, Universal tokens, path-explicit staging, NEVER push.
  `prestona255` never touched — fixtures only.

---

## STAGE 0 — RECON

### The two generator chains (anchored, HEAD 3d0843f)
**LENS chain** (title-grounded today — the LENS-1 bug):
- prompt: `LENS_GEN_SYSTEM` (`yumi-brain.js:840`) + `buildLensGenUserMessage`
  (`:922`, BULLET list `- "title"` + `lensNames`).
- gather: `gatherLensLibraryMetadata` (`:875`) → `{books, lensNames, titleToId}`;
  `lensNames` = distinct present genres + `state.userThemes[k].name`
  (userId===uid) (`:901-915`). No cap.
- proxy: `generateLenses` (`:941`, `claude-sonnet-4-6`, **max_tokens 1024**).
- eval: `evalLensResponse` (`:1005`) — EXACT-TITLE grounding (the truncation +
  drift bug).
- **callers (2):** `views.js:18443` `startPortraitLensSuggest` (EDITABLE) and
  `yumi-ui.js:1480` `startLensSuggest` (**NOT in the editable rail**). BOTH build
  `titles` from `meta.books[i].title` in order and call
  `evalLensResponse(raw, titles)`. Both already have loading/done/error states.

**VALUE chain** (index-grounded, RF1 v3.196 — the pattern to mirror):
- `VALUE_GEN_SYSTEM` (`:1067`) + `buildValueGenUserMessage` (`:1148`, NUMBERED) →
  `gatherValueMetadata` (`:1132`) → `generateValueRetrofit` (`:1166`) →
  `evalValueResponse` (`:1250`, index-mapped) → sole caller `views.js` retroRun
  (`:18137`, honest error-vs-empty via `valueRetrofitUnreadable`).

### Storage at generation time
- **lens names:** genres + userThemes names → already computed as `meta.lensNames`.
- **value slugs:** `profile.values` (array) via `getProfile(uid)`.

### Design decisions forced by the uneditable `yumi-ui.js` lens caller
The fix must reach BOTH lens callers through code I can edit (`yumi-brain.js`,
`views.js`) only:
1. **Index-grounding** works for both callers unchanged — they already pass
   `titles` built from `meta.books` in order; I number `buildLensGenUserMessage`
   from the SAME `meta.books` and index-map in the eval. No caller change needed.
2. **Prompt cross-awareness** flows via the gatherer: `gatherLensLibraryMetadata`
   gains `valueNames` (reads `profile.values`); `buildLensGenUserMessage`
   includes them. Both callers pass `meta` through → both get it. No caller change.
3. **Eval backstop** — the lens eval gets `valueNames` as an optional 3rd arg
   (portrait caller passes `meta.valueNames`; the yumi-ui caller passes nothing →
   the eval FALLS BACK to reading `profile.values` internally). Universal coverage.
4. **Honest empty-vs-error (silence impossible)** for BOTH lens callers:
   `generateLenses` REJECTS on an unreadable/truncated reply → both callers'
   EXISTING reject handlers → their graceful error state; a parseable-but-empty
   reply still resolves → their "no lenses" empty state. (Value path keeps RF1's
   caller-level `valueRetrofitUnreadable` — untouched.)
5. **Defensive annotated-first cap** must live in `gatherLensLibraryMetadata` (the
   shared gatherer) so `meta.books` is capped BEFORE either caller builds `titles`
   (numbering stays aligned). `gatherValueMetadata` re-caps (no-op at ≤240).

No `integrations.js`/`state.js`/`firestore.rules` edit implicated — the whole
change is `yumi-brain.js` + `views.js` (+ `components.css` only if a touched lens
render needs a mobile wrap; + `sw.js` bump last).

### GATE 0 — clean, no fork. Proceed to Stage 1.

## STAGE 1 — LENS-1 (port the RF1 index-grounding fix)
- `LENS_GEN_SYSTEM` — numbered-list framing; `books` = "ARRAY OF INTEGERS …
  1-based index NUMBERS … cite 2 to 6 … never a title string"; JSON example
  `"books":[3,17]`.
- `buildLensGenUserMessage` — emits `N. "title" by author [genre]` (1-based),
  "Cite books by their index numbers."
- `evalLensResponse` — REWRITTEN to index-grounding: maps cited index →
  `libraryTitles[idx-1]`, bounds/distinct/≥2, blocklist, cap 5, numeric-string
  tolerant. **NO display cap on `books`** — the lens adopt path assigns all of
  them (post-review FIX #1; the value eval keeps the cap). Both callers pass
  `titles` from the SAME `meta.books` order → aligned (no caller change).
- Honest empty-vs-error: `generateLenses` now REJECTS an unreadable/truncated
  reply (`parseValueRetrofitArray===null`) → BOTH callers (incl. the uneditable
  `yumi-ui` rail) hit their EXISTING error state; a parseable-but-empty reply
  resolves → the honest "no lenses" empty. Silence impossible.
- Defensive annotated-first cap in `gatherLensLibraryMetadata`
  (`prioritizeValueBooks`, `VALUE_RETROFIT_MAX=240`) — capped in the gatherer so
  both callers number the same trimmed list; never binds ≤240.

## STAGE 2 — VL-1 (cross-awareness, both directions)
- **Prompt.** `buildValueGenUserMessage` now includes existing LENS names ("these
  are ANGLES … do NOT propose a value that duplicates/rephrases"); `VALUE_GEN_
  SYSTEM` reinforces "a value is a COMMITMENT, never a LENS." Mirror:
  `buildLensGenUserMessage` includes declared VALUE names ("COMMITMENTS … do NOT
  propose a lens that duplicates"); `LENS_GEN_SYSTEM` reinforces "a lens is an
  ANGLE, never a VALUE." `gatherLensLibraryMetadata` gains `valueNames` +
  `themeLensNames`; `gatherValueMetadata` returns both `lensNames` (for the
  prompt) and `themeLensNames` (for the eval).
- **Eval backstop (enforcement).** `nameCollidesWith(name, otherNames)` — norm
  exact-match OR **WHOLE-WORD containment** either direction (`\b<needle>\b`,
  ≥3-char guard; post-review FIX #2 — an incidental infix like "war" inside
  "toward" does NOT collide, but "Power"↔"Power's Legibility" still does).
  `evalValueResponse` drops a value colliding with a **DECLARED** lens
  (`meta.themeLensNames` = userThemes only, NOT market genres — post-review
  FIX #2b); `evalLensResponse` drops a lens colliding with a value (portrait
  passes `meta.valueNames`; the yumi-ui rail falls back to
  `readActiveValueNames()` → `profile.values`). Every drop is `console.log`'d
  `[VL-1]`.
- **Caller wiring (views.js, 2 one-line changes):** retroRun →
  `evalValueResponse(raw, titles, meta.themeLensNames)`; portrait lens →
  `evalLensResponse(raw, titles, meta.valueNames)`. `yumi-ui.js` untouched.

### Mechanical gate (final, after the post-review fixes)
PARSE exit 0 (yumi-brain, views, sw.js; harness proven to fail an ES5 copy).
ES3 added lines CLEAN (parse-gate; the only backticks were in comments, since
removed). **BYTES (measured LF-normalized, `git cat-file -s HEAD:f` vs
`tr -d '\r' <f|wc -c`):** yumi-brain.js 142,724→149,354 = **+6,630 B** ·
views.js 851,543→851,613 = **+70 B** · sw.js **+0 B** (equal-length version
string). Line diffstat (`git diff --numstat`): yumi-brain 149/50 · views 2/2 ·
sw.js 1/1. `git diff --check` clean; CR counts consistent pre/post (no EOL flip).
components.css NOT touched. Foundations locked (9879ddb8 / 772886c0).

## STAGE 3 — VERIFY (two-layer, live proxy, synthetic fixtures — no account touched)

### Layer A — deterministic assertions (all PASS, re-verified vs the SHIPPED whole-word logic)
Collision `nameCollidesWith` (whole-word, FIX #2): exact · whole-word both
directions ("Power"↔"Power's Legibility", "Care"↔"Health Care", multiword
"Critical Theory & Pedagogy"↔"critical theory") · none · ≥3-char guard ·
**infix NON-collision** ("War"⊄"toward"/"reward", "Care"⊄"scared"/"careful").
**value_drops_lens** (value "Liberation" dropped + logged when lens "Liberation"
seeded) · **lens_drops_value** ("Power" dropped) · index-map · out-of-bounds <2
dropped · undefined-3rd-arg fallback (no crash/drop) · truncated→`parseArr`=null
(→ generateLenses rejects) · genuine-empty readable.

### Layer B — live proxy, MY exact new prompts + evals, 130-book fixture SEEDED with colliders
Seeded lenses `["Critical theory & pedagogy","Liberation","Power"]`; seeded value
slugs `["Liberation","Care","Solidarity"]`.

| call | stop_reason | out_tok | unreadable | survivors | **collision leaks** | dropped |
|------|-------------|---------|------------|-----------|---------------------|---------|
| **lens130** (was silent-nothing) | `end_turn` | 563 | false | **5** | **0** | 0 |
| **value130** | `end_turn` | 467 | false | **5** | **0** | 0 |
| lens3 control | `end_turn` | 228 | false | 3 | 0 | 0 |
| value3 control | `end_turn` | 173 | false | 3 | 0 | 0 |

- **LENS-1 fixed:** lens gen at 130 books returns 5 **index-grounded** lenses
  ("The Flesh as Political Territory", "Counter-Histories and the Buried
  Record"…), `end_turn`/563 tok — the title-string truncation→silent-nothing is
  gone. 3-book control unchanged (3 lenses).
- **VL-1 working:** with the sibling's names in-prompt, BOTH generators returned
  **ZERO cross-collisions** — value names (Abolition · Worldmaking ·
  Interdependence) are commitments; lens names are angles. The prompt persuaded
  (0 drops this run); the eval backstop ENFORCEMENT is proven by Layer A's
  `value_drops_lens`/`lens_drops_value`.
- **Register distinct:** no value echoed a seeded lens; no lens echoed a value.

### Error / accept / render
- **Error path:** `parseArr(truncated)===null` → `generateLenses` throws → both
  callers' existing reject → error state (silence impossible). Value path keeps
  RF1's `valueRetrofitUnreadable`.
- **Accept paths byte-unchanged:** `git diff js/views.js` = ONLY the 2 eval-call
  lines; the value accept handler (`views.js:18073-18082`) and the lens adopt path
  (`yumi-ui.js` lensSuggestAdopt) are byte-identical → no auto-mark, adopt intact.
- **No render/CSS touched:** `components.css` + `yumi-ui.js` untouched → mobile-390
  unaffected (the model's "cite 2 to 6" keeps the lens grounding line short; no
  lens display cap ships — FIX #1).

### Review gates + post-review fixes
Both reviewers **HOLD on Gate 3 (byte accounting)** only — I'd reported line-
diffstat labeled "Deltas" without units and slightly off; every CODE gate was
cleared by both (index alignment across the shared cap, generateLenses reject,
RF1 value-path preserved, covenant, ES3, parse, accept-paths). Corrected above
with measured byte deltas. fix-red-team ALSO raised two real code findings, both
now FIXED (a third was the byte nit):

- **FIX #1 — lens ADOPT truncation (was a real regression).** The
  `VALUE_GROUND_SHOW=6` cap I added to `evalLensResponse` truncated `lens.books`,
  and the yumi-ui adopt path (`lensSuggestAdopt`) assigns EVERY book in
  `lens.books` to the new theme → adopting a lens that gathered >6 books would
  link only 6. **Removed the cap from the lens eval** (restores the pre-fix
  behavior — the old title eval never capped); kept it in the VALUE eval where
  accept adds a stone and never uses `books`. Confirmed: exactly ONE cap-apply
  remains, in `evalValueResponse`.
- **FIX #2 — collision over-drop on incidental infixes.** `nameCollidesWith` went
  from raw substring-either-direction to **EXACT-or-WHOLE-WORD**
  (`\b<escaped-shorter>\b`). Re-verified (12 assertions): "War" no longer collides
  with "toward"/"reward", "Care" no longer with "scared"/"careful" — while "Power"
  still collides with "Power's Legibility", "Care" with "Health Care", multiword
  works, and both enforcement directions still fire.
- **FIX #2b — genre in the value collision set.** The VALUE eval now collides
  against **declared lenses only** (`themeLensNames` = userThemes, exposed by
  `gatherLensLibraryMetadata`; carried through `gatherValueMetadata`; retroRun
  passes `meta.themeLensNames`). The value PROMPT still gets the full `lensNames`
  (genres+themes). A value is never hard-dropped for merely sharing a word with a
  market genre. The LENS side (`valueNames`=profile.values) was already all-declared.

Re-verified after the fixes: parse exit 0 both files; ES3 clean; the 12 word-
boundary + enforcement assertions pass. **fix-red-team RE-DISPATCHED on the fixed
diff → all three findings RESOLVED, NO new code defect** (single value-eval cap
confirmed, whole-word regex `\b`/apostrophe semantics + no-ReDoS confirmed,
`themeLensNames` data-flow confirmed, no regressions). Its one residual — that
THIS report still described the pre-correction mechanism + wrong byte deltas — is
now cleared (all Stage 1/2/3 sections above refreshed to the shipped code; byte
deltas re-measured). The model output is unchanged by the fixes (all client-side)
so the live-proxy Layer-B results still hold. **praxis-reviewer's HOLD was
byte-accounting only** (now corrected); it cleared every code gate.

## STAGE 4 — HALT (Path B)

Novel, non-data-loss → FIX-PROTOCOL §5 Path B: HALT for Preston's exact
`commit and push`. sw.js bumped to v3.197 (LAST edit, byte-neutral); tree
commit-ready. NOTE: an earlier `commit and push` arrived BEFORE fix-red-team
surfaced FIX #1 (a real lens-adopt regression) — that authorization is treated as
STALE; this HALT is on the CORRECTED code and awaits a fresh word.

### Commit plan (on Preston's exact words)
- **Commit 1 (code, hook-clean):** `js/yumi-brain.js` · `js/views.js` · `sw.js`
  subject: `fix: VL-1 + LENS-1 — lens index-grounding + generator cross-awareness`
- **Commit 2 (docs):** `docs/studio/reports/vl1-lens1-2026-07-11.md` +
  `docs/studio/reports/r8-rf1-2026-07-11.md` (the ride-along status line).
- Then `git show --stat` self-check → push → wait Netlify (trigger can lag
  several min on this plan; poll the deploy) → confirm `CACHE_VERSION`
  `praxis-v3.197` live, cache-busted ×2.

### FINAL-PASS SUMMARY
```
FINAL-PASS — VL-1 + LENS-1 @ 3d0843f → CACHE_VERSION v3.197
FILES: js/yumi-brain.js · js/views.js · sw.js  (+ 2 docs)
PROOF 1 LENS-1 fix:   lens gen 130-book live → end_turn, 5 index-grounded (was truncate→silent)  PASS
PROOF 2 VL-1 both:    value130 + lens130 seeded w/ colliders → 0 cross-collisions each           PASS
PROOF 3 control:      value3 → 3 · lens3 → 3 (as shipped)                                          PASS
PROOF 4 backstop:     value_drops_lens + lens_drops_value (whole-word) enforce + log              PASS
PROOF 5 no-regress:   FIX#1 lens adopt uncapped · FIX#2 no infix over-drop · FIX#2b genre-safe    PASS
BYTES (measured LF): yumi-brain +6,630 · views +70 · sw.js +0
PARSE: harness self-check exit1 on ES5 · yumi-brain/views/sw.js each exit 0
FOUNDATIONS md5: lumen-amber 9879ddb8… ok · marks 772886c0… ok
ACCEPT PATHS byte-unchanged: value accept handler + yumi-ui lensSuggestAdopt (not in diff)
REVIEWERS: praxis-reviewer HOLD(byte-only)→cleared · fix-red-team findings→all fixed, re-check resolved
RESIDUALS: none open (genre-collision closed via themeLensNames; whole-word closes infix over-drop)
```

### WHAT PRESTON CHECKS (after push, real account)
- Account → Values → **"Ask Yumi to notice values"** → grounded suggestions in
  commitment-register names, **no echoes of your lens list**.
- Open **lens generation** (portrait "Suggest lenses" / the Lenses rail) → still
  returns lenses at your library scale, **no echoes of your declared values**, and
  adopting a lens links ALL its gathered books (not capped).
