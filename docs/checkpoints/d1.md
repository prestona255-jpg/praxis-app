# D1 — Light & Depth Law + Builder v2 + Gap Import (executor build report)

**Date:** 2026-07-08 · **HEAD at build:** `b7a24d8` · **Status:** all three stage gates PASS;
praxis-reviewer independent grade = **PASS / cleared to commit** (`docs/checkpoints/d1-review.md`).
UNCOMMITTED — awaiting Preston's exact commit words. Docs / mockup / tooling only; **0 live-app
files touched** (`js/`, `assets/`, `index.html`, `sw.js` all clean).

## Stage A — the Light & Depth law

| gate | evidence |
|---|---|
| sheet §8 + 8 recipes + 6 restraint laws | `## §8 · Light & Depth` present; recipe names 1–8 present; PERFORMANCE + ACCESSIBILITY + 4 short laws present; sheet bumped to v1.2 |
| `universal-depth.css` single source | header `Universal depth v1.2 — canonical source; copies must cite this file+version`; all named classes (`.u-gild-hairline .u-orb[--lit/--soft/--unlit + 10 hues] .u-inkgold .u-lamplight .u-btn-gild .u-ground-atmo .u-panel .u-sheet .u-thread .u-glyph-*`); braces 41/41 |
| canon §13 + prior headers preserved | 13 `<section>` (12 prior h2 byte-identical + "Light & depth"); links `../docs/studio/universal-depth.css` |
| galaxy depth pass, invariant held | `.galaxy-night{}` block byte-identical (not in diff); diff style-only (22+/9−, 0 JS lines); `:root` v1.1 values intact |

## Stage B — Builder v2

| gate | evidence |
|---|---|
| generator INLINES universal-depth.css | builder.html carries the depth version header + `<style id="universal-depth">`; reviewer byte-diffed the inlined block == the file (single source, no divergent copy) |
| opens on overview grid | default `location.hash||'#page-overview'`; overview page present |
| sidebar rail + 22 surface orbs | 22 `.brail-surf` entries each with a `.u-orb` |
| gilded progress bar | `.pfill` on overview + sequence (closed 0/22 · 7 shipped) |
| surface cards + empty-collapse | `cardify()` + `.bcard-none`; untouched surfaces confirmed to have empty sections (0 non-ws bytes) |
| runtime ES3 valid | extracted `(function(){…})();` → `cscript tools/parse-check` = PARSE OK |
| CLAUDE.md append-only + rule/guardrail | `git diff --numstat HEAD` = 47/0; re-plan rule + guardrail present |
| sequence.md Re-plan log | `## Re-plan log` seeded (2026-07-08 install→depth-law line) |

## Stage C — legacy gap import (146 findings)

Import rule: `fable-audit-combined.md` = PRIMARY (all 81); other docs contribute only findings
ABSENT from the combined (no triple-import). Independent 10-agent extraction cross-checked.

| source | imported | note |
|---|---|---|
| fable-audit-combined.md | 81 | §2 issues + §3 upgrades + §4 open-questions |
| praxis-2.0-phase2-ledger.md | 60 | net-new dispositions (post-wave gap/debt ledger) |
| fable-audit-charter.md §4 | 2 | friction notes absent from combined |
| pass3-scope.md | 1 | net-new |
| pass3-writing-loop.md | 2 | net-new |
| pass1 / pass2 / pass3-summary / -ia / -values | 0 | already deduped into the combined |
| **total** | **146** | 102 surface ledgers + 44 `cross-cutting.md` — arithmetic reconciles |

- Every line: `- [source: <doc> <date>] [status: unverified] [sev: <sev>] <finding>` (146/146 carry both tags).
- New `docs/studio/cross-cutting.md` (5-section format) holds the 44 infra/multi-surface findings; added to the Builder sidebar (a "layer" group) + overview (gap counts now render on tiles).
- `.claude/agents/studio-scan.md` amended: STEP-0 verify-first duty (flip `[status: unverified]`→`open, verified`/`resolved` before hunting new gaps).
- No pre-existing ledger line modified (ledgers were empty pre-import; non-ledger sections byte-identical to backup). `reader.md`/`walk.md` = 0 gaps (correct, not skipped).
- Fabrication spot-check (reviewer, 8 findings): 0 invented.

## Byte deltas (all touched files)

| file | base | after | Δ |
|---|---|---|---|
| docs/studio/praxis-universal-token-sheet.md | 10976 | 17345 | +6369 |
| docs/studio/universal-depth.css (new) | 0 | 9699 | +9699 |
| design/praxis-design-canon.html | 48749 | 58638 | +9889 |
| design/praxis-profile-galaxy-mockup.html | 54423 | 55968 | +1545 |
| tools/studio-build | 20659 | 28395 | +7736 |
| docs/studio/builder.html | 51852 | 113215 | +61363 |
| CLAUDE.md | 28339 | 29394 | +1055 |
| docs/studio/sequence.md | 7103 | 7751 | +648 |
| .claude/agents/studio-scan.md | 3770 | 4709 | +939 |
| docs/studio/cross-cutting.md (new) | 0 | 14133 | +14133 |
| 20 surface ledgers | — | — | +25754 |

## Residuals / notes
- Visual/runtime confirmation of the two mockups + the Builder is **Preston's felt pass** (VISUAL GATE);
  these are opened directly in a browser, not served by the app dev server, and node/python are blocked
  so no static server render was possible. Structural + parse + byte evidence stands.
- A single D1 commit also lands the prior **uncommitted studio-install scaffold** (builder.html, the 22
  surface md, tools/studio-build, studio-scan agent) — never committed since the install session.
- Origin is 3 behind local `main`; the commit ritual should end with `git push`.
