# 10.4 — slice checkpoints

## PRIVACY CHECKPOINT — resolved
Preston chose **A (exclude)** + an owner-facing draft indicator:
- PUBLISHED mode: omit any kind:'entry' evidence whose live entry isPrivate===true
  (or entry missing) from the evidence block; its inline citations fall back to plain
  italics. Reader-facing published output gets NO marker of any kind, not even a count.
- DRAFT mode: keep those rows but tag them muted "private -- excluded when published"
  so the author sees exactly what the published rendering will drop. Tag bytes are
  costed into SLICE 1's band.

## Build decisions (not Preston gates)
- mode is a parameter ('draft' | 'published'); 10.4 renders, never publishes
  (publishedAt is never set today).
- The read-only body renders the PUBLIC register (subTheory.bodyPublic) -- the
  public-facing register; the intellectual register is not part of the published
  artifact. (No roadmap directive; reasonable default, noted here.)
- Citation resolution in read-only picks the first candidate id (ids[0]); the 10.2
  session chooser/pins are an editing-surface concern, not part of the static render.

## SLICE 1 (views.js): renderSubTheoryReadOnly

STATUS: **PASS — comment-only deviation (band +2500..+6000 kept; logic in band).**

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse-check views.js | PASS | PASS | OK |
| byte delta | +2500..+6000 | +7,368; logic 5,765 IN band, +1,368 overage 100% comment | OK (classification) |
| helper defined once | 1 | renderSubTheoryReadOnly 1 def, 0 calls (SLICE 2 mounts) | OK |

Classification (clean isolation -- 10.2 committed): LOGIC 170 lines/5,765 b (in band);
COMMENT 23 lines/1,603 b; TOTAL 7,368 reconciles to file delta. Overage is comment-only.

### Behavioral harness (DOM-mock; parseCitations + renderSubTheoryReadOnly)
```
PUBLISHED: evidence items 3 (private entry excluded) | private tags 0 | superscripts 2
DRAFT:     evidence items 4 (incl private)           | private tags 1 | superscripts 0
EMPTY:     evidence items 0 (no block)
```
Privacy A confirmed: published excludes the private journal entry with no marker;
draft keeps it tagged "private -- excluded when published". Numbering is first-appearance;
draft uses dots (no numbers); empty evidence suppresses the block.

## SLICE 2 reframed (Preston) + band restated

Mount = in-place "Preview" toggle on the writing surface, rendering PUBLISHED mode only
(private excluded, superscripts) -- "what will readers see". The owner indicator moves
OUT of renderSubTheoryReadOnly and ONTO the editing-rail evidence rows (buildAttachedRow),
where evidence is managed -- visible at attach time, not behind a preview click. Draft
rendering stays covered by the existing 10.2 preview panes (dots).

SLICE 2 (views.js) scope:
- remove the now-relocated draft-mode private tag block from renderSubTheoryReadOnly;
- add a Preview toggle to renderSubTheoryPage that swaps the editor for
  renderSubTheoryReadOnly(subTheory, 'published') in a host, and back;
- add the "private -- excluded when published" tag to buildAttachedRow rows whose
  kind:'entry' evidence has a private or missing live entry.
RESTATED BAND (SLICE 2 had no byte band in the plan; the rail tag is re-costed here):
**+1,200..+3,000**, views.js only. Gates: parse PASS; renderSubTheoryReadOnly gains
exactly 1 call site (the toggle); the rail-tag guard appears once. CSS deferred to SLICE 3.

## SLICE 2 (views.js): Preview toggle + rail tag — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse-check views.js | PASS | PASS | OK |
| byte delta | +1200..+3000 (restated) | +1,888 (327,187 -> 329,075) | OK |
| renderSubTheoryReadOnly call site | 1 | 1 (in setPreview) | OK |
| rail-tag guard | 1 | subtheory-attached-private-tag x1 | OK |
| draft-tag removed from renderer | 0 | subtheory-readonly-private-tag = 0 | OK |

Preview toggle swaps editor (header/regToggle/bodies/2.2-panes) for
renderSubTheoryReadOnly(sub,'published') in subtheory-preview-host; exits via
showRegister(activeRegisterPublic). Rail tag relocated to buildAttachedRow rows whose
kind:'entry' has a private/missing live entry. CSS deferred to SLICE 3.

## SLICE 3 (components.css): read-only render + rail tag styles — PASS
- Plan gate (verbatim): "Vars only, zero hex, brace balance." NO byte band stated for
  SLICE 3, so +2,265 is informational/unbanded -- not measured against a band, no
  classification needed.
- brace balance 664 == 664; zero raw hex in added lines; byte delta +2,265 (96/0).
- Styles: preview toggle, readonly header/body, superscript .subtheory-cite-num,
  numbered evidence block + cite-line/quote/annotation, see-also, and the muted
  .subtheory-attached-private-tag. var() tokens only.

## SLICE 4 (sw.js): CACHE_VERSION -> v3.95 — PASS
praxis-v3.94 -> praxis-v3.95 (token 3.94 1->0, 3.95 0->1). Diff = line 10 only.

## 10.4 summary (pre-commit)

| File | Diff | Note |
|---|---|---|
| js/views.js | 237/0 | renderSubTheoryReadOnly + Preview toggle + rail private tag |
| assets/components.css | 96/0 | read-only render + superscript + rail-tag styles |
| sw.js | 1/1 | CACHE_VERSION -> praxis-v3.95 |

Privacy A enforced: published render excludes private/missing-entry evidence (no marker,
no count, inline cites fall back to plain italics); the owner sees a muted
"private -- excluded when published" tag on the editing-rail evidence row instead.
Preview toggle renders published mode; draft rendering stays in the 10.2 panes.
Slice 1 was a comment-only deviation (logic in band); Slice 2 in its restated band
(+1,200..+3,000); Slices 3 and 4 carry no plan byte band (gates: vars/hex/brace, and
version-bump respectively) -- both pass their stated gates.

AWAITING: Preston's "commit and push", then LIVE PASS-CHECKS: a sub-theory with 3
evidence items, 2 cited -> Preview shows superscripts in appearance order, block lists
cited-then-uncited; zero evidence -> no block; a private journal entry attached as
evidence -> rail row shows the tag AND the Preview/published render omits it; see-also
lists linked sub-theories. Screenshots: published render + evidence block + a tagged
rail row.

## LIVE VERIFICATION (agent-run on praxis-reading.netlify.app, v3.95)

Run in a SEPARATE test account, confirmed NOT Preston's: getCurrentUser email
`prestonpraxistest@gmail.com` (name "Charles" -> "C" avatar); Preston's is
prestona255@gmail.com. Account started empty (0 arcs / 0 sub-theories). Fixtures
seeded via the app's own state functions (createArc/createSubTheory/addEvidence/
updateSubTheory/linkSubTheories) in the test account only. Verification done by
(a) calling the LIVE deployed renderSubTheoryReadOnly + reading the live DOM, and
(b) UI screenshots via the Preview toggle.

Screenshots: captured live and visible in the session transcript (ss_* ids below).
The browser MCP saved them to the session, not to a repo-committable path, so they
are described here with their ids rather than embedded as files.

### Check 1 — T1 "Desire as pedagogy" (3 evidence, 2 cited) — PASS
DOM (published): superscripts=2; evidence block=3 items, citeLines in order
["Epstein, Range", "A Pamphlet — Anon", "Uncited Source — Nobody"] (cited 1,2 then
uncited 3); see-also=["A linked partner theory"]. Body: "I lean on Range^1 and then
the A Pamphlet^2 argument; a third source stays uncited here."
Screenshot ss_4057nchry: Preview shows Range^1 / A Pamphlet^2 superscripts, the numbered
block (1 Epstein,Range / 2 A Pamphlet / 3 Uncited Source) with quotes+annotation, and
the "A linked partner theory" see-also line.

### Check 2 — T2 "Empty theory (no evidence)" — PASS
DOM (published): hasEvidenceBlock=false; bodyEmpty=true; 0 superscripts; 0 see-also.
Screenshot ss_1935adwc4: Preview shows just the header, no evidence block; rail reads
"No evidence attached yet."

### Check 3 — T3 "Privacy test theory" (private journal entry attached) — PASS
Seeded: 1 book (Their Eyes Were Watching God — Hurston) + 1 PRIVATE journal entry
(isPrivate=true) attached as kind:'entry' evidence.
- Editing rail (DOM): the journal row carries `.subtheory-attached-private-tag` =
  "private — excluded when published"; the book row does not.
  Screenshot ss_1277wiq9n: rail shows the book row, then the journal row tagged
  "PRIVATE — EXCLUDED WHEN PUBLISHED" with its quote.
- Preview/published (DOM): evidence block = 1 item ["Hurston, Their Eyes Were Watching
  God"] only; the private journal entry is ABSENT; readonlyPrivateTags=0 (no marker/count).
  Screenshot ss_1528hgbn6: Preview evidence block shows only "1. Hurston, Their Eyes
  Were Watching God"; the rail (right) still shows the journal row with the tag — the
  rail-tag AND the published-absent state in one view.

### See-also — PASS (covered by Check 1: T1 links T4 "A linked partner theory").

RESULT: 4/4 checks PASS against the live deployed bundle. Fixtures remain in the test
account (harmless; not Preston's data). Privacy decision A verified end-to-end: private
journal content is tagged for the owner in the rail and excluded from the published
render with no marker.
