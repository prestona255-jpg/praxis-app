# 10.5 — slice checkpoints

## SLICE 7 SCOPE CONFIRMATION (Preston, at Slice-0 go)
Persisted citationPins ALSO route into renderSubTheoryReadOnly's citation
resolution: the published render respects the author's pin; first-candidate
remains the unpinned fallback. Views-side band restated at recon to +200..+1,200
to cover both read-through sites (editor closure + read-only resolver).

## SLICE 0 (Praxis_Roadmap.html): s10-5 roadmap entry — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| byte delta | +800..+2,500 | +1,947 (438,241 -> 440,188) | ✅ |
| `id: 's10-5'` count | 1 | 1 | ✅ |
| `name: 'Citation UX'` count | 1 | 1 | ✅ |
| diffstat | localized, no EOL flip | 22/0, single block insert | ✅ |
| s10 substage count | 5 (s10-1..s10-5) | 5 | ✅ |
| block parses as valid JS | yes | cscript standalone parse: BLOCK VALID, scope=7, nonGoals=4 | ✅ |

Inserted after s10-4 inside s10.substages: id/num/name, depConfirm (10.4 Shipped
+ the three live substrates), purpose (discoverability/friction gaps), 7 scope
bullets mirroring plan slices 10.5.1-10.5.7 (incl. the confirmed read-only pin
routing), 4 nonGoals. Data-string edit only; no engine/structural change.

## SLICE 10.5.1 (views.js): attach-disclosure rename/flatten — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse views.js | PASS | PASS | ✅ |
| byte delta | +100..+600 | +261 (329,075 -> 329,336) | ✅ |
| new label code literal | 1 | 1 (`'+ Attach evidence'`, +1 comment mention) | ✅ |
| old label remaining | 0 | 0 | ✅ |
| external-first order | yes | externalSection appended before sourceSection (views.js:5062-5063) | ✅ |

Disclosure button label -> "+ Attach evidence" via a single `attachLabel` var (used
at the button and the reset on close). externalSection now mounts FIRST in pickerWrap
so the external-source affordance reads at the same disclosure level as the book list.
No data change.

## SLICE 10.5.2 (views.js): Cite button — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse views.js | PASS | PASS | ✅ |
| byte delta | +1,200..+3,000 | +2,140 (329,336 -> 331,476) | ✅ |
| insertCitationAtCursor def | 1 | 1 (+1 call from citeBtn, +1 comment ref) | ✅ |
| subtheory-attached-cite | 1 | 1 | ✅ |

Adds last-focused-register tracking (focus listeners on both bodies) +
insertCitationAtCursor(text) (caret insert into the last-focused body; fallback =
append to the visible register; focuses + sets selection; repaints previews; blur-save
contract untouched). Each rail row with a citable title (citationMatchTitle != '')
gets a "Cite" action inserting *Title*.

## SLICE 10.5.3 (views.js): autocomplete — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse views.js | PASS | PASS | ✅ |
| byte delta | +1,500..+3,500 | +2,982 (331,476 -> 334,458) | ✅ |
| openAutocomplete def | 1 | 1 | ✅ |
| maybeAutocomplete wirings | 2 | 2 (one per register input) | ✅ |
| positionToSpan reuse (no dup) | shared | 1 def / 3 calls (hover, chooser, autocomplete) | ✅ |

Typing '*' in a register opens a picker of attached titles (dedup by lowercased title);
selecting writes '<Title>*' at the caret to complete the pair. Mounts/dismisses like the
chooser (outside-click + Esc), positions via the shared positionToSpan. No open when
nothing is attached. Blur-save untouched.

## SLICE 10.5.4 (views.js + components.css): coach line — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse views.js | PASS | PASS | ✅ |
| views byte delta | +300..+1,000 | +450 (334,458 -> 334,908) | ✅ |
| css byte delta | +200..+800 | +337 (161,917 -> 162,254) | ✅ |
| css brace balance | equal | 665 == 665 | ✅ |
| zero raw hex in added css | 0 | 0 | ✅ |

renderCitationPreview tracks hasCitation; when a pane resolves no citation it appends a
muted .subtheory-cite-coach ("Wrap a source title in *asterisks* to cite it."), removed
the moment a citation exists. CSS: mono, --ink-4, small.

## SLICE 10.5.5 (views.js): tap support — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse views.js | PASS | PASS | ✅ |
| byte delta | +300..+1,000 | +558 (334,908 -> 335,466) | ✅ |
| citeLines extracted (no dup) | 1 def / 2 calls | 1 def, 2 calls (hover + tap) | ✅ |
| citeTapTimer wired | decl + use | decl + clear + set | ✅ |

bindCiteHover now also binds 'click' (a tap on touch): shows the same .arc-tooltip card
via the extracted citeLines() helper, auto-hidden after 3.2s; desktop hover unchanged;
the ambiguous long-press chooser unaffected.

## SLICE 10.5.6 — Write/Preview toggle (Option C) — PASS

DESIGN GATE RESOLVED (Preston, on full-width live mocks captured in the test/real
account, read-only): **Option C, per-register, Write|Preview right of the register pill,
Published relabeled, stacked live-preview pane removed.** Field treatment DEFERRED (below).

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse views.js | PASS | PASS | ✅ |
| views byte delta | +800..+2,200 (set at gate) | +2,092 (335,466 -> 337,558) | ✅ |
| css byte delta | +150..+700 (set at gate) | +628 (162,254 -> 162,882) | ✅ |
| css brace balance | equal | 668 == 668 | ✅ |
| zero raw hex added css | 0 | 0 | ✅ |
| toggle classes | 1 each | subtheory-wp-toggle 1, subtheory-toggles-row 1 | ✅ |
| Published relabel | present | 'Published' x2 (init + exit) | ✅ |

A per-register Write|Preview pill (wpToggle) sits in a flex .subtheory-toggles-row beside
the register pill, with the Published toggle. applyEditorView() is the single source of
truth: textareas show only in Write, the matching preview pane shows only in Preview (the
always-stacked pane is gone), tab actives reflect both axes. The 10.4 published toggle
relabeled Preview->Published / Write->Edit; setPreview hides the row's draft controls and
restores via applyEditorView. Borderless surface retained (no field treatment this slice).

### DEFERRED -> SLICE 10.5.8 (writing-surface field treatment, audit bug #5)
Preston: spin the raw-textarea field treatment (border/surface tint, padding, prose
max-width) into its own slice, scoped APP-WIDE (every writing surface, not just this
register). Carries its own design comp-gate (before/after at desktop + mobile, both
registers, every textarea surface) and notes the 9.2/6.2 borderless-spec override THERE,
not here. To be authored into the plan/roadmap when 10.5.8 is taken up.

## SLICE 10.5.7 (state.js + views.js): persist pins + read-only routing — PASS

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse state.js | PASS | PASS | ✅ |
| parse views.js | PASS | PASS | ✅ |
| state byte delta | +800..+2,000 | +1,153 (75,019 -> 76,172) | ✅ |
| views byte delta | +200..+1,200 | +914 (337,558 -> 338,472) | ✅ |
| 1.17.0 terminal | 1 | 1 | ✅ |
| 1.9.3 literal anchor | untouched | untouched | ✅ |
| migration idempotent | yes | harness: existing pins preserved, missing backfilled, run2 no-op | ✅ |
| citeIdIn shared (no dup) | 1 def / 2 calls | 1 def, 2 calls (editor + read-only); old idIn removed | ✅ |

state.js: ensureSubTheoryFields backfills citationPins={}; createSubTheory seeds it;
migrate 1.16.0 -> 1.17.0 backfills all (idempotent). views.js: the editor's citePins now
REFERENCES the record's citationPins (read-through); the chooser persists a pick via
markSubTheoriesDirty + saveState; renderSubTheoryReadOnly resolves an ambiguous phrase
through the persisted pin (first candidate is the unpinned fallback) using the shared
top-level citeIdIn. Firestore merge copies whole sub-theory records, so citationPins
syncs without a merge change (nonGoal honored).

### Migration harness
```
after run1: schema=1.17.0
  a pins preserved? true
  b pins backfilled object? true
after run2: schema=1.17.0
  idempotent (b pins same ref, a unchanged)? true
ensureSubTheoryFields adds citationPins? true changed=true
```

## SLICE final (sw.js): CACHE_VERSION -> v3.96 — PASS
praxis-v3.95 -> praxis-v3.96 (3.95 -> 0, 3.96 -> 1). Diff = line 10 only.

## 10.5 summary (pre-commit)
| File | Diff | Note |
|---|---|---|
| docs/.../Praxis_Roadmap.html | 22/0 | s10-5 roadmap entry (Slice 0) |
| js/views.js | 251/42 | rename/flatten, Cite, autocomplete, coach, tap, Write/Preview toggle, pin read-through + read-only routing |
| js/state.js | 26/0 | citationPins field + 1.16.0->1.17.0 migration |
| assets/components.css | 32/0 | coach line + toggles row |
| sw.js | 1/1 | CACHE_VERSION -> praxis-v3.96 |

All parse checks PASS; every slice in band (Slices 0-7 + final); no recorded deviations.
10.5.8 (writing-surface field treatment, app-wide, audit bug #5) DEFERRED with its own
comp-gate. AWAITING Preston's "commit and push", then executor-run live checks in the
test session per the default protocol.
