# R8 — Values · fix-red-team (persisted; agent is read-only)

Verdict: **NOT clean — 1 blocks-commit, 2 residuals.** Scope confirmed exactly the six files; foundations + sw.js/test-arc untouched; parse gate green (self-validating); ES3 clean; migration gated/additive/idempotent; valueMarks rides the object's whole-record doc via the same markXDirty+saveState path as movedMe (no 6th collection); retrofit covenant (title/author/genre only) + fail-safe-to-[] eval hold; profile.values symmetric on both merge paths.

## FINDING 1 — BLOCKER (FIXED) — onboarding beat wipes prior profile.values on retake
`resetPicked()` (intros.js) set `picked.values = []` without seeding from `getProfile(uid).values`; `buildValues` renders chips against `picked.values` only (prior stones show UNSELECTED); each toggle calls `doValues(picked.values)` → `setProfile({values: …})` which **replaces**. Reachable retake paths: About "Retake the walk" (views.js:19336-19338), the release beat retake (intros.js), the yumi-ui startJourney entry (yumi-ui.js:848-850). A returning user with `[Liberation, Care, Doubt]` who retakes and toggles one value → `profile.values` becomes `[that one]`; the rest are wiped, cloud-synced, silently. New silent-loss path (pre-R8 the journey had no persisting beat). S6's clean test only passed because the stub started empty.
**FIX:** seed `picked.values` from `getProfile(uid).values` (a COPY) in `resetPicked()`, so the beat renders prior stones selected and edits the full set (matches the Account card + retrofit union idiom, which never wipe). Fix commit + re-verify below.

## FINDING 2 — RESIDUAL (R8-VR-BIND) — value-mark handlers bind the record at render-time
`buildValueMarkRegister` captures `rec`/`marks = rec.valueMarks` in `render()`; the add/remove/edit handlers mutate that captured array. `movedMe` is more robust — its handler re-reads `state.books[bookId]` fresh. If a Firestore REPLACE-merge swaps the record object between render and click, the click mutates a detached array and persist() saves the fresh (mark-less) record. Window is narrow: the merge calls `renderRoute()` immediately (integrations.js:224/281/331), rebuilding+rebinding the register. Documented residual, not a blocker. (Hardening = re-read the live record in the mutation handlers.)

## FINDING 3 — RESIDUAL (R8-VR-RACE, inherited/accepted) — marks on an existing synced record inherit the movedMe merge race
A valueMark added to an existing book/arc/sub-theory during the sub-second sign-in load window can be clobbered when the found-branch REPLACE overwrites the local record, then ensure*FieldsAll + saveState persist the mark-less record. Identical to movedMe/categoryOverride/tradition exposure — exactly the "rides movedMe" behavior the round accepted (the F-DL latches guard the deferred write + pending add/delete, not a field-edit on an existing synced record). Not new to R8; recorded so the round doesn't believe marks are fully race-safe.
