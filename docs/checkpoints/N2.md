# Notebook epic — N2 checkpoint (capture + visibility controls)

Self-driving Tier-1 stage. Local commit on PASS; no push.

## Scope delivered
- **Inline writeline capture** (left leaf): text input + `Marg / Jrnl / Ques` register chips
  (mic DEFERRED to N2b; camera out of scope). Enter commits. By-kind `isPrivate` at creation
  (journal private; marginalia + question visible) and `filed` routing:
  journal → placed (Journal); marg/ques on a book tab → `filed:true` + that bookId;
  marg/ques elsewhere → `filed:false` (Inbox). "Follow the note" sets the active tab to where it landed.
- **Master "Yumi reads along" switch → interactive**: toggles `profile.yumiReadsAlong`, persists via
  `setProfile` + the `/userProfiles` mirror (`saveProfileToFirestore`), re-renders. **Gates
  `assembleContextData`**: OFF → `recentEntries` + `recentArtifacts` emptied (Yumi sees no writing);
  ON → the existing `isPrivate` filter applies. Navigation context + conversation memory unaffected.
- **File Inbox → book**: a "File to book" action on Inbox entries (`filed===false`) opens a book
  picker (mirrors the arc picker) → `fileEntryToBook` sets `filed:true` + adds the bookId.
- **Stage-11 disclosure (honest update)**: copy now names questions (visible by default, like
  marginalia) and the "Yumi reads along" switch, and **drops the now-false per-entry "you decide
  each one"** framing (the toggle was removed in N1). `getAggregateCounts` gains `question` buckets;
  the figures line breaks out questions and includes them in the totals.

## Slice table — actual (numstat), component-accounted

| File | + / − | Accounting |
|---|---|---|
| `js/yumi-brain.js` | +17 / −1 | consent gate in `assembleContextData` (~12) + `question` buckets in `getAggregateCounts` (~6) |
| `js/views.js` | +257 / −14 | 7 new fns (`buildNotebookWriteline`, `appendWritelineChip`, `captureNote`, `fileEntryToBook`, `buildBookPickerPanel`, `appendBookPickerRow`, `openFileToBookPicker`) + master-switch interactivity + File-to-book action + Stage-11 copy/figures |
| `assets/components.css` | +45 / −0 | writeline + register-chip styles + master-switch `cursor` |

All added lines are planned + behavior-verified; no stray logic.

## Mechanical gates
- **cscript parse `views.js`:** `PARSE: PASS (406179 chars)`. (`yumi-brain.js` parse-exempt.)
- **ES3 (added lines, both files):** no `=> / backtick / .catch / .finally / const / let`.
- **F5:** no `isPrivate` assignment added (grep) — the gate empties the cross-to-Yumi arrays, never
  touches stored visibility.
- **Clean tree:** only `views.js`, `yumi-brain.js`, `components.css`. No EOL flip. No NON-GOAL touched
  (no proxy / generative Yumi call — the switch + capture are pure client logic).

## Live state + DOM proof (local server :8753, seeded user, no Firebase)
- **Capture shapes** (by-kind `isPrivate` + `filed` routing), all confirmed:
  journal/inbox → `{journal, isPrivate:true, filed:true, books:''}`;
  marg/inbox → `{marginalia, isPrivate:false, filed:false, books:''}`;
  ques/book → `{question, isPrivate:false, filed:true, books:'bk1'}`;
  marg/book → `{marginalia, isPrivate:false, filed:true, books:'bk1'}`; active tab follows to `bk1`.
- **Consent gate** via `getContextSnapshot`: ON → 3 visible entries (private journal excluded);
  **OFF → 0 entries + 0 artifacts**; back ON → 3 (proves `setProfile` persisted). currentBook context
  retained when off.
- **File Inbox→book**: the inbox marginalia became `filed:true, books:'bk1'`.
- **Counts**: `notebookPrivate:1` (journal), `marginaliaVisible:2`, `questionVisible:1` — question
  bucket correct, totals honest.
- **Interactive DOM**: writeline renders (placeholder + `Marg`(default on book tab)`/Jrnl/Ques`
  chips); master-switch **click flips** state (true→false); File-to-book link present on Inbox entries;
  **0 console errors** (the Firestore mirror fails gracefully offline).

## Residuals
- Mic (voice) capture = N2b (deferred); camera deferred — neither built or touched.
- Real-account Firestore round-trip (switch persists across reload; captured note survives reload)
  is a final-gate live check.

## Result: PASS — committed locally. Auto-proceeding to N3 (gather → sub-theory).
