# Notebook epic — N1 checkpoint (the spread shell, read-only)

Self-driving Tier-1 stage. Local commit on PASS; no push. Builds the Alt-1 spread
(left leaf = notes by tab; right leaf = empty working page) over the locked model.

## Scope delivered
- Header: `Structurally private` eyebrow + `Notebook` title (kept); right side rebuilt to the
  **display-only master "Yumi reads along" switch** + the existing **"What Yumi sees"** link
  (reused → `openTransparencyView`). The old `+New entry` / `+New arc` / `Settings` buttons are
  superseded by the spread.
- **Tab row** `Inbox / Journal / <books>` with **live counts**, F3 routing + click-to-switch.
- **Two-leaf spread**: left leaf = section header + tab's entries (newest-first) via
  `renderNotebookEntry`; right leaf = empty "Working page" (N3 fills it).
- **Per-entry privacy toggle REMOVED** (`togglePrivacy` call site + function deleted); the
  read-only visibility indicator is kept. Visibility is now by-kind default + master switch.
- **`question` register** rendering branch added (pill "Question", `--question-color` spine).
- No capture / gather / writes (read-only shell).

## Slice table — predicted vs actual (numstat)

| File | Predicted | Actual (+ / −) | Classification |
|---|---|---|---|
| `assets/theme.css` | +1 | +1 / −0 | exact (`--question-color: var(--river)`) |
| `assets/components.css` | ~+116 | +150 / −4 | structural spread CSS (24 selectors) + 4 dead privacy-toggle selectors removed; overage = CSS verbosity |
| `js/views.js` | ~+49 net | +249 / −140 (net +109) | component-accounted below; no stray logic |

**views.js net +109 reconciliation** (my +49 estimate undershot the helper volume; every added
line is planned + verified): 7 spread helpers (`notebookEntryMatchesTab`, `buildNotebookTabModel`,
`buildNotebookTabRow`, `appendNotebookTab`, `buildNotebookLeftLeaf`, `buildNotebookRightLeaf`,
`buildNotebookMasterSwitch`) ≈ +180; body restructure (flat stream → tab/spread) ≈ −55 net;
`togglePrivacy` removal −13; `renderNotebookEntry` toggle −9 / question-branch +4. No unplanned logic.

## Mechanical gates
- **cscript parse `views.js`:** `PARSE: PASS (397253 chars)`.
- **`togglePrivacy` fully removed:** grep → the only remaining occurrence is the removal-note
  comment; **no code, no export** (the stale `window.views` export was caught + removed — it would
  have thrown ReferenceError at module load; parse can't catch it, the grep + live load did).
- **ES3 (added views.js lines):** no `=> / backtick / .catch / .finally / const / let`.
- **Dead refs cleaned:** removed `.notebook-entry-privacy-toggle` from 3 CSS selector groups +
  the dead `togglePrivacy:` export + a stale doc-comment reference.
- **Clean tree:** only `views.js`, `components.css`, `theme.css` dirty. No EOL flip (389 changed
  lines in a ~9500-line file). No NON-GOAL touched.

## Computed + live-DOM proof
- **Routing/counts harness `.claude/n1-routing-test.js` (cscript): ALL PASS (18 assertions)** —
  F3 routing exact for journal/inbox/book across all three registers; tab model counts + order
  (Inbox, Journal, shelf-order books, orphan-book appended) correct.
- **Live DOM (local PowerShell server :8753, seeded signed-in user — no Firebase, per the local model):**
  - Spread renders: eyebrow "Structurally private"; tabs `Inbox 2 · Journal 1 · Pedagogy of the
    Oppressed 2 · Zami 1` (exact counts, correct order, Inbox active); two-leaf spread + "Working page".
  - Inbox leaf shows the Question + Marginalia entries (new "Question" pill confirmed); newest-first.
  - **`privacyTogglesInDom = 0`**; visibility indicators present (2).
  - **Tab click** Inbox→Pedagogy switches the filter → that book's 2 marginalia, including the
    **deliberately-private `m2`** rendered with its read-only "Private" indicator (no toggle).
  - Master switch reflects `yumiReadsAlong`: **on** when true, **off** when false.
  - **window.views constructed without error** (export-crash fix verified live); **0 console errors**
    across inbox render, tab switch, master-off, and reset.
  - Screenshot timed out (local SW/preview instability) — corroborating only; visual capture deferred
    to the acceptance gate on the deployed app. Live-DOM structural readouts are the hard PASS evidence.

## Residuals (noted, not blocking)
- `openJournalEditor` / `openArcEditor` / `openNotebookSettings` / `setRegisterDefault` are now
  unreferenced from the notebook header (functions remain defined + valid). The N2 inline writeline
  replaces capture; the master switch replaces register-default Settings. Dead-code cleanup deferred to N5.
- Real-account notes render (vs seeded) is a final-gate live check (no local Firebase session).

## Result: PASS — committed locally. Auto-proceeding to N2 (capture + visibility controls).
