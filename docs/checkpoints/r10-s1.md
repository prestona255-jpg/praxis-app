# R10 S1 "GROUNDS" — BUILD checkpoint (S1-1 … S1-4)

**Model:** Opus 4.8 · HEAD at start `d3352ec` · target CACHE_VERSION `praxis-v3.276` (bump at S1-4).
Ruling recorded in `r10-s1-recon.md`: additive-doors (S1 = `.pf-vcard` only), normalizer MERGES BY
NAME, workshop `.stb-conn` OUT OF JURISDICTION (linkedSubTheories = CN-6 future), stones VOID.
Rig: local static server `http://localhost:8791` serving the working tree; SW+caches cleared per L5.

---

## S1-1 DATA — the value-object shim — **PASS**

**Edits (11 sites, 4 files):**
- `state.js` (+5819 B / +120 −4 lines): `valueSlug` · `valueIdFor` · `normalizeValue` · `valueNamesFromArr` · `valueNamesFor` (new helpers); **`getProfile` read-shim** (lazy string→object, in place, idempotent); **`setProfile` write-normalizer MERGE-BY-NAME** (kills the `('' + entry).trim()` flattener + the integrations.js:565 sign-in corruption path); **`setValueStatement`** (per-value statement writer + history archive on revision). Delta above the low proposal floor (+0.4–0.8 KB) — driven by the binding NORMALIZER LAW + the statement writer + 6 helpers + provenance comments; all necessary, none bloat.
- `views.js` (+929 B / +16 −5): `_profileValueLoad` 19163 dict-key → key by `.name` (the L3 silent-collapse catch) + output carries `.id`/`.name`; `buildValueMarkRegister` vocab → names; `_pfRenderOffers` dedup → `.name`.
- `intros.js` (+431 B / +5 −1): `resetPicked` seed → `.name` (the data-loss-on-retake path).
- `yumi-brain.js` (+144 B / +3 −3): 3 source reads → `valueNamesFromArr` (the 2 `.join` sites fixed transitively).

**Mechanical gates:**
- **PARSE:** `cscript` harness — state.js / views.js / intros.js / yumi-brain.js all **PARSE OK (exit 0)**.
- **EOL:** all HEAD blobs LF (CR=0) → commit is LF regardless of worktree.
- **GREP:** old flatten `('' + fields.values[vi]).trim()` = **0** (gone); 6 helpers present (1 each); `MERGE BY NAME` present; `declaredSet[declared[i].name]` = 1; loader output `id/name` = 1; yumi-brain `valueNamesFromArr` = 3.

**CONSOLE SELF-TEST (real edited bundle, throwaway uid, cleaned up — prestona255 untouched):**
- **T1 (19163 dict-key, L3 class):** legacy STRING `'Care and love'` → read-shim wrapped `{id:'care-and-love', name:'Care and love', …}`; `_profileValueLoad` attributed the owned arc's mark why (`whys:["a test lineage line"]`), `orphans:[]`. **No `[object Object]` collapse. PASS** (asserted on output, not absence of exceptions).
- **T2 (merge-by-name):** wrote a statement, then onboarding-retake via a **STRING** `['Care and love']` → statement **survived** (`"My first articulation of care."`, id stable). **PASS.**
- **T2b (revision):** history 0→1, prior archived, `statement` updated, `statementRevisedAt>0`; first write left history empty (→ page shows no "revised" line). **PASS.**
- **T3 (removal path — characterized, NOT fixed):** removing `'Care and love'` via the values beat DROPS the object + its statement/history = **silent loss → NAMED RISK for the ONBOARDING round** (owns first-run).
- **Reader-path shapes:** `valueNamesFor`/`valueNamesFromArr` on a mixed string+object array → names; `getProfile` wraps all to objects; `valueIdFor('Care and love')='care-and-love'`; no `[object Object]` anywhere. **PASS.**

**Residual (named risk, on record):** T3 removal-path data loss → onboarding round. Schema-number doc corrections (live terminal **1.30.0**) → round-close docs task.

---

## S1-2 PAGE — route + renderValuePage — **PASS**

**Edits:** `views.js` (+7,646 B this slice): `#value/<id>` route (F2 note pattern; clears currentX; renders bright), `backOk += value`, `_valueResolve`, `renderValuePage` (header orb+name+provenance · BOTH statement states · inline plain-textarea composer · history read-only view · edit flow). `components.css` (+4,151 B): `.value-page` daylight skin (mirrors `.note-surface` tokens: `--ink`/`--page-2`/`--line-page`/`--gold-deep`) + composer + history + mobile.
- **PARSE:** views.js exit 0. **CSS:** renders clean (no broken block). Route absent-check held (added, not colliding). Byte deltas above the low floors but ~2× (views/css) — a full page render + full skin; justified, not bloat.

**RIG VERIFY (real edited bundle @ localhost:8792, L18 every-state × L19 hit-tested):**
- **Route/ground:** `#value/care-and-love` → `data-ground="bright"` (daylight); name `--font-serif` `rgb(36,23,16)` dark-ink; no S2/S3 leakage (`noGathering:true`).
- **UNWRITTEN:** provenance `"A ground in your library · declared 8/9/2026"` (declaredAt stamped); invitation `"This word is an invitation. Make it yours."` (declarative); write button **44px**, hit-tested + clicked.
- **WRITE:** composer textarea **17px** (P7); Save/Cancel present; Save (44px, hit-tested) → WRITTEN; **no "revised" line on first write** (correct); statement persisted.
- **REVISION:** edit (hit-tested) → composer prefilled; Save → WRITTEN + **"revised 8/9/2026"** + history btn; prior archived to history (`statementHistory.len 0→1`).
- **HISTORY:** history btn (hit-tested) → panel shows the archived prior version.
- **Mobile 390:** all states; edit/history controls **44px + hittable** (fixed from 27px, mobile `@media`); **no h-overflow** (`docScrollW==390`).
- **Desktop 1360:** `.value-page` `max-width:680px` centered (320/320); name 38px / statement 23px; **no h-overflow**.
- Screenshot skipped — Browser pane not displayed; hard evidence is live-DOM/computed-style (per protocol); visual felt pass = Preston's on live.

**Rig gotchas recorded:** the v1 static server hung on a HEAD probe (single-threaded) → v2 server injects a per-load `?v=<nonce>` into index.html script/style refs (defeats the mcp browser's aggressive JS cache — plain `navigate` served stale views.js twice) + guards HEAD. Auth stub is clobbered on a full reload (`getCurrentUser = ls('praxis_user')`; boot resets it to null when Firebase resolves no user) → re-seed `praxis_user` + profile post-boot. `location.hash=` renders async (hashchange) — probe in a separate call.

---

## S1-3 DOORS — `.pf-vcard` only — **PASS**

**Edits:** `views.js` (+682 B): `_pfValueCard` div gains `data-value-id` + `role="link"` + `tabindex=0` + aria-label; `handle()` gains a `[data-value-id]` route (`#value/<id>`), placed AFTER the `[data-sub]` check so an inner sublink wins. `components.css` (+228 B): `.pf-vcard{ cursor:pointer }` + `:focus-visible` gold outline (content UNTOUCHED). **PARSE:** views.js exit 0.

**RIG VERIFY (L19 hit-tested, scrolled into view):**
- Card: `data-value-id="care-and-love"`, `cursor:pointer`, `role="link"`, `tabindex=0`; sublink present (`data-sub="rigsub"`).
- **Card-body tap** (hit-tested on `.pf-vname`, top el = `pf-vname`) → `location.hash = "#value/care-and-love"`. ✓
- **Sublink tap** (hit-tested on `.pf-sublink`, top el = `pf-sublink`) → `location.hash = "#subtheory/rigsub"`, NOT the value page. ✓ (sublink wins; content preserved.)
- **Door isolation (grep):** `data-value-id` emit = **1** (only `.pf-vcard`); handler route = 1; the 5 deferred sites UNTOUCHED — shelf `vchip` filter (1), mark-chip `editIdx` edit (1), `.pf-vchip` strip `_pfLightValue` (1) all intact.

---

## Build totals (S1-1 … S1-3)

| file | Δ bytes (LF) | parse |
|---|---|---|
| js/state.js | +5,819 | OK |
| js/views.js | +9,257 | OK |
| js/intros.js | +431 | OK |
| js/yumi-brain.js | +144 | OK |
| assets/components.css | +4,379 | (renders clean) |
| **total** | **+20,030** | — |

Foundations byte-lock untouched (to re-md5 at ship). Deltas above the low proposal floors, driven by the NORMALIZER LAW + full page render + full daylight skin; classified as necessary code+comments, not bloat.

---

## S1-4 SHIP GATE — red-team + fixes + bump + commit

**RED-TEAM (fix-red-team, fresh Sonnet, declared NO authoring context, re-derived parse/bytes/EOL/coverage independently): NOT clean — 1 BLOCK + 3 NOTEs + 2 procedural.** It confirmed under independent re-derivation: the shim/write-normalizer Firestore round-trip (all 12 `saveProfileToFirestore` callers route through the normalizing `getProfile`; `.set()` object-transparent, no `undefined` fields), exhaustive `.name` coverage (incl. the 2 `.join` sites fixed transitively), ES3 compliance, door ordering/collision safety (`data-value-id` ≠ `data-value`; sublink checked first), the 3 deferred sites genuinely untouched, and T3 honestly documented.

**BLOCK 1 — `.pf-vcard` door keyboard-inoperable — FIXED + re-verified.** The card shipped `tabindex=0`/`role=link`/`:focus-visible` but the profile keydown delegate (views.js:20461) whitelisted `[data-value]` not `[data-value-id]` → Tab+Enter/Space did nothing (Space scrolled). Root cause of the miss: S1-3's L19 verify only mouse-hit-tested; the keyboard path was never walked. **Fix:** `+[data-value-id]` in the keydown selector. **Re-verify (rig):** card focusable; **Enter → `#value/care-and-love`**; **Space → routes + `defaultPrevented=true`** (scroll suppressed).

**NOTE 3 — silent truncation >2000 chars — FIXED.** Composer `<textarea>` now carries `maxlength="2000"` (matches `setValueStatement`'s cap; the field can't accept text that would be silently cut). Rig: `maxlength="2000"` present.

**NOTE 4 — blanked statement orphaned its history behind the unwritten UI — FIXED.** Save handler now no-ops on an empty/whitespace composer (`renderStatement(); return;`) — never blanks a written statement. Rig: cleared → Save → statement UNCHANGED, stays WRITTEN.

**NOTE 2 — `id`-collision mis-route (DOCUMENTED residual, not fixed).** `valueSlug` collapses near-duplicate names (`"Praxis"`/`"praxis"`, `"Care"`/`"Care!"`) to one `id` while `setProfile`'s name-dedup keeps both entries distinct → the `.pf-vcard` door + `_valueResolve` route by `id` first-match, so one value's card can open the other's page. No data lost (each keeps its own statement). Narrow trigger (custom near-duplicate names), same-account only. **NOT fixed:** a robust fix changes the "stable slug derived once from the name" id-scheme (Preston's spec) or the merge-by-name semantics — a deliberate call, not a ship-time patch. **Named risk → carried to a future hardening / R10 close.**

**PROCEDURAL 5 (data-loss tier):** per FIX-PROTOCOL §5 Path C (Build-3 smoke absent), this does NOT self-ship. Handled: commit **LOCAL only**, emit the FINAL-PASS SUMMARY, HALT for Preston's push word (his human read) — never a self-push.
**PROCEDURAL 6 (fork provenance):** the additive-doors ruling WAS Preston's (his GO message), now recorded verbatim-in-substance in `r10-s1-recon.md` §"HALT-1 RULING". Confirmed, not agent-assumed.

**Post-fix gates:** parse-check views.js exit 0; source total **+20,470 B** (state +5,819 · views +9,697 · intros +431 · yumi-brain +144 · css +4,379); foundations md5 **UNCHANGED** (`070679b0…` / `772886c0…`). CACHE_VERSION bump `praxis-v3.275 → v3.276` rides the commit.

**Commit:** LOCAL, path-explicit (5 source + sw.js + 2 checkpoints). **HALT 2 = Preston's push word.**

---
