# THE ARC STANDARD — ACCEPTANCE CARD (round CLOSED)

**Round:** THE ARC STANDARD (arc interior + sub-theory writing surface — the
program's flagship). **Closed:** 2026-07-23 on Preston's **full felt PASS** of the
whole vision, live on praxis-reading.netlify.app.
**Ships:** `591c702..c5a69f4` (8 commits), CACHE_VERSION v3.246→**v3.251**.
**Brief (binding + §11 as-built):** `docs/studio/arc-standard-brief.md`.
**Build records:** `docs/checkpoints/arc-standard-recon.md`, `-s1.md`, `-s2-s4.md`.

## The felt arc

**S1 field gate → FAIL on composition, foundation RATIFIED** ("the foundation is
right; the marks sit in the upper band of a viewBox far taller than their spread,
stranding a void"). **Fix-forward from Preston's words** (no revert — R-SHELF S5
precedent): S-FELT composed the field into one screen. **Final felt pass (whole
vision, live) → PASS.** The signature — the field drawing close (the zoom) — landed.

## The §3 laws — verbatim, as PASS rows

Each law below is quoted from the brief's §3 (the binding acceptance criteria);
every one verified with computed-style / DOM evidence, and on **real account data**
at the densest arc for the composition laws (the round's own stated gate — see
lessons L16). Evidence pointers are to the build checkpoints.

| # | law (verbatim) | verdict | evidence |
|---|---|---|---|
| 1 | **One ground.** The arc interior is one continuous parchment field — itself a lit sheet on the Hour's twilight, world margins visible. Nothing floats on the field as a panel. | **PASS** | `.af-field` computed `bg rgba(0,0,0,0) · border 0px · shadow none`; sheet `rgb(253,249,238)` on transparent-body twilight (real data). s2-s4 §Fidelity, §Final |
| 2 | **One world.** Field, clearing, and page are one place at three distances; brightness = depth of attention; no ground flip anywhere. | **PASS** | S2: field→clearing→page distances; the zoom is a camera transform, not a ground flip. s2-s4 §S2 |
| 3 | **The horizon speaks.** The question stands on the horizon at every distance's edge; on finishing, the answer joins it — and nothing else ever rides the sky except value embers when they exist. | **PASS** | question(=title, §11) on `.af-sky`; answer = newest finished sub's answeringLine; 0 values → no embers. s1 §2, s2-s4 §Final |
| 4 | **Sparse-honest.** Empty carries no furniture: an ember arc is open ground, the question on the horizon, and at most one `--ink-3` line. An empty values state is nothing. | **PASS** | blank title → pre-question invitation only; 0-book arc → one soil line; values panel dissolved. s1 §2 |
| 5 | **One gate.** All arc chrome lives in the Gate Row: name + kicker, one act, one door, one overflow. No control ever floats elsewhere on the field. | **PASS** | **0** controls on `.af-field` (real data); kicker · +Sub-theory · door · ⋯. s2-s4 §Final |
| 6 | **Form first.** A mark is told apart by silhouette before hue; no two marks in an arc share a full identity combination; nothing has composed identity before the mint. | **PASS** (uniqueness = composer-guaranteed; derived residual named) | real densest arc: 6 marks, 4 silhouettes / 5 pigments / varied treatments; composer closes taken triples + refuses Plant. s2-s4 §S4, §Final; §11 residual |
| 7 | **Approach, never teleport.** Every tap moves one distance deeper; the door is always one honest tap away and never an accidental one; the camera moves, the arrangement never does. | **PASS** | tap1 concentrate + "Open →" door, tap2 arrive; law-7 positions IDENTICAL ×3 under scale(2.8); drag round-trips EXACT (BLOCK-1 fix). s2-s4 §S2, §Red-team |
| 8 | **Honest doors.** The lifecycle seat shows only the act that can be taken, named for its destination. | **PASS** | ember→Graduate→ / graduated→Publish→ / published→Published; zero-marks→no door. s1 §2 |
| 9 | **Soil below, sky above.** Books stand at the field's foot in the shelf dialect; the question stands in the sky; the growth between them belongs to the user's hand alone. | **PASS** | 5 real covers in the soil; **0** canvas book-squares (`data-st-book-id`). s2-s4 §Final |
| 10 | **Threads are gold.** All connection chrome speaks the gold-ink family; flow is subtle, CSS-only, and still under reduced-motion. | **PASS** | 3 non-scaling gold gradient threads; cyan retired from the field. s2-s4 §Fidelity |
| 11 | **The pull is physical.** Sources rise from the soil into the field, and pass from the field into the prose by the hand — tap raises, drag weaves; no dropdown ever stands between a book and the argument it feeds. | **PASS** | soil-reach (tap a book → passages rise), native `<select>` retired; drag-across-edge weave at the caret point. s2-s4 §S3 |
| 12 | **Writing has a body.** Prose lives on a real canvas — bounded, lifted, with honest focus — never a bare textarea; every control on these surfaces speaks the kit dialect. | **PASS** | `createWritingCanvas` (already adopted, §11 G6); dress + honest focus shipped at S1. s1 §3 |

**Covenant tripwire (carried, absolute):** the user authors the arrangement; the
app composes defaults and offers tidying, never auto-reflows; position is expressive
spatial memory, never interpreted. **Verified:** the drag round-trips exactly, the
composed layout uses authored coords verbatim, and no `evidenceLayout`-class data
enters context assembly. (The first cut broke this with a bbox-normalize snap-back;
the red-team caught it; the BLOCK-1 fix restores it more strictly than before.)

## Gates

- **praxis-reviewer (Sonnet):** all mechanical gates PASS (ES3, byte-locks, sw.js,
  CSS balance, no new hex, data-write safety, EOL). Its two BLOCKs = the same two
  the red-team caught, already fixed. Residuals cleaned (`b4b1097`).
- **fix-red-team (Sonnet):** 3 BLOCK / 1 HOLD / 3 NOTE across two passes — **all
  addressed** (`5ca540e` + the S1 walk-lens fix). Headline: the drag snap-back
  (covenant) and the walk-lens identity mismatch, both fixed and verified.
- **Live smoke (v3.251):** served-byte match ×7, sw.js both refs, all markers
  present / retired at 0, live arc route renders the composed field, S2 approach
  fires, **console clean.**

## Felt pass — recorded

> **FULL FELT PASS = PASS — the whole vision, live, ratified.** — Preston, 2026-07-23

Byte-locked foundations untouched throughout (`lumen-amber.css` 070679b0… ·
`marks.js` 772886c0…). The `mockup/arc-standard` branch stays on origin as the
reference archive.
