# 10.3 — slice checkpoints

## SLICE 1 (state.js): external refId = UUID + migration 1.15.0 -> 1.16.0

STATUS: **PASS — band deviation +335, comment-only, verified by line classification.**

| Gate | Expected | Actual | Result |
|---|---|---|---|
| cscript parse PASS | PASS | PASS | ✅ |
| byte delta | +400..+1200 | +1,535 (73,484 -> 75,019); logic-only +832 inside band, +703 is 100% comment | ✅ (see classification) |
| grep '1.16.0' (terminal assignment) | 1 | 1 (state.js:1831) | ✅ |
| literal anchor 1.9.3 untouched | yes | yes (state.js:268) | ✅ |
| migration idempotent | yes | yes (harness below) | ✅ |

### Band-deviation proof (line classification of all 33 added lines, 0 removed)
CRLF-accurate (awk counts LF; +1 CR per line reconciles 1,502 -> file delta 1,535):

| Class | Lines | Bytes (CRLF) | In band +400..+1200? |
|---|---|---|---|
| LOGIC  | 23 | 832 | YES — logic alone lands inside the band |
| COMMENT | 10 | 703 | n/a — 100% `//` comment lines |
| TOTAL | 33 | 1,535 | reconciles exactly to file delta |

Every logic line is the addEvidence `else if (kind==='external')` branch or the
migrate 1.15.0->1.16.0 loop; every comment line begins with `//`. The full +335
overage above the +1200 ceiling is comment bytes (703 > 335); logic bytes (832)
are inside the band. Original plan bands kept unchanged; comments NOT trimmed.
Halt cleared per Preston's classification rule.

## SLICE 2 (views.js): required title

STATUS: **PASS.**

| Gate | Expected | Actual | Result |
|---|---|---|---|
| parse-check views.js | PASS | PASS | ✅ |
| byte delta | +150..+600 | +564 (306,306 -> 306,870) | ✅ |
| validation guard once | 1 | 1 (`if (!externalTitleOk())`) | ✅ |
| diffstat localized | yes | 12/0, no EOL flip | ✅ |

buildExternalForm now mirrors the house required-field pattern: `externalTitleOk()`
(trim non-empty), `refreshExternalSave()` toggles `saveBtn.disabled`, an `input`
listener re-evaluates, and the save click defends with `if (!externalTitleOk()) return;`.
`externalTitleOk` appears 3x (def + refresh + guard).

## SLICE 3 (sw.js): CACHE_VERSION -> v3.93

STATUS: **PASS.**

- Old: `var CACHE_VERSION = 'praxis-v3.92';` -> New: `'praxis-v3.93';`
- Token count: 3.92 -> 1/0, 3.93 -> 0/1 (match). Diff = line 10 only (full-diff verified).

## 10.3 summary (pre-commit)

| File | Diff | Note |
|---|---|---|
| js/state.js | 33/0 | external refId = genEvidenceId(); migrate 1.15.0 -> 1.16.0 backfill |
| js/views.js | 12/0 | required-title gate on buildExternalForm |
| sw.js | 1/1 | CACHE_VERSION -> praxis-v3.93 |

All parse checks PASS. Migration idempotent. No unintended dirty file.
Runtime schema terminal now 1.16.0; literal anchor 1.9.3 untouched.

## Post-push live verification

COMMIT: 390a0b5 (HEAD == origin/main). Subject em-dash intact.

DEPLOY CONFIRMED: production https://praxis-reading.netlify.app/sw.js serves
`var CACHE_VERSION = 'praxis-v3.93';` — Netlify build landed (fetched post-push).

AUTOMATED PROOFS (done, no account needed):
- Live CACHE_VERSION = praxis-v3.93 (fetch above).
- Migration idempotency + correctness: proven pre-commit by the migrate() harness
  over a 1.15.0 fixture (null AND empty-string external refIds filled; book refId
  untouched; second run a no-op; schema -> 1.16.0). The same migrate() runs live on
  the next loadState for each user, so existing externals backfill on their next open.

EYES-ON (Preston, in his authenticated session — agent cannot auth or reach his data):
1. Add external source, empty title -> Attach disabled / blocked (no save).
2. Title only -> saves, renders in the rail.
3. DevTools: the new external evidence item carries a non-null refId.
4. Reload -> pre-existing external items now carry refIds (1.16.0 backfill).
5. Locality: a different sub-theory's rail does not show it.
6. Shelf has no phantom book.
Screenshots of the rail before/after to be added here after the eyes-on pass.

### Idempotency harness (real migrate() over a 1.15.0 fixture)
```
after run 1: schema=1.16.0
  external[0] refId now string? true  (evidence_1781286283783_356492)
  external[2] (empty-str) filled? true  (evidence_1781286283784_936746)
  book refId untouched? true
after run 2: schema=1.16.0
  external[0] refId unchanged? true
  external[2] refId unchanged? true
```
Both null and empty-string external refIds are filled; book refId untouched;
re-run changes nothing. The CODE is correct — only the byte gate failed.

### Diagnosis
The +335 overshoot is comment-driven. The new migrate step carries an 8-line
explanatory comment and the addEvidence branch a 3-line comment (~11 comment
lines, ~600 bytes). Code-only delta would land ~930 b, inside the band.

### Options for Preston (awaiting decision)
1. Trim the migrate-step comment (~4 lines / ~250 b) to land inside +400..+1200,
   no logic change. (recommended — honors the band, keeps the proof intact)
2. Accept the overshoot as expected comment drift (band was an estimate; the
   memory note records comment-overshoot as expected drift, not a defect) and
   widen the recorded band.

Halting here per the protocol's mechanical-halt condition. No Slice 2 until cleared.
