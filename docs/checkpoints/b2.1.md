# B2.1 — categorization developed (executor build report)

**Date:** 2026-07-08 · **Base:** `988384e` (B2) · **Status:** gate PASS; praxis-reviewer =
**PASS / cleared to commit** (`docs/checkpoints/b2.1-review.md`); its one nit (unstable
gap-sort tie-break) FIXED post-review and re-verified. UNCOMMITTED — awaiting commit words.
**Scope: exactly 2 files** — `tools/studio-build` + regenerated `docs/studio/builder.html`.
No sequence.md, no surface ledgers, no law files, no app files.

## The new categorization (generator-only)

- **7 journey groups, fixed order** everywhere (sidebar + overview): ARRIVE · READ · WRITE ·
  SHARE · YUMI · FIND · SYSTEM. Old CORE retired; **Onboarding moved YUMI→ARRIVE**;
  `arcs`/`arc-detail` display names → **"Arcs page" / "Arc interior"** (kills the near-dup trap).
- Union complete + disjoint: every 22 surfaces + cross-cutting in exactly one group (23=23).
- **Group rollups** on every header (sidebar + overview): group orb + "N gaps · closed/total".
  Orb state: unlit (0 closed) / soft (some) / bright (all).
- **Journey order** is the fixed order; sidebar surfaces stay in map order (stable nav);
  overview grid tiles **gap-sorted DESCENDING** within each group, ties in map order.
- **Anchor jumps**: each sidebar group header links `#group-<name>`; overview sections carry the id.
- Reach-map hues follow the new groups (ARRIVE #f2c25a · READ #e07a52 · WRITE #a9b98c ·
  SHARE #8590d8 · YUMI #f8e078 · FIND #e8b068 · SYSTEM #b8896c).
- Everything else from B2 preserved (Plan thread, ticks, touch chips, PROPOSED template,
  CONNECTED strips, brief glyphs, mono discipline, cold-start, single night panel, inlined depth css).

## Gate evidence

| gate | result |
|---|---|
| 7 groups, journey order, sidebar + overview | ARRIVE READ WRITE SHARE YUMI FIND SYSTEM (both) ✓ |
| CORE absent as group label | 0 ✓ |
| Onboarding under ARRIVE (sidebar + grid) | YES / YES ✓ |
| "Arcs page" / "Arc interior"; bare Arcs/Arc gone | present; `>Arcs<`=0, `>Arc<`=0 ✓ |
| rollup arithmetic | 17+16+52+9+5+3+44 = **146** = global; closed/total = **0/23** ✓ |
| gap-sort (WRITE) non-increasing | 18,14,8,5,4,3 ✓ |
| gap-sort tie → map order (SHARE reader/walk) | Reader before Walk ✓ (post-fix) |
| anchors sidebar↔overview | 7/7 ✓ |
| night panel == 1 | 1 ✓ |
| universal-depth v1.2 header inlined | present ✓ |
| ES3 runtime parse | PARSE OK ✓ |
| stamp HEAD == repo | 988384e ✓ |
| tags balanced | div 199/199 · a 97/97 · section 34/34 ✓ |
| NON-GOALS | only generator + builder changed; 0 law/app/ledger/sequence ✓ |

## Reviewer nit — FIXED

`heat_group()`'s `sort -rn` reversed ties too (reverse-alpha), so equal-gap tiles rendered
out of map order. Fixed: sort with a map-order index tie-break (`sort -k1,1nr -k2,2n` over
`<gap> <index> <slug>`). Verified in the built HTML: WRITE unchanged; SHARE ties (Reader/Walk,
both 0) now render in map order.

## Byte deltas (vs committed 988384e)
| file | committed | now | Δ |
|---|---|---|---|
| tools/studio-build | 43289 | 45463 | +2174 |
| docs/studio/builder.html | 121747 | 124632 | +2885 |

## Residuals (non-blocking)
- ~2.5 min Windows generation (fork() emulation) — environmental.
- Inlined `universal-depth.css` has one extra trailing blank line vs the source file (the `cat` +
  appended `\n` in the generator) — **pre-existing** since B2 (reviewer reproduced it at 988384e),
  cosmetic, not introduced here.
- Preston's felt pass on the live builder is the visual gate.
